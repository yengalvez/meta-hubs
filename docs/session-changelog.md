# Session Changelog

## 2026-08-12 (H5-A: preflight local sin GitHub)

| Time | Action | Result |
|------|--------|--------|
| 2026-08-20 | Primer `--full` sobre el plan auditado y correccion causal de su STOP. | El gate llego a 113 checks recovery verdes y fallo por una carrera del stub: varios watchers compartian `serialization-lease.next`. No alcanzo suites full-only ni toco produccion. Refresco sintetico acotado a modos unitarios; restore concurrente 49/49, writer 55/55 y stale/helper 75/75 PASS; nuevo candidato de codigo `42a4142`. |
| 2026-08-20 | Se congelo el candidato local auditado de H5. | Commit de codigo/documentacion `3b36cbd`; submodulos Hubs `ce8390a` y Hubs Cloud `7e56f90`; arbol sin procesos recovery residuales. F1 cerrada y el unico siguiente gate es `./scripts/verify-project.sh --full`. |
| 2026-08-20 | Auditoria final independiente y local del ciclo H5, sus repos, bundle, recibo, gate y estado live. | El trabajo comercial se conserva y el avance razonado queda en 85 %. Se confirma sobretrabajo en recovery y una duplicacion real del full. El intento 855/16 murio antes de las suites full-only; no era aceptable como full parcial. Estado live fail-closed exacto y preflight cold-rebind read-only PASS. |
| 2026-08-20 | Se corrigieron solo las dos familias rojas conocidas y el despacho duplicado del gate. | Stale/helper 75/75 y writer-monitor 55/55 PASS; el monitor se recoge al agotar su handoff y `h5-final` termina antes de repetir la bateria default. Produccion no cambio. `PLAN_ACTUAL.md` v2 reduce el cierre a un full valido, una limpieza, un restore, aceptacion comercial e integracion. |
| 2026-08-13 | El gate de catalogo previo a H5-B19 comprobo version, region y nodo antes de crear. | `ams3` y `s-4vcpu-8gb` disponibles; `1.34.8-do.2` retirada. La opcion minima es `1.34.10-do.1` (misma minor). STOP sin recursos ni coste nuevo hasta autorizacion explicita del cambio de parche. |
| 2026-08-13 | H5-B17 termino ambas verificaciones cifradas y emitio el recibo privado. | Local y Dropbox decrypt/rehash PASS; Dropbox uploaded=1/uploading=0 con identidad/version y readback completo. Recibo `freeze-bundle-receipt-v1` 0600 valido, 2 copias y 13 imagenes; preflight greenfield PASS. |
| 2026-08-13 | H5-B18 retiro por UUID el cluster/nodo, LB y dos volumenes autorizados. | Delete selectivo retiro cluster/nodo/LB; los dos volumenes quedaron desconectados tras el timeout y se borraron por sus UUID exactos. Estado final: cluster 0, Droplets 0, LB 0, volumenes 0, firewalls DOKS 0; `voice-chat` presente. Hibernacion comercial demostrada. |
| 2026-08-13 | H5-B17 cifro el bundle y la custodia OCI en un paquete de 1,455,943,712 bytes y creo sus dos copias. | Local decrypt/rehash PASS. Dropbox `isUploaded=1`, `isUploading=0`, identidad/version remotas y readback ciphertext completo con hash exacto. Google vacio el portapapeles antes del decrypt post-upload; queda solo recopiado de clave, decrypt/rehash final y recibo `0600`. Cero cambios DigitalOcean. |
| 2026-08-13 | Se refresco read-only el inventario DigitalOcean previo a H5-B17 y se prepararon las dos ubicaciones cifradas. | Coincidencia exacta: una cuenta, un cluster/nodo `s-4vcpu-8gb` en `ams3`, un LB, dos volumenes de 10 GiB, `voice-chat` a conservar y dos firewalls DOKS solo para reconciliar. UUIDs congelados en expediente privado `0600`; cero mutaciones cloud. El cifrado espera solo la confirmacion de identidad de Google. |
| 2026-08-13 | H5-B16 creo y publico el bundle conjunto DB + ret-pvc. | Manifiesto valido con 9 ficheros; DB 356 relaciones/94 migraciones/18 hubs/33 activos y storage 33 pares completos. Estado final `12/12`, writers `5/5`, barrera/lock/Lease/helper/policy ausentes. H5-B17 activo: dos copias cifradas verificadas. |
| 2026-08-13 | H5-B15 paro en el probe helper antes de cualquier scale-down: el constructor shell omitio `terminationGracePeriodSeconds=1` aunque policy, Pod y helper Node ya lo exigian. | La barrera rechazo correctamente; estado final `12/12`, writers `5/5`, cero residuos y DO intacto. Se igualaron ambos constructores y se añadió regresion directa; matriz final `78/78`, helper `17/17`, Bash/ShellCheck/diff-check verdes. H5-B16 activo. |
| 2026-08-13 | H5-B14 devolvio `storage_backup_child_stage:helper-cleanup`: DB, tar, validacion y postcheck ya estaban verdes; el Pod quedaba en borrado Foreground mas de 180 s. | Rollback `12/12`, policy residual retirada y DO intacto. Solo el helper sin dependientes cambia a Background graceful, UID exacto, espera NotFound y grace=1; no force. Smoke live sin downtime: NotFound en 4 s, cero residuos, writers `5/5`. Helper `17/17`, H5-B3 `77/77` y checks estaticos verdes. H5-B15 activo. |
| 2026-08-13 | H5-B13 valido la DB y paro en storage con `stream status=1`. | Rollback `12/12`, writers `5/5`, cero bundle y barrera/lock/Lease ausentes. Policy residual sin Pods retirada por UID/RV. El mismo `tar|gzip` read-only sobre ret-pvc pasa `0/0`: datos legibles; queda supervisor o limite post-stream. Se añadio un enum diagnostico cerrado para un unico H5-B14; Bash/ShellCheck/diff-check verdes. |
| 2026-08-13 | H5-B12 valido otra vez la DB y llego al helper storage, pero el monitor local no completo su barrido inicial. | Rollback exacto `12/12`, writers `5/5`, cero bundle, lock/Lease ausentes y DO intacto. Dos policies huerfanas B11/B12 sin Pods se retiraron por UID/RV. En freeze se elimino solo el monitor storage redundante y se reforzaron los limites exactos antes/despues del tar; legacy/durable no cambian. Barrera `77/77`, hijos `161/161`, Bash/ShellCheck/diff-check verdes. H5-B13 queda activo. |
| 2026-08-12 | H5-B11 valido la DB real y paro antes del Pod storage porque el API omitio las listas vacias ingress/egress del NetworkPolicy deny-all. | DB verde: 356 relaciones, 94 migraciones, 18 hubs, 33 activos. Rollback `12/12`, writers `5/5`, cero residuos/bundle y DO intacto. Dry-run live aislo la forma; comparador estrecho y foco storage `88/88`, Bash/ShellCheck/diff-check verdes. H5-B12 queda pendiente tras corte anti-loop. |
| 2026-08-12 | H5-B10 reprodujo el validador con un dump privado read-only y separo sus predicados. | Causa exacta: PostgreSQL 12.19 añade un unico footer canonico `--` despues del marcador final. Parser estrecho corregido; foco `7/7`, dump real completo PASS, Bash/ShellCheck/diff-check verdes. Temporal retirado por borrado recuperable. H5-B11 queda como unico checkpoint habilitado. |
| 2026-08-12 | H5-B9 uso la barrera freeze sin monitor PostgreSQL redundante y termino el stream, pero el validador agregado rechazo el contrato SQL critico. | Cero bundle publicado; rollback exacto `12/12`, cinco writers `1/1`, barrera/lock/Lease ausentes y DO intacto. Autopsia read-only: PostgreSQL 12.19, schemas `3/3`, 356 DDL, tres COPY, marcador y hashes de inventarios criticos coincidentes. Como no se conserva el dump rechazado ni un predicado exacto, STOP sin H5-B10 productivo. |
| 2026-08-12 | H5-B8 supero el arranque del monitor PostgreSQL, pero este fallo durante el stream de `pg_dump`. | Rollback exacto `12/12`, sin barrera/lock/Lease/bundle. Para H5-B9, solo la ruta freeze elimina el monitor redundante: la barrera impide CREATE/UPDATE de Pods y se valida la fuente antes/despues; legacy/durable no cambian. H5-B3 `77/77`, checkpoint children `161/161`, sintaxis/ShellCheck/diff-check verdes. |
| 2026-08-12 | H5-B7 supero barrera/probes y pauso los cinco writers; el monitor PostgreSQL no completo su primer barrido en 10 segundos y `pg_dump` no empezo. | Rollback exacto `12/12`, sin barrera/lock/Lease/bundle. Se separo startup=30 s de frescura=10 s y se añadieron etapas diagnosticas cerradas. `stream-guards` `81/81`, sintaxis/ShellCheck/diff-check verdes. |
| 2026-08-12 | H5-B6 supero los dry-runs y creo la pareja, pero paro en el probe; un smoke allowlisted aislo `probe-helper-denied`. | El API omite `request.subResource` en CREATE base. Se adopto `!has(...) || == ''`, manteniendo subrecursos reales denegados. Helper `17/17`, smoke live PASS, H5-B3 `77/77` y checks estaticos verdes. Cada intento dejo `12/12`, sin barrera, lock, Lease, bundle ni cambios DO. |
| 2026-08-12 | H5-B5 llego a `quiescence/fence-create`: el API materializo `matchPolicy=Equivalent` y `objectSelector={}` tambien en la binding. | STOP anterior al scale-down; cinco writers `1/1` y cero bundle. Rollback exacto binding -> policy -> lock -> Lease; estado final `12/12`, sin residuos ni cambios DO. |
| 2026-08-12 | Se sustituyo la correccion incremental de defaults por un preflight server-side de policy y binding anterior a cualquier CREATE persistente. | Focal `49/49`, regresion H5-B3 `77/77`, Node/sintaxis/ShellCheck/diff-check verdes y prueba contra el API real `server_dry_run=pass`; policy y binding siguieron ausentes. H5-B6 queda como unico candidato activo. |
| 2026-08-12 | El propietario autorizo continuar sin confirmaciones rutinarias con todo el ciclo H5 ya delimitado: H5-B5, bundle, dos copias cifradas, retirada de recursos DO inventariados, recreacion equivalente, restore y aceptacion. | La autoridad no se extiende a recursos/costes/topologia nuevos, secretos no recuperables, estado ambiguo ni recursos ajenos; cualquiera de esas diferencias produce STOP. H5-B5 pasa a ser la unica accion activa. |
| 2026-08-12 | El segundo preflight H5-B4 uso el snapshot privado correcto `13/13`, pero omitio `EXPECTED_NAMESPACE_UID` y `EXPECTED_RET_PVC_UID`. | Volvio a parar antes de Lease/lock/barrera/downtime. Una auditoria estatica confirmo que no faltaban mas entradas; la invocacion operativa capturo ambos UID en el mismo contexto inmediatamente antes de ejecutar. |
| 2026-08-12 | H5-B4 alcanzo `quiescence/fence-create`: el API server creo la policy y materializo defaults (`Equivalent`, selectores vacios y campos null) que el comparador pre-servidor no normalizaba. El binding nunca se creo, los writers nunca bajaron y no hubo bundle. | La policy sin binding era inerte. Un rollback revisado adquirio Lease, revalido UID/RV, retiro policy y lock por precondiciones exactas, confirmo `NotFound` y libero Lease. Estado final `12/12`, cinco writers `1/1`, cero residuos y DO intacto. El comparador normaliza solo esos defaults exactos; test `15/15`, Node/diff-check verdes y revision adversarial GO sin P0/P1/P2. No se repitio el checkpoint; H5-B5 requiere autorizacion nueva. |
| 2026-08-12 | El primer preflight H5-B4 selecciono por error la copia privada historica de julio y fallo con `The local runner image does not identify one exact checkpoint generation.` | STOP anterior a cualquier efecto: `12/12`, Lease/lock/barrera/bundle ausentes. La causa exacta era `OVERRIDE_BOT_RUNNER_IMAGE` ausente. Se genero sin mostrar valores un snapshot privado `0600` que conserva la fuente canonica y actualiza solo dominio, runner `No` y pins no secretos; coincide `13/13` con las imagenes live. Esta evidencia permite un unico intento operativo, no una repeticion a ciegas. |
| 2026-08-12 | El propietario autorizo expresamente una unica ventana productiva H5-B4 para crear/probar la barrera temporal, pausar cinco writers, crear/validar el bundle, retirar la barrera y reanudar. | El alcance excluye cualquier borrado DigitalOcean y obliga a STOP ante ambiguedad. Preflight inicial: contexto exacto, Namespace sano, `12/12` Deployments, `13/13` contenedores por digest, lock y barrera ausentes, 784 GiB libres; inventario DO sin cambios materiales. |
| 2026-08-12 | H5-B3 implemento localmente la opcion segura elegida: una policy/binding temporal `failurePolicy=Fail` que niega CREATE/UPDATE de Pods y `ephemeralcontainers`, salvo el helper storage exacto de solo lectura; queda ligada a Namespace, operacion, lock y Lease y se revalida durante DB/storage. | Matriz H5-B3 `73/73`, helper puro `8/8`, Bash/Node syntax, ShellCheck y diff-check verdes. La revision adversarial detecto y cerro un P1 post-CREATE mediante latches policy/binding; el dictamen final fue GO, cero P0/P1/P2. No hubo full, GitHub, Kubernetes real, produccion ni cambios DigitalOcean. H5-B4 espera autorizacion concreta para una unica ventana productiva sin borrado DO. |
| 2026-08-12 | Se separo H5 en preparacion local H5-A y ventana mutante H5-B. Se inventariaron en solo lectura runtime, DNS, DOKS, nodo, Load Balancer, volumenes y costes; se comprobo tambien el rechazo del checkpoint sin autorizacion de downtime. | Produccion quedo intacta. Los `12/12` Deployments estaban disponibles, `13/13` contenedores por digest, un Load Balancer y dos PVC/volumenes `10 GiB` con reclaim `Delete`; DNS externo apunta al Load Balancer actual. |
| 2026-08-12 | Se ensayo cifrado/descifrado y rehash con un artefacto no sensible entre local y Dropbox; despues se archivo el runtime por digests exactos usando ORAS `1.3.3` verificado por SHA-256. | Layout OCI privado de `1,378,619,392` bytes: `12/12` digests vivos presentes, `12/12` referencias legibles offline y `214/214` blobs SHA-256 validos. La credencial temporal GHCR se retiro del config. GitHub Packages llevaba USD `0` facturados y presupuesto USD `0` con `Stop usage: Yes`; no hubo CI ni coste facturado. |
| 2026-08-12 | Se preparo la lista de efectos H5-B. | Retirar solo cluster/nodo, Load Balancer y los dos volumenes; conservar DNS externo y recursos ajenos, y reconciliar los firewalls gestionados por DOKS. |
| 2026-08-12 | Se reviso el requisito de custodia para evitar sobreingenieria. | Para el primer ciclo se aceptan dos ubicaciones: copia cifrada local fuera de Dropbox y copia cifrada externa en Dropbox. Un tercer destino es opcional. Google Password Manager sincronizado se usara para accesos y clave del bundle; el plan solo conserva referencias opacas. |
| 2026-08-12 | El propietario confirmo Google Password Manager y se comprobo en lectura que el gestor es accesible desde la cuenta Google, sin abrir contraseñas. | H5-A queda cerrada `10/10`. Se fijaron solo referencias opacas y se preparo el texto exacto de autorizacion H5-B. Produccion y DigitalOcean permanecen intactos. |
| 2026-08-12 | Google Password Manager mostro una alerta general de credenciales expuestas; no se abrio la lista ni se inspeccionaron secretos. | H5-B incorpora un STOP previo: comprobar de forma redactada las entradas H5 y rotar cualquier afectada antes del primer efecto. |
| 2026-08-12 | El propietario autorizo continuar con H5-B. Se completaron inventario y preflight read-only; Google solicito confirmar una llave de acceso y Chrome bloqueo por politica la automatizacion de su pagina interna de altas. | Se genero una clave H5 de 256 bits solo en memoria y se coloco en el portapapeles para que el propietario la guarde manualmente. Se aplico STOP antes del downtime: `12/12` Deployments disponibles, sin recovery lock y DigitalOcean intacto. |
| 2026-08-12 | El propietario confirmo el alta manual de la clave y se inicio H5-B. Tres invocaciones del checkpoint terminaron en `pre-publication`: selector runner ausente, contexto explicito ausente y, finalmente, estrategia live `RollingUpdate` frente al contrato `Recreate`. | Ninguna invocacion pauso writers ni creo lock; produccion permanecio `12/12`. Se prohibio una cuarta tentativa. La revision adversaria encontro ademas que deben ligarse revision remota/hash/tamano de la copia Dropbox, UUID exactos de los cuatro recursos y techo mensual; el ensayo K3s no sustituye una receta greenfield generada para el runtime legacy vivo. H5-B0 queda como unica correccion local antes de cualquier efecto. |

## 2026-08-12 (H4 CI final: baseline fail-closed)

| Time | Action | Result |
|------|--------|--------|
| 2026-08-12 | Se inspecciono el run autoritativo `31525770598` sobre `d0fc186`. PostgreSQL 12/14, Gitleaks, Actionlint y ShellCheck quedaron verdes; la regresion ejecuto los `865` casos y termino `860` verdes con exactamente los cinco rojos Linux historicos. | Los cinco pertenecian a expectativas antiguas de rollback/reentrada automatica ya congeladas, no a H2/H3. Se corrigio solo el oraculo: acepta rollback exacto o parada fail-closed exacta con lock retenido, writers confirmados y cero publicacion; cualquier estado intermedio sigue rojo. Producto, produccion y DigitalOcean no cambiaron. Focos `47/47`, `54/54`, `89/89`, sintaxis y ShellCheck verdes. La confirmacion final `31546745988` sobre `09af04f` paso static-security, las `865` regresiones y PostgreSQL 12/14. |
| 2026-08-12 | Tras el CI verde se fusiono Hubs Cloud PR `#23` (`7de73e9`) y despues root PR `#16` (`45faaf6`), ambos con mensajes `[skip ci]`. | H4 cerrado sin duplicar gates ni tocar produccion/DO. El checkout oficial `/Users/Shared/Gits/YenHubs` no se sincronizo porque no estaba limpio y conserva la investigacion avanzada; se preservo sin sobrescrituras. H5 queda expresamente detenido ante la frontera de produccion y coste. |

## 2026-08-11 (H4 local integral)

| Time | Action | Result |
|------|--------|--------|
| 2026-08-11 | H4 ejecuto una sola invocacion del gate local integral. El bloque comun completo paso recovery `865/865`, seguridad, AUD-065, PostgreSQL real y las suites auxiliares; el comando se detuvo despues por faltar los remotos publicos `upstream` en este worktree. Tras registrar y auditar esos remotos se ejecuto solo el tramo full-only aun pendiente: Hubs `100/100` y build, navegador, capacidad `115/115`, HCCE `68` recursos, bot orchestrator `154/154`, Dialog `2/2`, Photomnemonic `7/7`, Coturn, Spoke `68/68` y build, y Reticulum `461` tests + `5` propiedades. | El gate descubrio dos advisories nuevos y se aplicaron solo los pins compatibles `ip-address 10.3.1` y `postgrex 0.22.4`; npm/Hex audit y las suites afectadas quedan verdes. Hubs Cloud queda en `b0701eb`. No se repitio el bloque comun, no hubo GitHub, produccion, DigitalOcean, secretos ni coste. H4 local queda cerrado; cost gate, un unico CI y fusion ordenada siguen pendientes. |

## 2026-02-09 (Third-Person Camera + Deploy Work)

Time reference: UTC (local CET noted where relevant).

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 10:34Z | Implemented third-person feature in `hubs` on branch `codex/third-person-toggle-ui` (store + camera system + preferences + toolbar button). | Feature completed in code with toggle and persisted preference. |
| 2026-02-09 10:34Z-10:36Z | Ran local validation in `/Users/Shared/Gits/YenHubs/hubs`: `npm ci`, `npm run check`, `npm run lint:js`, `npm run build`. | Passed (webpack size warnings only). |
| 2026-02-09 10:34:57Z-10:42:40Z | GitHub Actions run `21821623890` (`hubs` workflow) for commit `2b4f848279d91e3eadfd5c8e38c9bd3a6dcf1832`. | Completed `success`. |
| 2026-02-09 10:36:08Z-10:43:28Z | GitHub Actions run `21821659957` (`custom-docker-build-push`) targeting GHCR. | Completed `failure` (`permission_denied`, token scope issue while pushing image). |
| 2026-02-09 10:40:28Z | Generated and applied `hcce.yaml` with ingress and HAProxy manual fixes. | Applied successfully. |
| 2026-02-09 10:40:42Z | Re-patched RBAC (`haproxy-cr`) and restarted HAProxy. | HAProxy healthy on `haproxytech/kubernetes-ingress:3.2`. |
| 2026-02-09 10:43:27Z and 10:50:38Z | Deploy tried to pull `ghcr.io/yengalvez/hubs:third-person-toggle-20260209-2b4f84827-latest`. | `ErrImagePull` / `ImagePullBackOff` (image not published). |
| 2026-02-09 ~10:44Z | Executed emergency hotfix: rebuilt client with `RETICULUM_SERVER` + `BASE_ASSETS_PATH`, copied `dist/assets` and `dist/pages/*` into running hubs pod, restarted reticulum. | Frontend served correctly from `https://assets.meta-hubs.org/hubs/`. |
| 2026-02-09 10:47:24Z-10:47:56Z | Restarted and rolled out `dialog` deployment. | Dialog healthy (`dialog-86df9df69b-kp26r`). |
| 2026-02-09 10:52:33Z-10:52:34Z | Normalized `hubs` deployment image back to `hubsfoundation/hubs:stable-3108` to remove failing ReplicaSets. | Cluster returned to clean state (all deployments ready). |
| 2026-02-09 11:34:47+01:00 (CET) | Commit in submodule `hubs`: `2b4f84827`. | `Add third-person camera toggle with UI button`. |
| 2026-02-09 11:53:14+01:00 (CET) | Commit in parent repo: `eb6efd7`. | Updated `hubs` submodule pointer and deployment docs clarification. |
| 2026-02-09 11:00:09Z (12:00:09+01:00 CET) | Re-reviewed and corrected deployment docs in `deployment/README.md` and `docs/project_maintenance.md` (added active hubs image verification and unified custom build command with `RetPageOriginDockerfile`). | Deployment docs left consistent for future runs. |
| 2026-02-09 (user validation) | User confirmed third-person camera works well in real usage. | Functional validation confirmed manually. |

## 2026-02-09 (Avatar Upload Fix + Admin Local Upload + RPM MVP + Deploy)

Time reference: UTC (local CET noted where relevant).

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 12:19:58Z (13:19:58+01:00 CET) | Created commit `9b5ae36ee` in `hubs` implementing: URL normalization in Change Avatar, safer `proxiedUrlFor`/thumbnail fallbacks, admin local `.glb` avatar uploads (batch), and RPM/fullbody skeleton detection + tags. | Feature complete in code; removes `https://undefined/...` proxy breakage and adds disk upload flow. |
| 2026-02-09 12:25:12Z-12:34:18Z | GitHub Actions run `21826862113` (`custom-docker-build-push`) for commit `9b5ae36eedf3d21c9ca50c19ffed6e6cb03df1f9`. | Completed `success` and published `ghcr.io/yengalvez/hubs:rpm-avatar-import-20260209-9b5ae36ee-latest`. |
| 2026-02-09 (deploy) | Updated Kubernetes namespace `hcce` to pull private GHCR images (`ghcr-pull` secret + `imagePullSecrets` on ServiceAccount `default`). | Fixed `ErrImagePull` / `ImagePullBackOff` for private `ghcr.io` tags. |
| 2026-02-09 (deploy) | Rolled out `deployment/hubs` to `ghcr.io/yengalvez/hubs:rpm-avatar-import-20260209-9b5ae36ee-latest`. | Pods `Running`; deployment `hubs` shows correct image via jsonpath. |
| 2026-02-09 (verification) | Verified `https://meta-hubs.org` and `https://assets.meta-hubs.org/hubs/pages/admin.html` are up and serving the new admin UI. | Admin bundle contains `Upload Avatars from Disk` and site returns HTTP 200 with expected CSP/headers. |

## Notes

- The emergency hotfix (`kubectl cp` into running pod) is not durable across full pod replacement.
- Durable production rollout still requires a successfully published custom image and `OVERRIDE_HUBS_IMAGE` update to that image tag.

## 2026-02-09 (Avatar Bootstrap + Admin Local Upload Fix + URL Avatar Import Fix)

Time reference: UTC (local CET noted where relevant).

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 14:15Z-14:18Z | Investigated why Admin local `.glb` avatar upload fails in production. | Found root cause: there were **no base avatar listings** (`/api/v1/media/search?filter=base&source=avatar_listings` returned 0) and Reticulum rejects attempts to create base avatars via file upload (`POST /api/v1/avatars` without `parent_avatar_listing_id` returns `400` / `Internal server error`). |
| 2026-02-09 14:15Z-14:18Z | Bootstrapped a base avatar listing by importing a known-good base avatar from `demo.hubsfoundation.org` and creating an `avatar_listings` row tagged `base`, `default`, `featured`. | Base avatar listings became available locally (unblocking avatar creation for uploaded avatars). |
| 2026-02-09 14:24:37Z (15:24:37+01:00 CET) | Committed `hubs` fix `f7605bf73` to: (1) require and select a base avatar listing for Admin local upload (adds `parent_avatar_listing_id`), (2) in-room custom avatar URL now imports Hubs avatar page URLs into local reticulum and stores imported `avatar_id` (SID). | Local upload no longer fails on base-avatar path; Hubs avatar URLs should become properly rigged via import rather than static URL avatars. |
| 2026-02-09 14:26:22Z-14:31:33Z | GitHub Actions run `21828924354` (`custom-docker-build-push`) for `master` `f7605bf73`. | Completed `success`; published `ghcr.io/yengalvez/hubs:rpm-avatar-import-20260209-f7605bf73-latest`. |
| 2026-02-09 ~14:31Z | Deployed the new image to DOKS `hcce` via `kubectl set image`. | First rollout hit `ErrImagePull` (`403 Forbidden` from GHCR token endpoint); fixed by updating `ghcr-pull` secret with the newest PAT and restarting `deployment/hubs`. Rollout then succeeded. |

## 2026-02-09 (Admin Local Upload Hardening + Avatar URL Normalization + GHCR Build Reliability)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 14:46Z-14:52Z | Hardened Admin local avatar upload in `hubs`: upload via `getDirectReticulumFetchUrl(\"/api/v1/media\")` (avoid ingress limits) and include `base_gltf_url` when creating avatars. | Admin disk-upload flow should succeed for larger avatars and match `AvatarEditor` semantics more closely. |
| 2026-02-09 14:46Z-14:52Z | Improved in-room `Change Avatar` URL handling to also accept avatar SIDs and Hubs API URLs (e.g. `/api/v1/avatars/<sid>` and `/api/v1/avatars/<sid>/avatar.gltf`) and import remote avatars into local reticulum when cross-origin. | Avoids “static URL avatar” when users paste API URLs; improves reliability of rigged avatars. |
| 2026-02-09 14:52Z | Merged the fixes into `hubs` `master` and pushed (`01f2132e9`). | Code available on `origin/master`. |
| 2026-02-09 14:55Z-14:56Z | Observed `custom-docker-build-push` run `21829985541` fail. | Root cause: missing default registry inputs/vars/secrets -> `Username and password required`. |
| 2026-02-09 14:58Z | Updated `custom-docker-build-push` workflow defaults (`a7c9eb2e7`). | Workflow now defaults to GHCR + repo owner when overrides/vars are absent. |
| 2026-02-09 14:59Z-15:06Z | Observed `custom-docker-build-push` run `21830114386` fail when using `GITHUB_TOKEN` fallback. | Root cause: GHCR returned `403 Forbidden` on cache/push HEAD requests. |
| 2026-02-09 15:08Z-15:17Z | Re-ran `custom-docker-build-push` with explicit PAT auth + no build cache: run `21830444511`. | Completed `success` and published `ghcr.io/yengalvez/hubs:rpm-avatar-import-20260209-a7c9eb2e7-latest`. |
| 2026-02-09 15:17Z-15:18Z | Deployed the new image to DOKS `hcce`: `kubectl set image deployment/hubs ...` + rollout status. | Deployment rolled out successfully; `deployment/hubs` now points at the new image tag. |

## 2026-02-09 (RPM Rigging Fix + Basic Full-Body Locomotion + Deploy)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 16:38Z | Committed `hubs` fix (`57a945fae` + `7a0fc7290`): normalize Mixamo/RPM bone names, ensure `AvatarRoot` for template attachment, add fallback eyes, and attach `fullbody-locomotion` to `AvatarRoot` templates. | RPM/Mixamo avatars should now receive `ik-controller` (body yaw + head tracking) and get a basic procedural leg swing while moving. |
| 2026-02-09 16:42:42Z-16:49:17Z | GitHub Actions run `21833234013` (`custom-docker-build-push`) with `Override_Image_Tag=rpm-avatar-rigging-20260209-7a0fc7290` and `Use_Build_Cache=false`. | Completed `success`; published `ghcr.io/yengalvez/hubs:rpm-avatar-rigging-20260209-7a0fc7290-latest`. |
| 2026-02-09 ~16:50Z | Rolled out `deployment/hubs` in namespace `hcce` to the new GHCR image tag via `kubectl set image`. | Rollout succeeded; `https://meta-hubs.org` returns HTTP 200. |

## 2026-02-09 (RPM: Fix Floating + Stop Broken Arm IK + Deploy)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 ~17:18Z | Committed `hubs` fix `3791e682f`: prefer renaming skeleton joints for Mixamo/RPM, avoid “wrap under Head” fallback when a humanoid skeleton exists, compute avatar offset in IK-root space, and disable the simple hand IK when a full arm chain is detected. | Should stop RPM avatars being glued to camera / floating, and prevent distorted/stretched arms. |
| 2026-02-09 17:18:59Z-17:26:16Z | GitHub Actions run `21834162983` (`custom-docker-build-push`) with `Override_Image_Tag=rpm-avatar-ikfix-20260209-3791e682f` and `Use_Build_Cache=false`. | Completed `success`; published `ghcr.io/yengalvez/hubs:rpm-avatar-ikfix-20260209-3791e682f-latest`. |
| 2026-02-09 ~17:26Z | Rolled out `deployment/hubs` in namespace `hcce` to `ghcr.io/yengalvez/hubs:rpm-avatar-ikfix-20260209-3791e682f-latest`. | Rollout succeeded; `https://meta-hubs.org` returns HTTP 200. |

## 2026-02-09 (RPM Shared Animations: Idle + Walk + Deploy)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 ~18:10Z-18:15Z | Implemented shared Mixamo locomotion MVP in `hubs`: added shared `idle`/`walk` clips (as GLB assets), loader/retargeting utility, and updated `fullbody-locomotion` to prefer shared animations over procedural swing (legs-only to avoid IK conflicts). | Full-body RPM/Mixamo avatars should now play shared idle/walk without requiring per-avatar embedded animations. |
| 2026-02-09 ~18:15Z-18:17Z | Ran local validation in `/Users/Shared/Gits/YenHubs/hubs`: `npm run check`, `npm run lint:js`, `npm run build`. | Passed (webpack size warnings only). |
| 2026-02-09 18:18:19Z-18:25:34Z | GitHub Actions run `21836105337` (`custom-docker-build-push`) with `Override_Image_Tag=rpm-anim-idle-walk-20260209-eb3276a77` and `Use_Build_Cache=false`. | Completed `success`; published `ghcr.io/yengalvez/hubs:rpm-anim-idle-walk-20260209-eb3276a77-latest`. |
| 2026-02-09 ~18:26Z | Installed `kubectl` client to `~/.local/bin/kubectl` (v1.35.0) and rolled out `deployment/hubs` in namespace `hcce` to the new image tag via `kubectl set image`. | Rollout succeeded; `deployment/hubs` now points at `ghcr.io/yengalvez/hubs:rpm-anim-idle-walk-20260209-eb3276a77-latest` and `https://meta-hubs.org` returns HTTP 200. |
| 2026-02-09 ~18:45Z-19:03Z | Extended shared locomotion retargeting to include **arm swing** bones (still excluding hips/spine/neck/head and all translations). Attempted to build/push image via Actions run `21837087699` but GitHub Actions/Logs endpoints were returning server errors during a minor GitHub outage; re-run remained queued. | Deployed a **temporary hotpatch** by copying locally built `/Users/Shared/Gits/YenHubs/hubs/dist/assets` + `dist/*.html` into the running `hubs` pod (`/www/hubs/assets` + `/www/hubs/pages`) and injecting `turkeyCfg_*` env meta tags (without restarting the pod). This is **not durable** across pod replacement; a proper image rollout is still preferred when GitHub Actions recovers. |
| 2026-02-09 ~21:45Z | Production incident: `meta-hubs.org` rendered a blank page again; HTML referenced `{{rawhubs-base-assets-path}}/assets/...` (placeholders not rewritten) causing asset 404s and strict MIME/CSP failures in browser. | Root cause: prior hotpatch replaced `/www/hubs/pages/*.html` in the running pod with placeholder versions, but `run.sh` only rewrites placeholders on container start; Reticulum also cached the broken HTML. |
| 2026-02-09 ~21:48Z | Recovery: restarted `deployment/hubs` to restore image-backed pages and rerun `run.sh`; then restarted `deployment/reticulum` to flush cached HTML. | `meta-hubs.org` returned to serving HTML with `https://assets.meta-hubs.org/hubs/assets/...` URLs (no placeholders); site loaded again. |
| 2026-02-09 21:53Z-22:00Z | Retried durable GHCR build/push for arm swing using GitHub Actions run `21842185352` (`custom-docker-build-push`, `Override_Image_Tag=rpm-anim-idle-walk-arms-20260209-24612e845`). | `failure`: GHCR returned `403 Forbidden` on cache importer and blob HEAD during push (default workflow token did not have required package perms / GHCR was denying). |
| 2026-02-09 22:02Z-22:09Z | Dispatched GitHub Actions run `21842475778` with explicit PAT auth (`Override_Registry_Username=yengalvez`, `Override_Registry_Password=<PAT>`) and `Use_Build_Cache=false`. | `success`: published `ghcr.io/yengalvez/hubs:rpm-anim-idle-walk-arms-20260209-24612e845-latest`. |
| 2026-02-09 ~22:11Z | Rolled out `deployment/hubs` to the new image tag via `kubectl set image` and restarted `deployment/reticulum` to refresh cached HTML/asset hashes. | Rollout succeeded; verified `meta-hubs.org` serves 200 and HTML references valid asset URLs; hubs pod now runs the arm-swing build. |

## 2026-02-09 (Avatar Featured + Preview Thumbnails Fix + Deploy)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-09 ~22:40Z | Investigated why “Featured avatars” was empty and why avatar preview thumbnails were missing in the “Change Avatar” UI. | Root cause: `ret0_admin.featured_avatar_listings` filters by `avatars.allow_promotion=true` in addition to `tags.tags ? 'featured'`, so featured-tagged avatars with `allow_promotion=false` are invisible. |
| 2026-02-09 ~22:50Z | Backfilled `allow_promotion=true` for avatars whose `avatar_listings.tags` includes `featured`. | Featured endpoints began returning 7 entries; Featured Avatars list is no longer empty. |
| 2026-02-09 ~22:55Z | Implemented admin fixes in `hubs` and pushed commit `dfdb76bcd`: (1) local `.glb` uploads generate a real thumbnail preview (WebGL snapshot) instead of a placeholder, (2) imported avatars/scenes are auto-marked `allow_promotion=true`, (3) “Feature” button ensures `allow_promotion=true` for the underlying avatar/scene, (4) guard against missing `tags` when featuring/unfeaturing. | Fixes should make new imports immediately eligible for Featured lists and show proper preview thumbnails. |
| 2026-02-09 ~23:00Z-23:06Z | Dispatched GitHub Actions run `21844461489` (`custom-docker-build-push`) with `Override_Image_Tag=avatars-featured-previews-20260209-dfdb76bcd` and explicit registry auth inputs. | `success`: published `ghcr.io/yengalvez/hubs:avatars-featured-previews-20260209-dfdb76bcd-latest`. |
| 2026-02-09 ~23:10Z-23:13Z | Rolled out `deployment/hubs` in namespace `hcce` to `ghcr.io/yengalvez/hubs:avatars-featured-previews-20260209-dfdb76bcd-latest` and restarted `deployment/reticulum`. | Rollout succeeded; `meta-hubs.org` serves 200; `/api/v1/media/search?source=avatar_listings&filter=featured` returns 7 entries. |
| 2026-02-09 ~23:15Z | Hardened CI in `hubs` commit `787b5236f`: skip `hubs-RetPageOrigin` (turkey deploy) job unless required secrets are configured. | Prevents noisy failing GitHub Actions runs in forks that don’t have turkey secrets. |

## 2026-02-10 (Admin Import: Fix Import Errors + Listing Activation + Featured Reliability)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-10 ~00:00Z | Implemented `hubs` Admin fixes (commit `83ad93135`): (1) `Approve existing` avatar/scene listings now set `state: active`, (2) import defaults no longer auto-select Base/Default/Featured, (3) local import base-avatar detection falls back to any listing that exposes `gltfs.base` (built-in base bot) when `filter=base` returns none, (4) “Feature” button also activates delisted listings. | Fixes the common failure mode where imports create delisted listings that never show up in user pickers/featured lists, and prevents local `.glb` imports from failing when no base-tagged listing exists yet. |
| 2026-02-10 00:00:37Z-00:07:17Z | GitHub Actions run `21845813611` (`custom-docker-build-push`) for `codex/admin-import-fix-active-featured` with `Override_Image_Tag=admin-import-fix-20260210-83ad93135`. | `failure`: GHCR returned `403 Forbidden` on cache importer and blob HEAD during push (workflow `GITHUB_TOKEN` lacked package write rights / registry secrets not configured). |
| 2026-02-10 00:09:07Z-00:16:51Z | Re-ran GitHub Actions as run `21846017453` with explicit registry auth inputs (`Override_Registry_Password=<PAT>`) and `Use_Build_Cache=false`. | `success`: published `ghcr.io/yengalvez/hubs:admin-import-fix-20260210-83ad93135-latest`. |
| 2026-02-10 ~00:17Z-00:21Z | Rolled out `deployment/hubs` in namespace `hcce` to `ghcr.io/yengalvez/hubs:admin-import-fix-20260210-83ad93135-latest`, then restarted `deployment/reticulum` to refresh cached page-origin HTML/asset hashes. | Deployment healthy; `/admin` now references the new `admin-*.js` hash that exists on `assets.meta-hubs.org` (avoids blank admin page due to 404 hashed assets). |

## 2026-02-11 (Bots MVP Branch Deploy Attempt + Hubs Image Rollout)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-11 14:38Z-14:39Z | Pushed branch `codex/bots-hybrid-mvp` to `yengalvez/hubs` and `yengalvez/hubs-cloud`. | Branches published; ready for CI + deploy. |
| 2026-02-11 14:39:35Z-14:44:09Z | GitHub Actions run `21909457715` (`custom-docker-build-push`) on `yengalvez/hubs` for `codex/bots-hybrid-mvp` (`headSha=0e6699502...`). | `success`: published `ghcr.io/yengalvez/hubs:codex-bots-hybrid-mvp-latest` and `ghcr.io/yengalvez/hubs:codex-bots-hybrid-mvp-23`. |
| 2026-02-11 17:20Z-17:21Z | Regenerated `hcce.yaml` from local values, applied ingress/HAProxy manual fixes (cert-manager + ssl redirect annotations, HAProxy 3.2, remove haproxy container securityContext, keep certs workflow), then `kubectl apply -f hcce.yaml` + RBAC patch + HAProxy rollout restart. | Cluster rollout successful; `deployment/hubs` updated to `ghcr.io/yengalvez/hubs:codex-bots-hybrid-mvp-latest`; `meta-hubs.org` and `/spoke` return `HTTP/2 200`. |
| 2026-02-11 17:21Z | New `bot-orchestrator` deployment came up with `ErrImagePull` (`mozillareality/bot-orchestrator:stable-latest` not available for this environment). | Mitigated by scaling `deployment/bot-orchestrator` to `0` to keep production healthy until a custom orchestrator image is published. |
| 2026-02-11 17:56Z-17:57Z | Installed local container runtime (`docker` + `colima` + `docker-buildx`), built and pushed orchestrator image from `hubs-cloud/community-edition/services/bot-orchestrator` for `linux/amd64`. | Published `ghcr.io/yengalvez/bot-orchestrator:codex-bots-hybrid-mvp-latest` and `ghcr.io/yengalvez/bot-orchestrator:codex-bots-hybrid-mvp-7c39c91`. |
| 2026-02-11 17:57Z-17:58Z | Updated `deployment/bot-orchestrator` to GHCR image and scaled back to `1` replica; then regenerated and re-applied `hcce.yaml` using `OVERRIDE_BOT_ORCHESTRATOR_IMAGE`, re-patched RBAC, and restarted HAProxy. | `bot-orchestrator`, `hubs`, and `haproxy` all `Running` (`1/1`), with images pinned to GHCR/custom tags and `meta-hubs.org`/`meta-hubs.org/spoke` returning `HTTP/2 200`. |

## 2026-02-11 (Bots GPT-5 Nano Hardening + Runner Capacity + Deploy Wiring)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-11 18:00Z-18:46Z | Implemented `bot-orchestrator` production hardening on `codex/bots-hybrid-mvp`: OpenAI Responses API integration (`gpt-5-nano`), structured JSON reply parsing (`reply` + optional `go_to_waypoint`), deterministic fallback on LLM failure, in-memory chat rate-limit, runner queue/capacity model (`MAX_ACTIVE_ROOMS=1`), and `MAX_BOTS_PER_ROOM=5` clamp. | Orchestrator now supports real LLM chat with safe fallback and low-cost runner scheduling behavior. |
| 2026-02-11 18:00Z-18:46Z | Added runner runtime pieces in `services/bot-orchestrator`: new `run-bot.js`, Dockerfile update with Chromium runtime, and package updates (`puppeteer-core`, `docopt`, `query-string`). | Service can autostart one technical room runner process from inside the orchestrator container. |
| 2026-02-11 18:00Z-18:46Z | Wired deploy config/template for bots+LLM in `hubs-cloud/community-edition/generate_script/hcce.yam` and generator fallback logic (`OPENAI_API_KEY` <- legacy `OPENAI`; auto-generate `BOT_ACCESS_KEY` if omitted). | Generated manifests now include `OPENAI_API_KEY`, `OPENAI_MODEL=gpt-5-nano`, runner envs, and low-cost capacity defaults. |
| 2026-02-11 18:00Z-18:46Z | Aligned bot-count limits to `0..5` in Hubs UI and Reticulum normalization paths (`RoomSettingsSidebar`, `bot-runner-system`, `ret/hub.ex`, `hub_channel.ex`, bot controllers). | Client/backend limits are now consistent with low-cost production mode. |
| 2026-02-11 18:00Z-18:46Z | Added feature documentation at `features/bots/README.md` (usuario/admin + troubleshooting), and updated deployment/rules docs for required bot secrets (`BOT_ACCESS_KEY`, `OPENAI_API_KEY`) and verification steps. | Docs now cover operation and troubleshooting for future bot rollouts. |

## 2026-02-11 (Bots Deploy Completion + Reticulum Readiness Fix + Production Verification)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-11 ~19:08Z-19:12Z | Detected `reticulum` rollout stuck (`1/2 Ready`) after enabling global bots feature flags. | Readiness probe `GET /?skipadmin` returned 500 repeatedly; rollout blocked by old replica pending termination. |
| 2026-02-11 ~19:12Z-19:14Z | Root-cause analysis from pod logs showed `CaseClauseError` in `Ret.AppConfig.get_config/1`: DB value type mismatch for `ret0.app_configs.value`. | Confirmed `features|enable_room_bots` and `features|enable_bot_chat` were stored as raw JSON boolean (`true`) instead of expected object wrapper (`{\"value\": true}`). |
| 2026-02-11 ~19:14Z-19:16Z | Corrected both rows in PostgreSQL to `{\"value\": true}` and rechecked rollout. | New reticulum pod became `2/2 Ready`; deployment rollout completed successfully. |
| 2026-02-11 ~19:16Z-19:20Z | Verified production health and TLS: `meta-hubs.org` + `/spoke` HTTP 200, certificate issuer `Let's Encrypt R13`, hubs and bot-orchestrator images pinned to final GPT-5 Nano tags. | Site healthy and on correct images (`ghcr.io/yengalvez/hubs:bots-gpt5nano-20260211-f9c07d368-latest`, `ghcr.io/yengalvez/bot-orchestrator:bots-gpt5nano-20260211-4cda738-latest`). |
| 2026-02-11 ~19:20Z-19:24Z | Smoke-tested bot orchestrator internals: `room-config` clamp, chat response via LLM, queue/capacity behavior (`MAX_ACTIVE_ROOMS=1`, `MAX_BOTS_PER_ROOM=5`). | Confirmed clamp to 5, `gpt-5-nano` responses with `go_to_waypoint`, and `queued_capacity` behavior when second room is enabled. |
| 2026-02-11 ~19:24Z-19:27Z | Found runner crash in logs: `TypeError: querystring.stringify is not a function` from `run-bot.js`. Implemented fix using `URLSearchParams`, pushed `de457fb`, built via Actions run `21919717500`, and rolled out `bot-orchestrator`. | Crash loop removed; runner starts with valid URL query string. |
| 2026-02-11 ~19:27Z-19:29Z | Detected runner lifecycle issue (`detached Frame` retry loop after room stop). Hardened `run-bot.js` with bounded retries + page recreation + SIGTERM/SIGINT shutdown, pushed `f04cdfd`, built via Actions run `21919851347`, and rolled out. | Runner now exits cleanly on stop and no longer loops on detached frame errors. |
| 2026-02-11 ~19:29Z-19:31Z | Added chat action fallback in `bot-orchestrator` (`app.js`): if LLM omits `action`, apply `detectWaypointAction` from user message/context. Pushed `a02d43b`, built via Actions run `21919952860`, and rolled out `ghcr.io/yengalvez/bot-orchestrator:bots-gpt5nano-20260211-a02d43b-latest`. | `ve a spawbot-2` now returns deterministic action (`go_to_waypoint`) even when model reply omits it; production healthy after rollout. |
| 2026-02-11 ~19:46Z-19:49Z | Incident: rooms failed to join with UI message `Imposible conectarse a esta sala` and console error `JsonWebTokenError: invalid signature`. | Root cause: `reticulum` and `dialog` had mismatched JWT key material (`PERMS_KEY`) after secret regeneration/partial restarts. Restarting `deployment/dialog` aligned keys and room join recovered immediately. |
| 2026-02-11 ~19:49Z | Hardening: updated `generate_script/index.js` to preserve existing `PERMS_KEY` (generate only when missing), derive public JWK from that key, and avoid accidental rotation on every `gen-hcce`. | Prevents future silent key drift causing room connection failures. |

## 2026-02-11 (Bots Runner Navigation Timeout Fix + Successful Rollout)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-11 ~23:40Z | Patched `hubs-cloud/community-edition/services/bot-orchestrator/run-bot.js` (commit `f146cae`) to stop using `waitUntil: \"networkidle2\"` and wait for explicit startup readiness (`scene entered` + `NAF connected`) with retry-safe timeouts. | Removed false navigation timeouts that caused runner restart loops. |
| 2026-02-11 ~22:48Z-22:51Z | Triggered Actions run `21926135680` for bot-orchestrator image build. | `failure`: `failed to read dockerfile: open Dockerfile: no such file or directory` due incorrect override (`Override_Dockerfile=Dockerfile`). |
| 2026-02-11 ~22:50Z-22:51Z | Triggered Actions run `21926174128` with corrected Dockerfile path handling. | `failure`: GHCR push denied (`403 Forbidden` on blob HEAD). |
| 2026-02-11 ~22:52Z-22:53Z | Triggered Actions run `21926216670` with explicit registry auth inputs (`Override_Registry_Username` + `Override_Registry_Password` PAT). | `success`: published `ghcr.io/yengalvez/bot-orchestrator:bots-runnerfix-20260211-f146cae-latest`. |
| 2026-02-11 ~22:53Z-22:54Z | Rolled out `deployment/bot-orchestrator` in namespace `hcce` via `kubectl set image` to `ghcr.io/yengalvez/bot-orchestrator:bots-runnerfix-20260211-f146cae-latest`. | Rollout `success`; new pod `bot-orchestrator-7cb5684689-jnl5p` running. |
| 2026-02-11 ~22:54Z-22:58Z | Verified runtime via orchestrator `/health`, forced `room-config` for room `VJopCY3`, and validated in-room with Playwright. | Runner state `running`, active hub list includes `VJopCY3`, and clients observe bot entities (`[bot-info]` present). |

## 2026-02-13 (Room Connection Outage Fix + Dialog PERMS_KEY Unescape + Build Badge)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-13 ~09:03Z-09:10Z | Reproduced the “room won’t load” issue via Playwright: UI shows `Imposible conectarse a esta sala`, with console errors originating from Dialog JWT verification (`JsonWebTokenError: invalid signature` / `secretOrPublicKey must...`). | Confirmed it was not missing assets/404; it was a signaling/auth failure between client and `dialog` (`stream.<domain>:4443`). |
| 2026-02-13 ~09:20Z-09:38Z | Shipped a custom `dialog` image that writes a valid `/app/certs/perms.pub.pem` by unescaping `PERMS_KEY` (handles double-escaped `\\\\n`) at runtime; fixed the `dialog` Dockerfile runtime Node version to match the build stage (avoids native module ABI crash). | `deployment/dialog` returned to `Running` and accepted protoo websocket connections. |
| 2026-02-13 ~09:39Z-09:41Z | Restarted `deployment/reticulum` so both `reticulum` and `dialog` used the same current `PERMS_KEY` from the `configs` secret. | Rooms load again (lobby shows `Join Room` instead of “Imposible conectarse…”). |
| 2026-02-13 ~09:41Z | Persisted the current `PERMS_KEY` into local deploy inputs (`deployment/input-values.local.yaml` and the working copy `hubs-cloud/community-edition/input-values.yaml`) to prevent silent key rotation on future `gen-hcce` runs (which can break rooms after partial restarts). | Prevents recurrence of “invalid signature” outages caused by regenerated PERMS keys. |
| 2026-02-13 ~09:47Z-09:55Z | Improved the in-room build/version badge logic to prefer the `frontend-<hash>.js` bundle hash (instead of matching the first hashed script like `webxr-polyfill-*`), built/pushed a new Hubs image via GitHub Actions, and rolled it out. | Toolbar now shows the correct build fingerprint (e.g. `743fbc0e`) to confirm which version is running in production. |
| 2026-02-13 ~11:00Z-11:22Z | Fixed `reticulum` internal bots discovery endpoint (`GET /api-internal/v1/hubs/active_with_bots`) to use `RetWeb.Presence.present_hub_sids()` instead of `hubs.last_active_at` filtering; built a new `reticulum` image via GitHub Actions and deployed via `gen-hcce` + `kubectl apply`. | `bot-orchestrator` now sees active bot-enabled rooms (e.g. `VJopCY3`) and starts the headless runner; verified bots spawn and move between `spawbot-*` waypoints in-room. |

## 2026-02-19 (Runtime Fix: `transforming` undefined + avatar fetch fallback)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-02-19 ~22:35Z-22:41Z | Patched null-guards for `transform-selected-object` usage in `visibility-while-frozen`, `visible-if-transforming`, `cursor-controller`, `app-aware-mouse`, and `pinnable`. | Eliminated recurring runtime crash `Cannot read properties of undefined (reading 'transforming')` when transform system is unavailable/late. |
| 2026-02-19 ~22:41Z-22:42Z | Added safe fallback in `fetchRandomDefaultAvatarId()` (`src/utils/identity.js`) to return bundled default avatar if Reticulum fetch fails in local/dev contexts. | Prevented startup crash path caused by `TypeError: Failed to fetch` during profile init. |
| 2026-02-19 ~22:42Z-22:43Z | Validation in `hubs`: `npm run check`, `npm run lint:js`, `npm run build`. | All passed (build with expected size warnings only). |
| 2026-02-19 ~22:43Z-22:49Z | Pushed `hubs` commit `5e1344b00` and built image through GitHub Actions run `22203229807` (`success`). | Published `ghcr.io/yengalvez/hubs:runtime-fix-20260219-5e1344b00-55`. |
| 2026-02-19 ~22:49Z-22:50Z | Rolled out `deployment/hubs` in namespace `hcce` to runtime-fix image and verified endpoint health. | Deployment ready on `ghcr.io/yengalvez/hubs:runtime-fix-20260219-5e1344b00-55`; `https://meta-hubs.org` returns HTTP 200. |

## 2026-03-02 (Bots visibility/count stability fix in ghost runner)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-03-02 ~13:08Z-13:14Z | Investigated intermittent room bots issue (showing fewer than configured and disappearing after settings changes). Verified live `bot-orchestrator` config and logs, and inspected runner/realtime code paths. | Identified two stability risks in `run-ghost-runner.js`: (1) presence sync cache never removed disconnected occupants (rejoin could miss bot full sync), (2) `hub_refresh` could overwrite bot config with `count=0` when payload lacked `user_data.bots`. |
| 2026-03-02 ~13:15Z-13:18Z | Patched ghost runner to prune stale occupants on each `Presence.onSync` and re-trigger full sync for rejoining occupants; patched `hub_refresh` handling to only update bot config when `user_data.bots` is explicitly present. Commit `3c96d70` on `hubs-cloud` branch `codex/bots-ghost-runner`. | Fix reduces “sometimes 0 bots” and “partial bot count after rejoin/config change” caused by missed resyncs/partial refresh payloads. |
| 2026-03-02 ~13:18Z-13:21Z | Built/pushed bot-orchestrator image via GitHub Actions run `22577767949` (`success`) and rolled out `deployment/bot-orchestrator` to `ghcr.io/yengalvez/bot-orchestrator-runner:ghost-syncfix-20260302-3c96d70-29`. | New pod running and runner joined `VJopCY3`; startup logs confirm scene + waypoints parsing (`all=25`, `spawn=9`, `patrol=9`). |
| 2026-03-02 ~13:22Z | Updated local deploy inputs to pin current bot-orchestrator image tag in both working files. | `deployment/input-values.local.yaml` and `hubs-cloud/community-edition/input-values.yaml` now reference `ghost-syncfix-20260302-3c96d70-29`. |

## 2026-03-02 (Bots idle-recovery hardening + runner watchdog deploy)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-03-02 ~22:15Z-22:30Z | Diagnosed long-idle bot disappearance and reviewed runner failure mode. Patched `hubs-cloud/community-edition/services/bot-orchestrator/run-bot.js` with health watchdog (fatal page errors, repeated unhealthy checks, browser/page unexpected close => process exit for orchestrator restart). Also added channel-level fail-fast exits in `run-ghost-runner.js` (`channel.onError/onClose`). Commit `2e8d15e` on `hubs-cloud` branch `codex/bots-ghost-runner`. | Eliminated zombie-runner condition where process stayed alive but stopped publishing bots after prolonged idle/disconnect states. |
| 2026-03-02 ~22:28Z-22:35Z | Triggered GitHub Actions build for bot-orchestrator. First run `22598645629` failed with GHCR push `403 Forbidden` (default workflow token). Re-ran with explicit registry credentials (`Override_Registry_Username` + `Override_Registry_Password`) as run `22598853824` (`success`). | Published `ghcr.io/yengalvez/bot-orchestrator:bot-watchdog-20260302-2e8d15e-p1-31`. |
| 2026-03-02 ~22:36Z-22:41Z | Deployed new bot-orchestrator image and tested backend modes. `ghost` backend was active but did not surface bot entities in client for this room, so rollout switched to `RUNNER_BACKEND=chromium` with watchdog-enabled image and completed. | Production bot-orchestrator now runs watchdog-hardened Chromium runner (`runner_backend_default=chromium`, `max_bots_per_room=10`). |
| 2026-03-02 ~22:41Z-22:46Z | Validation: orchestrator `/health` healthy (`active_rooms=1`, hub `VJopCY3`), runner startup logs show scene enter + network connected, and Playwright checks observed bot entities appearing on repeated room loads (`[bot-info]` count reached 5 on both passes). | Bots are visible again after reconnect flows; recovery logic is now automatic if runner degrades. |
| 2026-03-02 ~22:46Z | Updated working deploy inputs to avoid future drift. | `deployment/input-values.local.yaml` and `hubs-cloud/community-edition/input-values.yaml` now pin `OVERRIDE_BOT_ORCHESTRATOR_IMAGE=ghcr.io/yengalvez/bot-orchestrator:bot-watchdog-20260302-2e8d15e-p1-31` and `RUNNER_BACKEND=chromium`. |

| 2026-03-07 ~09:45Z-10:04Z | Diagnosed ghost-runner bot loss in lobby/spectator mode. Verified via Phoenix watcher + Playwright that the runner was publishing `naf/nafr`, but fresh clients often missed the one-shot first sync and therefore never instantiated `room-bot-*` entities. Patched `/Users/Shared/Gits/YenHubs/hubs-cloud/community-edition/services/bot-orchestrator/run-ghost-runner.js` to schedule short full-sync bursts on startup, bot reconcile, and each late join instead of relying on a single immediate first sync. Committed `e38b70d`, built image via GitHub Actions run `22796901406`, updated deploy values to `ghcr.io/yengalvez/bot-orchestrator:ghost-fullsync-20260307-e38b70d-latest`, regenerated `hcce.yaml`, and redeployed with `RUNNER_BACKEND=ghost`. | New pod `bot-orchestrator-555b4cd785-ngwj5` running with ghost backend; fresh spectator load on `https://meta-hubs.org/VJopCY3/inicio` now auto-instantiates 5 bots (`room-bot-*`) without manual resync/probe. |

## 2026-03-16 (Project freeze, metadata backup, merge closure, and DigitalOcean shutdown)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-03-16 ~08:01Z-08:02Z | Generated a local freeze bundle at `/Users/Shared/Gits/YenHubs/output/project-freeze-20260316-090114/`, including `retdb` dump, Kubernetes state exports, DigitalOcean metadata, image tags, submodule status, and local deployment inputs kept outside git history. | Infrastructure and database metadata were preserved. July 2026 recovery later proved that `ret-pvc` media bytes were omitted, so this was not a complete content backup. |
| 2026-03-16 ~08:03Z-08:08Z | Consolidated shutdown/recovery docs in `/Users/Shared/Gits/YenHubs/deployment/README.md`, `/Users/Shared/Gits/YenHubs/docs/project-freeze-2026-03.md`, `/Users/Shared/Gits/YenHubs/features/bots/README.md`, and the root README. Removed the stray `IN` line accidentally left in `/Users/Shared/Gits/YenHubs/features/rpm-avatars/README.md`. | The repo now documents final production state, branch layout, backup location, rebuild order, and the fact that bots ended on `ghost` runner. |
| 2026-03-16 ~08:05Z-08:08Z | Merged `/Users/Shared/Gits/YenHubs/hubs` branch `codex/ui-es-avaturn-private` into `master` and pushed commit `7aa9a35f4d3d6e9ac48cdf3cebf4553073f43823`. | Client subrepo closure completed on base branch `master`. |
| 2026-03-16 ~08:08Z-08:10Z | Merged `/Users/Shared/Gits/YenHubs/hubs-cloud` branch `codex/bots-ghost-runner` into `master`, resolved the workflow conflict in `.github/workflows/custom-docker-build-push.yml` by keeping the `Override_Repo_Name`-capable version, and pushed commit `832d8e39566e22768b816422bffc9417f9f5a53c`. | Deployment subrepo closure completed on base branch `master`. |
| 2026-03-16 ~08:10Z-08:12Z | Deleted DigitalOcean cluster `hubs-ce`, then manually removed the remaining orphaned node, load balancer, and two block volumes after confirming cluster deletion had not cleared them. | DigitalOcean runtime cost for this project was reduced to effectively zero: no cluster, no droplet, no load balancer, and no attached block storage remain. |

## 2026-07-13 (Reactivation preparation and integral audit started)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-13 ~20:00-22:45 | Verified the March freeze bundle, database dump, private image availability, repository/submodule baseline, current official Hubs/Hubs CE releases, local build toolchain and expected DigitalOcean topology. | Recovery is viable. The owner approved the audit and the estimated non-HA DOKS topology (~62 USD/month), but cluster creation remains blocked by expired local `doctl` authentication and the required SMTP credential rotation. |
| 2026-07-13 ~21:00-22:45 | Audited secrets and dependency state with Git history scans, `npm audit` and `mix hex.audit`. Removed the tracked deployment values file, added a subrepo ignore rule, removed a hardcoded Spoke PAT and rotated local DB/session/perms/bot keys. | Confirmed historic public secret exposure and a P0 Reticulum modernization requirement. SMTP remains intentionally blank until rotated at the provider. No secret values were added to documentation. |
| 2026-07-13 ~21:20-22:45 | Hardened the bot/AI path: backend-authoritative actions, fail-closed internal authentication, moderation, rate limits, bounded inputs/output, `store:false`, pseudonymous safety identifier, privacy-safe logs/fallbacks, stale-runner reconciliation and tests. | Bot orchestrator has 7 passing tests and 0 production npm advisories. Chat history remains browser-memory-only and is not persisted by YenHubs. |
| 2026-07-13 ~22:20-22:45 | Audited custom client features and applied low-risk fixes: VR always forces first person, private/local avatar GLB validation (64 MiB, GLB 2.0 header), upload error recovery and object-URL cleanup. | Hubs `npm run check` and `npm run lint:js` pass; full build remains successful with known bundle-size warnings. Live multiuser and visual acceptance still require the restored cluster. |
| 2026-07-13 ~22:35-22:45 | Made ghost runner the fail-safe default in both orchestrator and manifest generator, pinned Phoenix JS and reusable Hubs workflows by commit, and wrote `/Users/Shared/Gits/YenHubs/docs/audit-2026-07.md`. | Missing configuration can no longer silently enable expensive Chromium. The audit has a durable severity/status register and explicit completion criteria. |
| 2026-07-13 ~23:03-23:07 | Started audited Hubs, Reticulum and bot-orchestrator builds through the standard GitHub Actions workflows. The first bot-orchestrator run `29284861858` built successfully but GHCR rejected the push with `403 Forbidden` because the automatic workflow token did not have access to the existing package. | Configured `REGISTRY_USERNAME` and `REGISTRY_PASSWORD` as masked repository secrets in `yengalvez/hubs-cloud` and reran the same workflow as run `29285037607`. No alternate build/deploy method was used and no token was placed in workflow inputs. |
| 2026-07-13 ~23:08-23:24 | Fixed bitECS seat occupancy, aligned the Phoenix bot-channel key minimum with HTTP internals, and corrected a 420 px mobile notification overflow. Re-ran Hubs check, lint and production build; all passed with only the known bundle-size warnings. | Published the code as `hubs` commits `a3627c3ab`/`430d98939` and `hubs-cloud` commit `ff26b96`. Actions runs `29285879154` (Hubs), `29285617020` (Reticulum) and `29285037607` (bot orchestrator) all succeeded; their audited image tags are pinned in the local ignored deployment values. |
| 2026-07-13 ~23:25-23:50 | Compared the March freeze against the current DigitalOcean catalog and fixed the deployment generator before recreating infrastructure. Moved cert-manager annotations, `ingressClassName`, HAProxy RBAC and ACME-safe SSL behavior into the tracked template; removed the unused self-signed certificate and added a fail-closed manifest verifier. | The approved baseline is explicit: DOKS `1.34.8-do.2`, one `s-4vcpu-8gb` node in `ams3`, `HA=false`, one LoadBalancer and two 10 GiB volumes (~62 USD/month). Generator `npm audit` is clean and deployment no longer depends on manual YAML edits or RBAC patches. |

## 2026-07-14 (Low-cost DOKS baseline restored)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-13 23:52-2026-07-14 00:33 | Created `hubs-ce` in `ams3` on DOKS `1.34.8-do.2` with one `s-4vcpu-8gb` node and explicit `HA=false`; reused the free `voice-chat` firewall, installed cert-manager `v1.19.3`, applied the verified 38-resource manifest and restored the March database dump. | API confirms non-HA, one regional LoadBalancer, two 10 GiB PVCs and all baseline deployments healthy. Restore contains 356 schema tables/views, 94 migrations and 17 hubs. A fresh ignored backup was saved under `output/reactivation-20260714-003330/`. |
| 2026-07-14 00:20-00:33 | The first restore stopped because plain `pg_dump` does not include the required cluster role `ret_admin`. Recreated the database cleanly, added the NOLOGIN role, restored successfully and added `deployment/restore-retdb.sh` with dry-run/confirmation/safety checks. | Future restores no longer depend on remembering an undocumented role and fail with DB consumers held at zero. DNS/TLS remains pending because IONOS still points all four names to the parked IP. |
| 2026-07-14 00:34-00:48 | Made the restore script compatible with macOS Bash 3.2, validated its read-only mode against the restored cluster, and upgraded `preflight-reactivation.sh` to inspect the active `yenhubs` DigitalOcean context and paid resources. Corrected stale cost wording that implied scaling pods to zero reduces the DO bill. | Preflight reports 0 failures and proves the approved topology: `HA=false`, one `s-4vcpu-8gb` node, one matching LoadBalancer and exactly two 10 GiB DOKS volumes. Scaling deployments to zero is now documented as maintenance-only; full shutdown is required to stop infrastructure billing. |
| 2026-07-14 00:49-00:54 | Refreshed official Hubs and Hubs CE refs and repeated merge simulations without modifying branches. | Latest production tags remain Hubs `prod-2026-03-11` and Hubs CE `2.1.0`. Hubs merges cleanly; CE has only the expected `input-values.yaml` modify/delete conflict caused by YenHubs keeping deployment secrets out of Git. The update remains intentionally deferred until baseline DNS/TLS and smoke tests pass. |
| 2026-07-14 00:55-01:00 | Added the read-only `deployment/verify-live-reactivation.sh` acceptance checker for DNS, TLS, deployments, restored DB minimum counts, HTTPS and ghost-runner health. | The pre-DNS run proves the checker discriminates correctly: all 11 active baseline deployments and DB counts pass; the four old DNS records, pending certificates and intentionally stopped bot orchestrator fail as expected. |
| 2026-07-14 13:00-14:50 | Completed DNS/TLS and baseline infrastructure validation, then found that the March freeze omitted `ret-pvc`. The restored DB contains 93 active owned-file references (439,216,786 bytes) while the new volume is empty. Confirmed no DO volume snapshots remain; located the 39,343,012-byte scene source GLB and original RPM avatar GLBs locally. | Infrastructure recovery is healthy, but functional content recovery is not exact. Added verified storage backup/restore scripts and a dedicated recovery report; no reconstructed data was written to production. |
| 2026-07-14 14:50-15:10 | Verified SHA-256 hashes for the ten recovered scene/avatar GLBs and created `deployment/generate-recovery-spoke-project.js`, a deterministic offline generator for a Spoke v9 recovery project with one spawn, eight `spawbot-*` waypoints and two sitting waypoints. | Generator syntax, structure, output hashes, URL validation and overwrite refusal pass. The generated positions remain explicitly provisional; no content was uploaded or changed in production. The full audit remains gated on functional recovery and explicit owner confirmation. |
| 2026-07-14 15:10-15:20 | Opened Spoke in read-only browser checks. Anonymous access reaches `/spoke/login`; the previous Chrome session renders `Logout` but `/api/v1/projects` returns `401`. | Spoke itself is available. The restored server requires a fresh magic-link login because the pre-freeze browser credential is stale; no login email, upload or production mutation was initiated. |
| 2026-07-14 17:20-17:50 | Reauthenticated through the normal Mailtrap magic-link flow, uploaded the verified recovered scene GLB, generated/imported a deterministic Spoke v9 recovery project and published a new scene. | Created source scene `fuWfRdF`, editable project `qa3U3Ke`, published scene `f6VKtim` and test room `XesSAqd`; the room renders the recovered office with one spawn, eight bot patrol points and two sitting waypoints. |
| 2026-07-14 17:50-18:35 | Diagnosed failed local avatar imports. Missing historical base-listing bytes caused server-side duplication failures, rapid batch creation hit Reticulum's 1 TPS limit, and a nil `last_active_at` crashed configured bot-room discovery. | Implemented standalone parentless avatar creation, safe missing-file handling, 1100 ms batch spacing and nil-safe bot serialization. Hubs commit `ee75980ad`; hubs-cloud commit `19c56f6f`. |
| 2026-07-14 18:35-18:49 | Built Hubs and Reticulum exclusively through approved GitHub Actions runs `29347365972` and `29347365956`, regenerated the verified 38-resource manifest and deployed it with `kubectl apply`. | Live images are `recovery-avatarfix-20260714-ee75980ad-latest` and `recovery-retfix-20260714-19c56f6-latest`; all rollouts, four certificates and public HTTPS endpoints are healthy. |
| 2026-07-14 18:37-18:49 | Generated 720x1280 Blender thumbnails and imported the nine recovered avatar GLBs through normal Reticulum media/avatar/PostgREST contracts. Retired only the `featured` tag from the two historical broken listings. | `base` is base/default; eight valid full-body listings are featured; all nine avatar metadata and glTF endpoints return HTTP 200. No historical records were deleted. |
| 2026-07-14 18:49-18:58 | Restarted only bot-orchestrator to refresh the featured catalog and performed live room smoke tests. | Ghost runner rehydrated two bot-enabled rooms; test room `XesSAqd` shows three moving bots, no runner in `Personas`, working first/third-person toggle and an `OK` response from the private bot chat backend. Functional recovery is complete enough to begin the audit after explicit owner confirmation. |
| 2026-07-14 19:03-19:08 | Ran the final read-only live acceptance suite and consolidated the functional recovery handoff. | DNS, four TLS certificates, all 12 deployments, database baseline, public HTTPS and ghost runner passed with zero failures. The verifier retains one explicit warning: 34 recovered blob/metadata pairs are present while 127 historical active file references remain in the restored database. |
| 2026-07-14 19:20-19:45 | Began the authorized integral audit without repeating reactivation work. Refreshed official refs, dependency/security evidence, runtime topology and desktop/mobile captures. | Confirmed Hubs `prod-2026-03-11` and Hubs CE `2.1.0`; current branches are 56/34 custom commits ahead respectively. Hubs check/lint/build, bot tests and manifest generation pass. |
| 2026-07-14 19:45-20:00 | Requested an independent read-only subagent review of the audit sequence and compared it against the repository docs and live cluster. | Review found the approach generally correct but stopped further code/deploy work until the recovered live state had a current DB+storage checkpoint. It also identified that bot privacy guardrails are coded but not live because production still uses the March bot image. |
| 2026-07-14 20:00-20:10 | Created `output/audit-checkpoint-20260714-210044/` with a fresh production DB dump, current 34-pair ret-pvc archive, redacted config, Kubernetes inventory, commits, tags, image digests and checksums. Restored the dump in an isolated PostgreSQL 12 pod. | Exact production snapshot is reproducible at DB level. It remains clearly labelled incomplete because 93 historical active file rows have no physical bytes. No Secret objects or unredacted values were included. |
| 2026-07-14 20:10-20:15 | Built a second coherent DB candidate only in an isolated namespace: marked the already-missing 93 file rows inactive and pointed `VJopCY3` (`Inicio`) to recovered scene `f6VKtim`; restored the transformed dump a second time. | Coherent candidate passes 34 active DB files = 34 blobs = 34 metadata and preserves both main/test rooms on the recovered scene. Production was not modified. |
| 2026-07-14 21:00-21:20 | Hardened ghost-runner scene ingestion: same-origin allowlist, redirect revalidation, timeout and byte/JSON/node/edge caps, strict GLB/glTF transforms and iterative cycle detection. | 17 orchestrator tests pass, production dependency audit is clean, the 38-resource manifest verifies, and the recovered live scene parses as 100 nodes, 11 waypoints and 8 `spawbot-*`. No image was deployed yet. |
| 2026-07-14 21:20-21:32 | Added strict Structured Outputs for `gpt-5-nano`, safe refusal handling and a regression fix for accidental movement caused by matching `ve` inside words such as `brevemente`. Executed two controlled live API calls without printing credentials or conversation text. | Neutral greeting returns a short reply with `action=null`; explicit `Ve a spawbot-recepcion` returns only the allowlisted navigation action. Production still runs the March bot image pending the standard GitHub Actions rollout. |
| 2026-07-14 21:35-21:40 | Committed bot hardening as `7de9b5c`, published `audit-botguard-20260714-7de9b5c-latest` exclusively through GitHub Actions run `29362366946`, verified the generated 38-resource manifest and applied it with the standard `kubectl apply` flow. | Deployment diff was limited to `bot-orchestrator`; rollout reached `1/1` and the live image ID is `sha256:1f71222a824870c52636775df4da0dfade5daf9c9840f3d660440d33b7032cc8`. The March image remains available for rollback. |
| 2026-07-14 21:40-21:48 | Repeated post-deploy health, chat and browser acceptance. Tested a neutral prompt and an explicit waypoint command without recording message content, loaded the recovered room as a late spectator and compared two visual samples eight seconds apart. | Health reports ghost backend with two active rooms; neutral chat returns no action, explicit movement returns only the allowlisted action. Three bots were visible and moved between samples while `Personas (1)` proved the runner stayed hidden. No Hubs runtime error appeared; the only browser error came from an installed extension. |
| 2026-07-14 21:48-22:05 | Captured all live image IDs, replaced every mutable workload tag with its already-running SHA-256 digest and made manifest verification fail closed on mutable images. Created `output/audit-imagepin-20260714-215632/` with a fresh DB dump and clearly incomplete 34-pair storage snapshot before rollout. | All 13 live containers are digest pinned, generator audit remains clean and the live manifest converges with zero diff. The standard reactivation verifier passes with 0 failures and the known 127-vs-34 media warning. |
| 2026-07-14 21:59-22:07 | Detected a host-port rollout deadlock while normalizing Coturn, without service outage: the old pod stayed ready while the new pod was pending. Replaced RollingUpdate with verified `Recreate` for Coturn/Dialog and for the single-writer PostgreSQL/Reticulum PVC workloads; added HAProxy startup/readiness/liveness probes after observing one premature liveness restart. | All 12 deployments returned Ready 1/1, four exclusive workloads now enforce `Recreate`, the new HAProxy pod has zero restarts, public Hubs/Spoke/Admin return 200 and `kubectl diff` is empty. No manual scale or alternate deployment path was used. |
| 2026-07-14 22:08-22:24 | Removed Reticulum's undocumented host privileges after an isolated smoke with the exact image proved it could write storage and bind without them. Added fail-closed manifest checks for privilege escalation, capabilities, seccomp and mount propagation. | Commit `83598d9` deployed through the standard manifest. Live Reticulum has zero effective capabilities, `NoNewPrivs=1`, no propagated mount, healthy storage writes and unchanged public behavior. |
| 2026-07-14 22:24-22:38 | Found that Phoenix channel-join logs could include the internal bot key. Added global nested credential filtering, regression tests and matching key-checksum annotations for Reticulum/orchestrator; published Reticulum with Actions run `29365678444`, accepted the filter, then rotated the ignored local key. | Commit `7ae357f` is live at digest `sha256:f4a8c5d9fa2a19acbed03fcaae9cf0bab1eefd6046a4d63a35a92fdf9bb481d2`. The real key is absent from new logs, `[FILTERED]` is present, Secret/annotations/process environments match by hash, all 12 deployments are Ready, public Hubs/Spoke/Admin return 200, `kubectl diff` is empty and the standard verifier reports 0 failures plus the known media warning. |
| 2026-07-14 22:38-22:48 | Mapped live service dependencies and enabled five Cilium ingress policies for bot-orchestrator, PostgreSQL, both PgBouncer pools and Photomnemonic. Added exact peer/port checks to the manifest verifier. | Reticulum can reach every required internal service; Hubs cannot reach bots/DB/thumbnails and bot-orchestrator cannot reach PostgreSQL. No pod restarted, public endpoints remained 200 and the live manifest converged. Commit `4e0414d`. |
| 2026-07-14 22:48-23:05 | Measured six idle resource samples, then introduced requests and memory limits without CPU limits. Rolled eight stateless workloads together and each `Recreate` workload (Coturn, Dialog, PostgreSQL, Reticulum) separately, validating ports, DB counts, security and bots after every step. Extended the live verifier to cover budgets, digests, strategies, Reticulum security, key checksum and policies. | All 13 containers are budgeted at 665m/2,944Mi application requests; node reservations are 33% CPU/58% RAM and memory limits 146% overcommitted for burst. Every pod has zero restarts, DB remains 356/94/18/127, ghost backend has two active rooms, `kubectl diff` is zero and the expanded verifier returns 0 failures plus the known media warning. Final cloud commit `b2cfd27`. |
| 2026-07-14 23:05-23:42 | Audited Kubernetes credentials and removed automatic service-account tokens from seven stateless Deployments, then Dialog and Coturn separately. Runtime inspection proved only HAProxy needs Kubernetes API access. | Each rollout used the generated manifest and returned Ready with zero restarts; new pods had zero token mounts, TLS/HTTPS/DB/bots stayed healthy and `kubectl diff` returned zero. Commits `3284541`, `75c2604` and `9a5917c`. |
| 2026-07-14 23:42-2026-07-15 00:18 | Coturn validation exposed that the inherited entrypoint logged its full PostgreSQL URI and carried it in `argv`. Created a digest-based wrapper with a `0600` config and regression test, published only through Actions runs `29371237341`/`29371663849`, and created `output/audit-db-rotation-20260714-235659/`. Two isolated restore attempts initially missed DB readiness/stdin wiring; neither touched production. The corrected restore passed `356/94/18/127`. | Final Coturn digest is `sha256:c2ad335349d477d342d5b17c82b513bfebc8c17b8e6b4e27a3049f3478207780`. A first password test used localhost `trust`, aborted and restored before apply; the corrected PgBouncer-to-PostgreSQL test proved old/new authentication, then DB rotation and four checksum-driven rollouts passed. Logs/argv contain no URI, TURN TCP is reachable and local/live credentials match without exposing values. |
| 2026-07-15 00:18-00:23 | Removed the final unnecessary service-account token from PostgreSQL in its own `Recreate` rollout and made both manifest/live verifiers fail closed on token mounts, DB-checksum drift and the known leaky Coturn digest. | All 11 non-controller Deployments are tokenless; only HAProxy retains `haproxy-sa`. Twelve pods are Ready with zero restarts, Hubs/Spoke/Admin return 200, DB remains `356/94/18/127`, ghost has two active rooms and `kubectl diff` is zero. Cloud HEAD `f896ba1`. |
| 2026-07-15 00:24-00:34 | Updated every direct GitHub Action to its current Node 24 release pinned by immutable SHA. `actionlint` also exposed and drove fixes for two empty branch filters, an undefined smoke input and untrusted PR branch interpolation. | Hubs commit `99f1e9cad` and cloud commit `97e6032`; real builds `29373238787`/`29373240303` passed without the prior Node 20 or buildx deprecation warnings. Two remaining Hubs annotations are Dockerfile style/signal warnings, not workflow failures. |
| 2026-07-15 00:34-00:41 | Captured a production-only npm dependency audit in `output/dependency-audit-20260715-0035/` and compared exact versions with Hubs `prod-2026-03-11` and Hubs CE `2.1.0`. | Hubs=20, Admin=7, Dialog=15, Photomnemonic=4, generator=0 and bot-orchestrator=0 vulnerabilities. Official upstream has the same vulnerable versions. No automatic fix was applied; Dialog has a tested zero-audit simulation but still needs Node 22/WebRTC acceptance. |
| 2026-07-15 00:41-00:54 | Changed bot-orchestrator from root to UID/GID 1000 and added fail-closed image/manifest checks for non-root, privilege escalation, capabilities and seccomp. After an initial isolated pod exposed Kubernetes' inability to verify a named user, switched to explicit numeric IDs, rebuilt through Actions `29374198198`, then repeated the isolated test before rollout. | Live digest is `sha256:0c2f0fb16828ee93dea0a9dc42f49928ed4e161a51d9ca02866fc02ab8c99ab8`; UID/GID 1000, CapEff=0, `NoNewPrivs=1`, seccomp=2, no token mount. Two ghost rooms rehydrated, no errors/restarts, all public/runtime checks pass and `kubectl diff` is zero. |
| 2026-07-15 00:54-01:20 | Modernized Dialog from Node 18 to Node 22, removed the unused direct `sctp` dependency, moved to Mediasoup 3.19.22, replaced private worker/producer internals, and added real worker/router/WebRtcTransport tests plus TCP startup/readiness/liveness probes. Built only through GitHub Actions `29375052801`, tested the image in an isolated pod, checkpointed the live Deployment, then applied the generated manifest. | Live digest is `sha256:95687f4765e7a68ef05a714b807bf5c80e0f9187e2715f3a5a96e2d664377a23`; production npm audit is 0, UID/GID 1000, CapEff=0, `NoNewPrivs=1`, seccomp=2, no token, three probes active, 0 restarts, valid TLS/4443 and no manifest drift. Two isolated browsers then published and consumed fake audio with connected signaling/send/recv/ICE, live producer/consumer tracks and more than 125 RTP packets each way in 2.5 seconds; Dialog logged no fatal/runtime errors. |
| 2026-07-15 01:20-01:45 | Replaced Photomnemonic's Node 14/Buster, Chromium 112 and Puppeteer 19 runtime with Node 22, Chromium 149 and Puppeteer Core 25.1. Added per-request URL/redirect/DNS validation, private/reserved range rejection, one-job concurrency, privacy-safe logs, page cleanup, non-root runtime, HTTP probes and exact egress policy. Built only through GitHub Actions `29376531637`; tested the digest first in an isolated pod and measured ~400 MiB RSS before setting 384/768 MiB request/limit. | Live digest is `sha256:aef369b82212429d01c0f1f554b16c34a99cf4bbb75e0693e190c796b33012f2`; production audit is 0, UID/GID 1000, CapEff=0, `NoNewPrivs=1`, seccomp=2, no token, probes active and 0 restarts. Real PNG capture passes; loopback/metadata/non-web ports return 403, a second concurrent request returns 429, sensitive path/query data is absent from logs, cluster API/metadata egress is blocked, all deployments are Ready and `kubectl diff` is empty. Cloud commits `662035c`/`e670a4a`. |
| 2026-07-15 09:40-10:30 | Completed Reticulum modernization and requested a second, independent read-only review of the SQL reconciliation and deployment gate. CI `29403210817` passed PostgreSQL 12.19/14.23, 278 tests and release; the reviewer confirmed that the transaction performs no delete/storage mutation and required a coherent backup plus canary before production. | Cloud commits `1ec6163`/`623e4ce`. Two initial isolated canaries correctly stopped on optional config placeholders and the HTTPoison 3 TLS option; both defects were fixed before any production rollout. |
| 2026-07-15 10:31-11:05 | Created `output/audit-retmodern-predeploy-20260715-103120/`, restored the baseline and coherent candidate repeatedly in PostgreSQL 12, then applied the reviewed reconciliation transaction to production. | Production changed from 127 active/0 inactive files and scene `vrsQuSc` to 34 active/93 inactive and `f6VKtim`; no row or file was deleted. Post-change DB and 34-pair storage archives pass dry-run restore validation and are fixed by `SHA256SUMS-POST-COHERENT`. |
| 2026-07-15 11:05-11:25 | Published Reticulum only through GitHub Actions `29403433612`, accepted its exact digest in an isolated PostgreSQL 12 canary with copied storage, reviewed the generated manifest diff and deployed through the standard `gen-hcce` + `kubectl apply` flow. Repeated backend and real-browser smoke tests after rollout. | Reticulum is live at `sha256:0d671eb2013e11ba2110998e7f25ed849c67fc9e9945894d2c5a71b77e23f595`; all pods/certificates are healthy, DB/storage counts remain coherent and manifest diff is zero. Ghost health shows two active rooms with backend `ghost`; a browser entered `VJopCY3`, received five bots and observed them moving. The only browser error was the expected missing audio producer after denying microphone permission. |
| 2026-07-15 11:29-11:51 | Audited bot-chat data flow and closed two privacy/abuse gaps: Hubs now shows a session-retention/provider notice, the orchestrator rate-limits by account+room across every bot, and Reticulum filters `message`/`prompt` parameters. Ran Hubs check/lint/build, 18 orchestrator tests and the full 278-test Reticulum suite. Created `output/audit-botprivacy-predeploy-20260715-114137/` before rollout. | GitHub Actions builds `29405023919`, `29405025982`, `29405028046` and Reticulum CI `29404978302` passed. The three exact digests were deployed only through generated `hcce.yaml` plus `kubectl apply`; 12 deployments, DNS/TLS, DB/storage, HTTPS and two ghost rooms pass with zero manifest drift. Live tests rejected harmful input, did not expose system markers, proved cross-bot rate limiting, found no test prompts in logs and showed the privacy notice with five moving bots in a real browser. |
| 2026-07-15 11:55-12:12 | Reproduced the empty Home hero at 390 px and found that 24 JavaScript bundles retained the literal base-assets placeholder because container startup only rewrote HTML/CSS. Extended substitution to JS with a fail-closed residual check and reserved mobile header space for the session block. Ran TypeScript, lint, unit test, production build and a Docker-equivalent placeholder substitution fixture. | Hubs commit `26bea43d2`, Actions `29406729718` and digest `sha256:7171f00792264f18b6e92d8ab6dac28894a69bdb0d0bb7257314aea2f294c3a0` deployed through the standard generated-manifest flow. Browser acceptance shows a real 1920x1080 hero from `assets.meta-hubs.org`, no placeholder, no 390 px overflow/overlap and a healthy 3D lobby. All 12 deployments, DB/storage, TLS, ghost runners and zero-drift verification pass. |
| 2026-07-15 12:15-12:38 | Ran an independent read-only review of the remaining audit path, then fixed Spoke's broken `401` publish retry and missing abort signal on the main GLB upload. Added regression tests and ran lint, all 65 unit tests and the production build under the Dockerfile's Node 16.13.2. | Cloud commit `cc5c831`, GitHub Actions `29408127281` and digest `sha256:ee8f90904034e60be8e82489870e3d7e7dec59ab2bdba6087e051bbfaf39e8fc` deployed through an exact one-image manifest diff. Spoke is 1/1 with zero restarts; bot-orchestrator did not restart; DB/storage, HTTPS, ghost runners, verifier and post-apply `kubectl diff` all pass. Mutable publish/orphan-file testing remains gated by a fresh checkpoint. |
| 2026-07-15 12:38-12:58 | Fixed sitting state races found by the independent review. Mobile movement now clears sitting, both loaders cancel pending waypoint travel on ownership loss and bitECS restores a safe spawn. Ran Hubs type check, targeted lint/tests and production build. | Hubs commit `6e0092252`, Actions `29408883985` and digest `sha256:a0d5b5f220c882a3c4dc2ac328f59b5970ee1040b09eade474057eb57cf85eca` passed and was accepted live. Basic sit/stand works over TCP; the recovered scene's two provisional seat waypoints still need repositioning and a real multiuser race test remains open. |
| 2026-07-15 12:58-13:23 | Hardened private Avaturn validation and stale preview handling, then made Spoke reject malformed/expired local JWTs. Added three skeleton tests plus five direct Spoke API tests; full Hubs checks/build and all 68 quoted-glob Spoke tests passed. Cancelled incomplete Hubs build `29410111645` before deploy, then built the final commits only through Actions. | Hubs `7f016c986`/run `29410309085` and Spoke `56afaee`/run `29410656894` are live at digests `sha256:64953f6323ba4c87e0408fba73a9784a6a45f68c4a4608494d419a48aefbff5a` and `sha256:f5120264938e189e702f835182ed4a28a5ce20b140d7262bc2a3074e6d0b6657`. Generated diff contained only those two image fields; rollouts are 1/1 with zero restarts, bot-orchestrator was not restarted, `kubectl diff` is empty and the full live verifier reports 0 failures/0 warnings. An authenticated browser confirmed the Spanish Avaturn actions, private guide and disabled save gate without mutating production. |
| 2026-07-15 13:41-14:25 | Created a fresh DB+storage checkpoint, changed and restored the name of the real Spoke project with two authenticated saves, and compared DB UUIDs with physical pairs after each operation. Cancelled Publish after proving the project scene is referenced by live rooms. | Both saves preserved 34 active files and created four complete deferred pairs, with no missing active bytes. The real project name was restored; no scene was published or room reference changed. |
| 2026-07-15 14:25-16:33 | Added per-file owned-file reconciliation, complete-pair validation before reactivation, 24-hour two-stage orphan cleanup and daily scheduling. Two independent reviews drove removal of a global transaction, preservation of historical missing rows, metadata-only scanning and a full cron isolation test. | Cloud commit `dfc248f6bd`; local suite passed 300 tests + 5 properties, strict compile/release and both PostgreSQL CI jobs in run `29432345676`. Residual risk is the legacy absence of DB foreign keys; two grace windows mitigate it until the stable CE upgrade is audited. |
| 2026-07-15 16:28-16:40 | Captured `output/audit-spoke-reconcile-predeploy-20260715-182804/`, aligned backup/restore verification with complete deferred pairs, published Reticulum only through Actions `29432642364`, reviewed a one-image generated-manifest diff and deployed it. | Live Reticulum digest is `sha256:5afd810973aa2f54d863ec524fa2320f83b74d210abb7e5b15df62a708233651`. All deployments, DNS/TLS, DB, 34 active/38 complete storage pairs, HTTPS and two ghost rooms pass with 0 failures/0 warnings; post-deploy `kubectl diff` is zero. |
| 2026-07-15 16:40 | Refreshed official upstream refs and fixed the upgrade order. | Stable targets remain Hubs `prod-2026-03-11` and Hubs CE `2.1.0`. Upgrades will be prepared on separate branches from the accepted baseline and will not be deployed until custom features and the full audit pass. |
| 2026-07-15 16:42-17:05 | Created isolated upgrade branches and integrated Hubs `prod-2026-03-11` plus Hubs CE `2.1.0`. Preserved the ignored local-values policy, retained Bamboo `server` alongside Swoosh `relay`, and aligned the CE lockfile version. | Hubs check/lint HTML+JS, 4 unit tests and production build pass. The CE generator verifies 44 resources and Reticulum Actions `29433706318` passes PostgreSQL 12.19/14.23 plus release. No candidate image was built or deployed; production remains on the audited baseline. |
| 2026-07-15 16:50-16:54 | Built the stable-upgrade Hubs candidate only through GitHub Actions `29434015549`. | Candidate digest is `sha256:fa72e5ea526704337ed0eef182b0005899d559cae775a2c9761c13577d909786`. Local deployment values and Kubernetes were not changed; the image awaits isolated acceptance. |

## 2026-07-16 (Cierre de auditoria, upgrade estable y aceptacion final)

Time reference: UTC.

| Time | Action | Result |
|------|--------|--------|
| 2026-07-15 16:42-2026-07-16 01:20 | Integramos Hubs `prod-2026-03-11` y Hubs CE `2.1.0` en ramas aisladas, modernizamos y auditamos servicios, seguridad, privacidad, bots, avatares, sitting y UI, y construimos todas las imagenes custom exclusivamente por GitHub Actions. | TypeScript, lint, 12 pruebas Hubs, 19 pruebas del orquestador, suites backend y generador de 44 recursos pasan. Hubs/Admin/Dialog/Photomnemonic/generador/orquestador quedan con 0 advisories de produccion. |
| 2026-07-16 00:00-01:20 | Probamos rollback real de Hubs, corregimos la incompatibilidad runtime de `js-cookie`, reiniciamos Reticulum tras cada cambio de assets y anadimos una comprobacion obligatoria de carga fria (`APP`, `AFRAME`, CSP y excepciones). | El rollback recupero produccion sin perdida y el verificador evita aceptar una pagina HTTP 200 cuyo JavaScript no arranque. |
| 2026-07-16 01:20-01:56 | Cerramos la revision visual responsive. Anadimos badge de version movil, ocultamos la toolbar durante los modales de entrada y recorrimos el flujo completo a 390x844 y 1440x900. Publicamos el commit Hubs `cbded5883` con Actions `29464896181` y desplegamos el digest `sha256:c5e2ee4eb125535b8b8ca55a369f24e2e2c5bcf2882158e53996bf5df3c030f3`. | El boton de entrada movil queda visible, la sala arranca sin overflow, el badge `45de1528` aparece en movil/escritorio, cinco bots se ven y se mueven y no hay excepciones de navegador. El verificador live termina con 0 fallos/0 avisos y `kubectl diff` queda vacio. |
| 2026-07-16 01:56-02:01 | Consolidamos por fast-forward las ramas auditadas en `hubs/master` (`cbded5883`) y `hubs-cloud/master` (`cc43df4`), actualizamos los punteros del superproyecto y cerramos el handoff operativo. | Los subrepos quedan limpios y sincronizados con GitHub; `meta-hubs/main` pasa a ser la unica base de continuidad documentada. |
| 2026-07-16 08:45-09:35 | Investigamos el magic link desde navegador, Reticulum y SMTP. La conexion directa a Mailtrap funcionaba, pero Bamboo/gen_smtp fallaba en OTP 27 antes de AUTH con `verify_peer` y sin CA bundle. Mantuvimos STARTTLS obligatorio, fijamos `tls_verify: :verify_none` para el adaptador legacy y corregimos las metricas para contar `sent` solo tras una entrega real. | Cloud commit `9781d06`; CI `29480369600` paso PostgreSQL 12.19/14.23, 304 tests, 5 properties y release. Build `29480568375` publico el digest `sha256:0b1f8104a520a15828f92ac1428c98a8f45846dcf49d0c99e8ea929f26dad317`, desplegado por el manifiesto estandar. Un magic link real fue entregado a `info@virtualmente.com`, Hubs verifico la sesion y Spoke abrio `/spoke/projects`. |
| 2026-07-16 09:32-10:05 | Diagnosticamos que la escena recuperada tenia `walkable`/`collidable` pero ningun `nav-mesh`, causa del movimiento vertical a traves del suelo. Creamos el checkpoint `output/magiclink-scene-prepublish-20260716-093347/`, anadimos un `Floor Plan` desde Spoke y republicamos el proyecto `qa3U3Ke`. | La escena conservo el SID `f6VKtim` y ahora usa `749efd34-73a0-496c-8584-3958b01ef186.bin`, con nodo `navMesh` y componente `nav-mesh`. La sala `VJopCY3` cargo un nav mesh, el modelo respondio 200 y el navegador no registro errores. |
| 2026-07-16 09:00-10:10 | Durante el diagnostico se ejecuto accidentalmente un `ret rpc` contra el proceso principal y el pod antiguo de Reticulum sufrio un OOM/restart. No se perdieron datos; el proceso se recupero y el rollout normal sustituyo despues ese pod. | Se deja registrado para no usar `ret rpc` de diagnostico pesado contra produccion. La verificacion final confirma los 12 deployments Ready, Reticulum 2/2 con 0 reinicios en el pod nuevo, DB/storage coherentes, TLS valido, ghost runner sano y `verify-live-reactivation.sh` con 0 fallos/0 avisos. |
| 2026-07-16 09:40-09:48 | Investigamos por que Espacio no mostraba los waypoints de las sillas y por que faltaban controles de edicion. Confirmamos que los 10 waypoints publicados tenian `canBeClicked=false`; las dos sillas si tenian `willDisableMotion=true`. Tambien confirmamos que `info@virtualmente.com` posee el proyecto `qa3U3Ke` y la escena `f6VKtim`, mientras la sala `VJopCY3` conservaba una cuenta creadora historica distinta. | La ausencia de iconos no era un fallo de permisos: `Disable motion` activa Sitting, mientras `Clickable` controla el objetivo de Espacio. Se anadio la cuenta actual como propietaria adicional de la sala sin eliminar al creador historico. |
| 2026-07-16 09:48-09:51 | Antes de mutar contenido creamos `output/room-seat-ownership-prechange-20260716-114047/` con dump Reticulum, archivo de storage y checksums. Dos intentos de usar helpers de release `ret eval/rpc` dentro del contenedor live provocaron dos reinicios por OOM; se detuvo la mutacion hasta que Reticulum volvio a `2/2` y la web a HTTP 200. | No hubo perdida de datos. Se prohibe usar helpers BEAM dentro del pod live en esta topologia; la sesion posterior se firmo fuera del contenedor y fue validada por la API normal. |
| 2026-07-16 09:51-10:00 | Modificamos unicamente `canBeClicked=true` en `Seat recovery 1` y `Seat recovery 2`, tanto en el proyecto Spoke actual como en el JSON chunk del GLB publicado. Guardamos el proyecto y publicamos por las APIs normales de Spoke/Reticulum, conservando scene SID `f6VKtim`, navmesh, Floor Plan, posiciones, ocupacion y los ocho waypoints `spawbot-*`. | El proyecto sigue siendo `qa3U3Ke`; el modelo actual es `beadd397-5ade-4105-ae24-2e7189edea9e.glb`. Las copias descargadas desde produccion coinciden por SHA-256 con los archivos preparados y contienen 10 waypoints, dos sillas y dos sillas clicables. |
| 2026-07-16 10:00-10:05 | Ejecutamos aceptacion autenticada en navegador aislado. Spoke abrio el proyecto con jerarquia, propiedades, guardado y publicacion; Hubs mostro los controles de propietario de sala. Tras entrar completamente y mantener Espacio aparecieron exactamente dos objetivos blancos; no hubo errores de consola. | Queda validado el flujo. `Mirar` solo entra al vestibulo y no debe usarse para probar interaccion de waypoints. |

## 2026-07-16 (Pulido visual Obsidian Aurora)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-16 12:15-13:15 | Auditamos visualmente home, entrada, audio, sala, toolbar, sidebars, chat y selector de avatares a 1440x900 y 390x844. Reestructuramos capas y superficies, compactamos el dock, separamos toolbar/sidebar, refinamos controles compartidos, anadimos estado vacio de chat, convertimos las acciones de avatar en tarjetas compactas y corregimos el header autenticado. | La escena 3D permanece visible, no hay overflow horizontal, los modales no compiten con la toolbar, la sidebar usa toda su altura y la navegacion de escritorio deja de partir el correo. `Invitar` y `Compartir` quedan diferenciados por funcion. La especificacion de upgrade y rollback vive en `features/ui-award-polish/README.md`. |
| 2026-07-16 13:28-13:52 | Publicamos Hubs commit `c679151ed` exclusivamente mediante GitHub Actions run `29494554300`, fijamos la imagen por digest `sha256:e61d253ea651280f75800a717cb24f66af35949266cd316402526d520766f103`, generamos y verificamos los 44 recursos y revisamos un diff limitado a `deployment/hubs`. Aplicamos el manifiesto estandar y reiniciamos Reticulum para renovar HTML y hashes. | Hubs y Reticulum completaron rollout; los 12 deployments estan Ready. `verify-live-reactivation.sh` termino con 0 fallos/0 avisos. Navegadores limpios desktop y movil entraron en `VJopCY3`, cargaron cinco bots, no registraron excepciones ni overflow y validaron home, chat, avatares y scroll movil. El badge live es `785aa63d`; las capturas estan en `output/ui-award-audit-20260716/production/`. |

## 2026-07-16 (Landing Obsidian Aurora y analisis de capacidad)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-16 14:15-15:10 | Redisenamos la landing completa como portal Obsidian Aurora, conservando autenticacion, creacion de salas, favoritos y salas publicas. Validamos check, lint JS/HTML, 12 pruebas, build de produccion y QA visual a 1440x900 y 390x844. | Hubs commit `1f569385d` se publico solo mediante GitHub Actions `29499784485`; el digest `sha256:b568a9c8565f7983018c2a72d9e13b7bdb32b552381b79a32fdc904bc5e0097c` se desplego mediante un diff limitado a `deployment/hubs`. Tras reiniciar Reticulum, el verificador completo termino con 0 fallos/0 avisos, `kubectl diff` quedo vacio y navegadores limpios confirmaron la landing, la sala 3D, ausencia de overflow, imagenes rotas y excepciones. Capturas en `output/playwright/landing-aurora-production/`. |
| 2026-07-16 14:25-15:10 | Auditamos sin modificar codigo de bots el coste del cluster, el ghost runner, la navegacion y objetivos de 30/100 usuarios por sala y 300/10.000 CCU totales. | El coste base medido es ~62 USD/mes. Dos ghost rooms usan ~140 MB de cgroup y CPU minima. Los logs prueban `colliders=0`, por lo que el fallback permite atravesar estructuras. Se recomienda `mobility: static` para bots inmoviles y navmesh+A* sobre el ghost runner para navegacion robusta. El informe durable es `docs/bots-cost-capacity-analysis-2026-07.md`. |
| 2026-07-16 15:10-16:23 | Implementamos `mobility: static` de extremo a extremo y navegacion A* sobre el navmesh de Spoke en el ghost runner. El parser usa HTTP Range, limites de seguridad y fallback compatible; el generador y verificadores exigen la nueva configuracion. | Hubs check/lint/build, 22 pruebas y audit del orquestador, compilacion estricta y 305 pruebas Reticulum, y generacion de 44 recursos pasan. La escena real produce 350 triangulos, 11 grupos, 8 puntos y 56/56 rutas. |
| 2026-07-16 16:23-16:45 | Publicamos exclusivamente por GitHub Actions: Hubs `29506573351`, bot-orchestrator `29506574473`, Reticulum CI `29506498175` y build `29506780044`. Fijamos los tres digests exactos, generamos 44 recursos y revisamos un diff limitado a Hubs, Reticulum, bot-orchestrator y las cuatro variables de navegacion. | Los tres rollouts y el reinicio obligatorio de Reticulum terminaron correctamente. `kubectl diff` quedo vacio y `verify-live-reactivation.sh` termino con 0 fallos/0 avisos: 12 deployments Ready, TLS, DB/storage, CSP, seguridad y ghost runner sanos. |
| 2026-07-16 16:27-16:35 | Una edicion local insegura con interpolacion in-place trunco los dos YAML ignorados antes del apply. El verificador bloqueo el proceso, por lo que no hubo cambio en el cluster. Reconstruimos los 50 valores necesarios desde el Secret y deployments live, normalizamos y validamos criptograficamente el PEM, y exigimos paridad exacta del Secret generado. | No se expuso ni versiono ningun secreto y la comparacion final fue `missing=[]`, `extra=[]`, `changed=[]`. Se documenta como regla permanente usar temporal `0600`, validacion y reemplazo atomico; no usar Perl/Ruby in-place sobre referencias `@sha256`. |
| 2026-07-16 16:45-16:55 | Aceptamos la feature en navegador y API live. Cinco bots fueron visibles y se movieron sin excepciones; `static` mantuvo los cinco exactamente a 0 m durante la muestra y, tras restaurar `low`, cuatro retomaron movimiento. El chat respondio a un saludo sin accion y a `Ve a spawbot-2` con la accion allowlist correcta. | Evidencia en `output/playwright/bots-navmesh-static-production/`. Los logs de las dos salas activas confirman `Navmesh ready: triangles=350 groups=11 mode=navmesh_preferred` sin fallback. `hubs/master` quedo en `a7214eb88` y `hubs-cloud/master` en `5a82de5`. |

## 2026-07-16 (Auditoria integral, recuperabilidad y limpieza)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-16 17:00-18:20 | Auditamos los tres repos, upstream estable/master, dependencias, Actions, secretos, imagenes, manifests, costes, navegacion, privacidad y UX. Ejecutamos suites de Hubs, Admin, Hubs CE, Reticulum, bots, Dialog, Photomnemonic, Coturn y Spoke, ademas de Gitleaks, Trivy, SBOM, Actionlint y ShellCheck. | Las releases estables estan completamente integradas. Las suites y builds pasan; el navegador desktop/movil y el verificador live informan sala lista, cinco bots y 0 fallos/0 avisos. Se documentan deuda legacy Spoke, bundle Hubs, cowlib y falta de prueba de carga. |
| 2026-07-16 17:30-18:10 | Investigamos credenciales historicas sin imprimir valores. Revocamos el PAT GitHub antiguo; detectamos una clave OpenAI historica aun activa y un `ghcr-pull` live caducado. | El entregable actual pasa Gitleaks. La revocacion OpenAI queda como accion manual del propietario y el preflight bloquea nuevos rollouts hasta renovar GitHub/GHCR. No se reiniciaron pods ni nodos. |
| 2026-07-16 18:31-18:41 | Creamos el checkpoint completo `output/backups/20260716-183112` con DB, `ret-pvc`, inventario Git/Kubernetes/DigitalOcean, digests, presencia de configuracion y checksums. | DB 49 KiB y storage 183 MiB; schema 356, 94 migraciones, 33 activos, 47 pares completos y 14 diferidos. Ambos restore dry-runs y `SHA256SUMS` pasan. |
| 2026-07-16 18:20-19:20 | Anadimos gates reproducibles (`audit-upstream.sh`, `verify-project.sh`), CI de seguridad, healthchecks y correcciones de calidad en servicios. Centralizamos reglas en `AGENTS.md`, escribimos workflow upstream, inventario custom y ciclo de vida por cliente. | Los cambios estan validados localmente pero no desplegados. Produccion conserva los digests aceptados hasta renovar GHCR y construir por Actions. |
| 2026-07-16 18:45-19:20 | Creamos `OLD/` y trasladamos runbooks sustituidos, freeze incompleto, investigacion Avaturn previa, parches de tercera persona y notas bitECS. Eliminamos referencias operativas a esos archivos y documentamos su sustituto. | El arbol activo queda orientado a fuentes vigentes; `OLD/README.md` deja claro que el archivo no es input de implementacion o deploy. |
| 2026-07-16 19:03-19:14 | Repetimos el gate integral desde cero tras estabilizar el wrapper local de Spoke. Pasaron Gitleaks, Actionlint, ShellCheck, Hubs/Admin, generador CE, bots, Dialog, Photomnemonic, Coturn, Spoke y Reticulum. | Resultado final: 12 tests Hubs, 22 bots, 2 Dialog, 7 Photomnemonic, 1 Spoke y 305 Reticulum, todos correctos; `verify-live-reactivation.sh` termino con 0 fallos/0 avisos. |
| 2026-07-16 19:14-19:17 | Fusionamos por fast-forward y publicamos el cierre tecnico en `hubs/master` (`492625c57`) y `hubs-cloud/master` (`0e63d0f`). | Las ramas fuente quedan limpias y sincronizadas con GitHub. Produccion no se modifico: los cambios de CI/healthchecks esperan una futura build por Actions tras renovar GHCR. |
| 2026-07-16 19:18-19:20 | El gate post-merge detecto que BSD `cp` intentaba copiar atributos extendidos de un FBX al staging temporal de Gitleaks. Anulamos resource forks/xattrs solo para esa copia mediante `COPYFILE_DISABLE=1` y repetimos el gate. | La verificacion estatica vuelve a pasar en macOS y conserva el mismo comportamiento en Linux; no hubo cambio de runtime. |
| 2026-07-16 19:20-19:28 | La primera ejecucion post-push de cloud descubrio que el generador dependia del `input-values.yaml` local ignorado y que ShellCheck encontraba deuda en `hab-wrap-and-push.sh`. Anadimos `input-values.ci.yaml` no sensible, lo cableamos al workflow y corregimos quoting/seleccion de artefactos en `d797792`. | Security CI `29519816469` y Reticulum CI `29519815859` pasaron. La validacion de servicios avanzo hasta revelar una incompatibilidad GNU/BSD de `stat` en la prueba Coturn, sin afectar produccion. |
| 2026-07-16 19:28-19:36 | Hicimos portable `test-entrypoint.sh` seleccionando explicitamente GNU o BSD `stat` y publicamos `hubs-cloud/master` en `4a1e3b9`. Sustituimos ademas la copia temporal del verificador root por staging byte a byte y recreacion explicita de symlinks para eliminar definitivamente la dependencia de xattrs macOS. | Cloud Security `29520235224` y Services `29520235446` pasaron, incluidos generador, 22 tests bot, Dialog, Photomnemonic, Coturn y Spoke. El gate estatico local completo vuelve a pasar; no hubo rollout ni cambio del runtime live. |
| 2026-07-16 21:41-21:59 | Renovamos la autenticacion GitHub/GHCR sin exponerla: GitHub CLI y llavero macOS, secretos `REGISTRY_*` de `hubs`/`hubs-cloud`, `Secret/ghcr-pull` y `ServiceAccount/default`. Verificamos token de pull para los tres repositorios y permiso de inicio de upload, sin publicar una imagen. | El preflight completo usa el checkpoint nuevo y termina con 0 fallos/0 avisos; un nodo nuevo o reschedule ya puede descargar los digests privados. |
| 2026-07-16 21:50-21:56 | El primer checkpoint de la sesion exporto la DB pero una interrupcion transitoria de `kubectl exec` trunco el stream de storage; el script rechazo correctamente el resultado. Repetimos desde cero y creamos `output/checkpoints/20260716-215518/`. | Checkpoint valido: DB 49 KiB, storage 183 MiB, 356 de schema, 94 migraciones, 33 activos, 47 pares completos y 14 diferidos. `SHA256SUMS`, gzip y restore dry-run pasan; el intento incompleto se elimino. |
| 2026-07-16 22:00-22:10 | Creamos una clave OpenAI nueva desde el panel, la guardamos en llavero, validamos Models/Moderation/Responses con `gpt-5-nano`, sustituimos el valor local de forma atomica y generamos los 44 recursos. La comparacion previa mostro que solo cambiaba `OPENAI_API_KEY`; el resto del manifiesto tenia diff cero. | Aplicamos el manifiesto estandar, reiniciamos solo bot-orchestrator y confirmamos por huella que cargo la clave nueva. `/health` muestra ghost, dos salas y GPT-5 Nano; un chat live respondio sin fallback. El verificador final termina 0/0. La clave anterior queda pendiente de revocacion en el panel sin mover la sesion visible del usuario. |
| 2026-07-16 22:35-22:36 | El propietario revoco en el panel la clave OpenAI historica `Meta-Hubs`. Comprobamos la credencial anterior desde terminal sin imprimirla y eliminamos su copia temporal del llavero. | La clave antigua devuelve HTTP 401 y la nueva sigue devolviendo HTTP 200. La rotacion OpenAI queda completamente cerrada. |

## 2026-07-17 (Hardening local de recuperacion)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-17 | Endurecimos backup y restore sin tocar produccion: contexto y UID de namespace exactos, confirmaciones destructivas ligadas al destino, espera fail-closed de pods, permisos `0600` y validacion offline cruzada entre `ret0.owned_files` y pares del tar antes de checksums. | Fixtures y stubs locales de `kubectl` cubren preflight, contexto/UID incorrectos, timeout, pods remanentes, activos ausentes, pares incompletos y modos de fichero. No se uso ningun cluster ni se leyo configuracion sensible. |

## 2026-07-17 (Auditoría final, sitting autoritativo, bots y capacidad)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-17 | Evaluamos el cierre de Ready Player Me y alternativas oficiales sin crear cuentas ni gasto. Neutralizamos la UI candidata a carga GLB privada y mantuvimos importación manual; MPFB/MakeHuman queda como piloto local recomendado y Avaturn/MetaPerson en hold contractual. | La decisión, precios observados, licencias, privacidad, gates y rollback están en `docs/avatar-provider-evaluation-2026-07.md`. El candidato Hubs provider-neutral pasó integración local; no está desplegado. |
| 2026-07-17 | Reproducimos con dos sesiones aisladas la carrera de sitting del baseline live y el cierre abrupto. | Se observó doble ocupación transitoria (~17 ms) y estado NAF visual obsoleto durante más de 6,5 s; no se mutó producción. El candidato reemplaza esa autoridad cliente por protocolo Phoenix/PostgreSQL v1, lease de 15 s e identidad Spoke estable. |
| 2026-07-17 | Implementamos y revisamos adversarialmente el cliente sitting autoritativo, incluidos switches atómicos, reintentos/idempotencia, migración de canal, cleanup condicional, claims locales y reutilización de EID/Object3D. | Commit Hubs candidato `49342d8c4`; check/lint, 34 pruebas y build pasan. La integración con auth de runner y GLB neutral termina en `9c2da562b` con 38/38; falta staging/live. |
| 2026-07-17 | Implementamos reservas server-side y endurecimos bots/IA: runner Presence autenticada, namespace exacto, ACK de spawn, tipos NAF fail-closed, lifecycle sin carreras, navmesh/readiness autoritativos, recuperación, moderación/deadline de 4 s y modelo reply-only. | Bot commit `90c0a8f4b`; integración con reservas `ce20e209f`. Pasan 51/51 del orquestador, Reticulum 336 tests + 5 properties, compilación estricta y generación aislada de 44 recursos. No hay build Actions ni despliegue. |
| 2026-07-17 | Endurecimos adversarialmente el contrato reproducible de capacidad para `room-30`, `room-100-experimental`, `total-300` y el modelo `total-10000-model`, siempre con 0/5/10 bots. Cerramos esquema, identidad raw/manifest, mapping Prometheus, destino/service worker/WebSocket/ICE, árbol real Node+Chromium, shards/STOP, envelope global, SKU/topología y confianza externa. | 91/91 pruebas locales y `npm run validate` pasan, incluida una mutación con Chromium local sobre `about:blank`. El almacén de confianza de producción queda vacío hasta recibir la clave pública del propietario. No se ejecutó carga Hubs/staging ni se certifican CCU o compras. |
| 2026-07-17 | Auditamos Spoke legacy y corregimos en candidato solo el glob AVA para descubrir la suite completa. | Commit `0edd75b` pasa 68/68 con Node 16.13.2/Yarn 1.22.22. El informe registra 42 hallazgos Trivy, bases flotantes, root y probes incompletas; la modernización queda separada. |
| 2026-07-17 | Durante un diagnóstico se mostró al registro de la tarea contenido real del `hcce.yaml` local ignorado. Otra comprobación aislada sobrescribió brevemente ese fichero por una colisión temporal; se reconstruyó desde valores locales ignorados sin apply. | No hubo commit, push, cambio Kubernetes ni pérdida de producción. Los gates de generación quedaron aislados, pero el siguiente rollout está bloqueado hasta checkpoint y rotación coordinada de todos los secretos potencialmente expuestos, verificando solo por huellas. |
| 2026-07-17 | Actualizamos contratos de sitting/bots/avatares/capacidad, el handoff, auditoría y runbook; archivamos los prototipos RPM sustituidos y ampliamos el verificador live para exigir `/ready` autoritativo. | Las pruebas del gate de seguridad cubren liveness/readiness exactos. Al cierre de esa pasada todavía faltaban los gates raíz, publicación y toda aceptación staging/live. |
| 2026-07-17 | Endurecimos los gates raíz de aceptación, checkpoint/restauración, contexto/PVC Kubernetes, credenciales temporales, rangos Gitleaks y gitlinks, sin usar manifiestos generados como input ni tocar producción. | La suite local de recuperación pasa 52/52 y la de seguridad 22/22 contra el handler `/ready` candidato real. El gate integral y el conteo final siguen pendientes de integrar el commit Cloud en el submódulo compartido y repetir ShellCheck, Actionlint, Gitleaks y `verify-project`; no se declara todavía un cierre publicable. |
| 2026-07-17 | Cerramos una segunda oleada local de recuperabilidad: checkpoint atomico, sidecar checksummed con contrato DB exacto, materializacion con propiedad explicita, backup de PVC ligado al montaje exacto UID/pod/volumen/claim y driver destructivo unico para DB+storage. | Los hijos de restore ya no pueden reactivar consumidores entre mitades; un lock global create-only impide restores concurrentes y queda retenido con los cinco consumidores a cero ante cualquier fallo. El pod temporal se liga por UID/token/spec/montaje exactos, y el driver reanuda proxies -> Reticulum Ready -> Coturn -> bot antes de soltar el lock. El preflight liga los 13 contenedores del inventario a los overrides exactos y rechaza init/efimeros, replicas o estrategias inseguras. Pasan 119/119 regresiones de recuperacion y 28/28 gates de seguridad contra el handler candidato real, ademas de ShellCheck y diff-check del arbol raiz, Actionlint de todos los workflows y Gitleaks worktree en los tres repos. No hubo commit, push, cluster ni produccion. |
| 2026-07-17 | Una busqueda local de fixtures incluyo accidentalmente referencias de imagen del fichero privado de values; no se repiten ni se inspecciona de nuevo ese fichero. | Se mantiene el bloqueo AUD-065: antes de cualquier rollout hay que crear checkpoint y completar la rotacion coordinada por huellas de todo valor potencialmente expuesto. No hubo mutacion externa. |
| 2026-07-17 | Durante la revisión adversarial actual, una segunda búsqueda local demasiado amplia volvió a alcanzar una línea del fichero de configuración privado. El contenido no se reproduce ni se usó como evidencia. | El evento queda absorbido por AUD-065 y por el mismo alcance preventivo de checkpoint y rotación coordinada. Las búsquedas y gates posteriores se limitan a rutas trackeadas o exclusiones explícitas; no hubo commit, push, apply ni mutación externa. |
| 2026-07-17 | Al comprobar una opción de cliente se ejecutó accidentalmente `kubectl delete --raw` con `--dry-run=client` contra un namespace y ConfigMap deliberadamente ficticios (`nope/nope`); el modo raw ignoró el dry-run y contactó el contexto actual. | El API respondió `NotFound`: ninguno de los dos recursos existía y no hubo eliminación ni cambio de estado. Se detuvieron todas las comprobaciones kubectl reales y el resto de la revisión continuó solo con fixtures locales. |
| 2026-07-17 | En una segunda comprobación de sintaxis se ejecutó accidentalmente un DELETE raw contra el namespace y pod ficticios `x/y`, también con `--dry-run=client`; este modo no protege las peticiones `--raw`. | El API respondió `NotFound`, por lo que el recurso no existía y el cambio de estado fue cero. Desde ese punto se prohibieron incluso las consultas de ayuda con contexto activo; toda validación restante usa exclusivamente stubs y fixtures locales. |
| 2026-07-17 | Cerramos localmente el hardening raíz de recuperación y seguridad sin volver a contactar cluster/red: checkpoint coordinado schema 2 con procedencia local y quiescencia, hash del SQL completo, layout storage exacto, lock/UID/CAS, helpers create-only y restore DB+PVC sin ventana intermedia. Los gates preflight/live fijan Reticulum a una réplica, `Recreate` exacto, un único Pod Ready ligado al UID del Deployment y ningún HPA. El bootstrap Gitleaks usa política/baselines solo de la revisión base confiable; si esa base aún no los contiene, genera una configuración mínima `extend.useDefault=true`, ignora los ficheros candidatos y escanea toda la historia alcanzable de cada submódulo. | Pasan 133/133 regresiones recovery con stubs, 34/34 gates root contra el checkout candidato Cloud aislado, ShellCheck, Actionlint, sintaxis shell/Node, diff-check y Gitleaks worktree en root/Hubs/Cloud. El gate root sobre el submódulo compartido permanece explícitamente pendiente hasta integrar Cloud; no hubo commit, push, apply, rollout ni mutación externa. |
| 2026-07-17 | Cerramos tres gaps P1 del arnés de capacidad exclusivamente en código, fixtures y documentación local: timestamps de fuente dentro de la ventana firmada; derivación Prometheus multiserie real para counters/resets, histogramas y ratios; e inventario machine/boot/cgroup con `cgroup.procs` post-STOP ligado a shards y bundle final firmado. | El loader revalida el inventario y cero huérfanos, el modelo `INSUFFICIENT` conserva un conjunto exacto de claves sin fit/nodos/costes, `npm test` pasa 104/104 y `npm run validate` mantiene readiness `BLOCKED`, `physicalExecutionAllowed: false` y `certified: false`. No hubo carga física, red, clúster, compra ni despliegue. |
| 2026-07-17 | Consolidamos los candidatos locales: Hubs `d7f0c2fc4` pasó 10/10 pruebas focales, 79/79 totales, build, check, lint, Prettier y Gitleaks; Cloud `aa43df6` + `b7b752f` pasó Reticulum 391 tests + 5 properties, Node 102/102, generador 26/26, Spoke 68/68 con lint/build y Gitleaks. | El gate integrado de raíz sigue pendiente. No hubo despliegue ni aceptación live: permanecen bloqueantes AUD-065, AUD-075, el fencing DB de runners, la aprobación o migración de configuraciones heredadas y la prueba física de capacidad no ejecutada. |
| 2026-07-17 | Integramos los gitlinks finales y repetimos `verify-project` normal y `--full`. El primer full detectó que Spoke no recibía su `NODE_PATH` bajo el wrapper Node 16/Yarn 1; se endureció el gate con versiones exactas y el segundo full terminó verde: seguridad 36, recuperación 137, Hubs 79, navegador 11, capacidad 115, bots 102, Dialog 2, Photomnemonic 7, Spoke 68 y Reticulum 391 + 5 properties. | La integración local queda cerrada y reproducible. No hubo push, Actions, despliegue ni aceptación live; siguen bloqueando producción AUD-065, AUD-075, fencing DB, aprobación/cuarentena de configuraciones heredadas y la ausencia de evidencia física de capacidad. |
| 2026-07-17 | Publicamos las ramas candidatas y abrimos PRs draft separados: Hubs `#3` hacia `master`, Hubs Cloud `#1` hacia `development` y meta-hubs `#1` hacia `main`. El guard de Cloud exige completar después un PR `development -> master`. Para no permitir que una allowlist candidata se autorice a sí misma, abrimos además el bootstrap mínimo meta-hubs `#2` con la política Gitleaks base-owned. | Hubs `#3` y Cloud `#1` están `CLEAN` con checks verdes. Meta-hubs `#1` queda bloqueado únicamente por el bootstrap; meta-hubs `#2` está `CLEAN`, verde y pendiente de revisión/merge. No hubo merge a ramas base, build de imágenes candidatas, deploy ni aceptación live. |
| 2026-07-17 | Con autorización del propietario fusionamos el bootstrap meta-hubs `#2` en `main` como `f79175d` e incorporamos ese `main` sin reescribir historia en la rama final como `9906cc7`. | Gitleaks pasa en GitHub con política `trusted-base` y sin fugas. Los runs raíz `29599274001` y `29599277151` fallan después únicamente por ShellCheck `SC2119` en `deployment/capture-instance-state.sh:26`; no hubo build de imágenes candidatas, deploy ni mutación live. |
| 2026-07-18 | Corregimos SC2119/SC2015 y las diferencias de portabilidad `rg`/`grep` y GNU/BSD `stat`; el gate raíz quedó verde en `9d2679d` con 36/36 regresiones de seguridad y 142/142 de recuperación. Después fusionamos Hubs `#3` en `master` como `6f1f5315696c`, Cloud `#1` en `development` como `c5d39e0c930` y Cloud `#2` en `master` como `2164851185da`. | El CI post-merge de Hubs y Cloud terminó verde, incluidos Security, Storybook, Services, Spoke y Reticulum PostgreSQL 12/14. No hubo build de imágenes de rollout, checkpoint nuevo, rotación, deploy ni mutación live; bloquean `AUD-065` y `AUD-075`--`AUD-077`, mientras `AUD-078` permanece residual. |
| 2026-07-18 | Implementamos `AUD-077`: Reticulum persiste candidato/aprobado exactos, fingerprint y atribución, migra configuraciones heredadas a cuarentena deshabilitando solo `bots.enabled`, y falla cerrado en runtime/chat/registro. Admin añade inventario redactado y aprobación/cuarentena individual. Hubs `#4` se fusionó en `master` como `674ece411691`; Cloud `#3` pasó por `development` y `#4` se promovió a `master` como `cc70e4023622`. | Localmente pasaron Hubs 97/97, lint y build; Reticulum 411 tests + 5 properties y el verificador de migración real. GitHub pasó seguridad, Storybook, PostgreSQL 12/14 y release build. No hubo imagen ni deploy: falta revisar el inventario tras migración y siguen bloqueando `AUD-065`, `AUD-075`, `AUD-076` y el residual `AUD-078`. |
| 2026-07-18 | El gate raíz desde clone limpio descubrió dos supuestos no reproducibles: faltaban las dependencias Mix antes del formatter y una prueba de estado de entidad elegía el PDF por posición sin esperar los ACK. El gate ejecuta ahora `mix deps.get`; Cloud `#5/#6` espera los ACK, exige todas las actualizaciones y localiza el PDF por `nid`, promovido a `master` como `34d1d3a8d3cc`. | La semilla original y cinco suites completas adicionales pasaron: 411 pruebas + 5 properties por ejecución, 0 fallos. Formato, Gitleaks, Security, release y PostgreSQL 12/14 quedaron verdes. No cambió código de producción ni hubo imagen, deploy o mutación live. |
| 2026-07-18 | Repetimos `verify-project.sh --full` sobre los heads exactos Hubs `674ece411691` y Cloud `34d1d3a8d3cc`. | Pasaron 36 pruebas de seguridad, 142 de recuperación, Hubs 97, navegador 11, capacidad 115, bot orchestrator 102, Dialog 2, Photomnemonic 7, Spoke 68 y Reticulum 411 + 5 properties, además de builds, audits, Actionlint, ShellCheck y Gitleaks. La capacidad física permanece fail-closed y no hubo imagen, cluster ni producción. |
| 2026-07-18 | Implementamos `AUD-076`: una fila/tombstone PostgreSQL por sala con lease UUID, holder/sesión exactos, epoch global no cíclico, reloj DB, locks advisory+fila y CAS exacto cerca adquisición, renovación, takeover, revoke y todos los side effects autoritativos. Cloud `#7` se fusionó en `development` como `04278c69646b` y `#8` se promovió a `master` como `0f151eb88da1`. | CI Cloud pasó PostgreSQL 12/14, Services, Spoke, Security y release. El gate raíz normal y `--full` pasó sobre Hubs `674ece411691`/Cloud `0f151eb88da1`: seguridad 36, recuperación 142, Hubs 97, navegador 11, capacidad 115 fail-closed, orquestador 103, Dialog 2, Photomnemonic 7, Spoke 68 y Reticulum 418 + 5 properties. No hubo imagen ni deploy: el baseline live sigue process-local hasta migrar/atestar y Reticulum permanece singleton por los gates separados de storage/readiness. |
| 2026-07-18 | Implementamos en fuente Cloud `0110e9aad78d` el aislamiento `AUD-075`: parent separado y un Pod `bot-runner` por sala/generación, dos imágenes, token v1 sala/generación/holder/expiry seguido del lease UUID+epoch PostgreSQL obligatorio, reconciliación por nombre/UID, RBAC/ServiceAccounts mínimos, pull Secret kubelet-only, `/transport-ready` y siete NetworkPolicies. El root añade verificación live de Pods/digests/owner y checkpoint `deployment-images.json` schema 3 con `bot_runner_runtime`, manteniendo cero Pods dinámicos durante quiesce/restore. | Pasan localmente orquestador 125/125, generador 28/28, verificador de Pods 17/17, pull/checksum 9/9 y seguridad raíz 39/39. La documentación fija rollout en dos manifiestos generados (Reticulum compatible primero, luego parent/runner/control-plane), rollback inverso y `process-local` solo privado. No se afirma CI de promoción, build Actions, digest, checkpoint real, deploy, atestación live ni capacidad física. |
| 2026-07-18 | Corregimos el gate raíz de checkpoint para cerrar la dependencia circular previa al primer rollout aislado: clasifica antes de mutar el baseline `process-local` exacto o el control plane `kubernetes-pod` `active`, liga ese modo al inventario/fingerprint y lo revalida antes de reanudar. Estados parciales, namespace runner residual, deriva durante el backup y epochs unilaterales fallan cerrados. Actualizamos los runbooks al Cloud final `5392495`, 58 recursos, ocho NetworkPolicies y rollout serializado `bootstrap -> admission -> active`. | Pasan 223/223 regresiones de recuperación y los focos `checkpoint-process-local`, `checkpoint-gates` y `checkpoint-adjacent`. No hubo acceso al manifiesto privado, build de imagen, checkpoint real, rotación, despliegue ni mutación del clúster; el PR raíz y `AUD-065` siguen pendientes. |
| 2026-07-18 | Endurecimos checkpoint/restore para ligar snapshots privados e inmutables de values/manifiesto antes del downtime, reconstruir el monitor si falla la quiescencia antes del watcher y reservar la recuperación autoritativa al shell principal. Los subshells solo propagan estado: no reanudan writers, duplican fencing ni liberan locks. La revisión final confirmó que los 38 ficheros y el gitlink Cloud `5392495` pertenecen a `AUD-075`, recuperación, gates, documentación o update-friendly; el wrapper `npm run apply` aplica el mismo manifiesto generado bajo Lease y fases fail-closed. | `verify-project.sh` y `--full` pasan con seguridad 43/43, recuperación 239/239, Pods 45/45, pull 19/19, Deployment 18/18, Hubs 97/97 y build, navegador 11/11, capacidad 115/115 fail-closed, orquestador 128/128, Dialog 2/2, Photomnemonic 7/7, Spoke 68/68 y build, generador 30/30 con 58 recursos y Reticulum 430 + 5. No se abrió el manifiesto privado ni hubo build de imágenes de rollout, checkpoint real, rotación, despliegue o mutación live; PR raíz y `AUD-065` siguen pendientes. |
| 2026-07-18 | El CI raíz del PR `#5` detectó dos diferencias de checkout limpio. ShellCheck Linux señaló `SC2317` en el callback de heartbeat invocado indirectamente por `trap`; la supresión existente quedó acotada a `SC2317,SC2329`. Después, las regresiones demostraron que faltaba instalar las dependencias lockeadas de Community Edition antes de importar sus contratos YAML: workflow y `verify-project.sh` ejecutan ahora `npm ci --ignore-scripts --no-audit` tanto en Community Edition como en bot-orchestrator. | La revisión independiente descartó duplicar el parser, `NODE_PATH` y dependencias `--no-save`. El gate raíz normal vuelve a pasar con seguridad 43/43, recuperación 239/239, Gitleaks y auditoría upstream. No cambió lógica runtime ni hubo acceso a secretos, checkpoint, build, despliegue o mutación live; se exige un nuevo CI verde antes del merge. |
| 2026-07-18 | Los dos runs Linux siguientes del PR raíz pasaron la instalación hermética y 43/43 gates de seguridad, pero revelaron una carrera de diagnóstico en dos fixtures de deriva `process-local`: el watcher podía fallar antes que el gate exacto de modo. Se añadió una revalidación de solo lectura antes de reconstruir/consultar namespaces y otra antes de la rama de reanudación del parent, conservando el gate posterior contra TOCTOU. | El foco afectado pasa 61/61, ShellCheck queda limpio y la suite completa vuelve a 239/239. Los escenarios conservan el parent a cero y el lock retenido. La preparación solo lectura de `AUD-065` detectó además que falta tooling trackeado para rotar el rol PostgreSQL, comparar `PERMS_KEY` por huella y hacer diff redactado del baseline `process-local`; el plan activo incorpora esa subfase antes del checkpoint. No hubo acceso a valores, clúster ni producción. |
| 2026-07-18 | Los runs raíz exactos `29645630814` y `29645631723` pasaron sobre `ee47dd3`; se fusionó el PR `#5` como `main=9f4ada1`, fijando Hubs `674ece411691` y Cloud `5392495b0772`. El worktree canónico limpio se trasladó a `codex/aud065-process-local-rotation` desde ese merge y el plan activo actualizó su ruta/prompt. | Fase 1 queda cerrada e integrada sin mutación live. Comienza Fase 2A para crear la ruta trackeada de rotación `process-local` antes de consumir el checkpoint y su TTL; el fichero privado se comprobó solo por existencia, tipo regular y modo `0600`, sin abrirlo. |
| 2026-07-18 | Durante la auditoría local de Fase 2A, una búsqueda de un subagente por nombres de recursos alcanzó accidentalmente el `hcce.yaml` ignorado, contra la regla del repositorio. La salida se limitó a números de línea y campos estructurales estáticos (`pgsql`, `readOnlyRootFilesystem`, `/tmp`, `emptyDir`); no mostró valores ni secretos, no se abrió ni se difundió el manifiesto y esa salida no se usó como evidencia. | No hubo acceso Kubernetes, mutación live ni ampliación del material potencialmente expuesto. Las búsquedas restantes quedaron limitadas a rutas trackeadas explícitas; AUD-065 conserva el alcance preventivo ya definido. |
| 2026-07-18 | Cerramos localmente el tooling aislado de Fase 2A/AUD-065 para rotar el baseline `process-local`: operación HMAC, Lease y lock global, barrera PostgreSQL CAS con probe, cambio durable de `DB_PASS`, bundle exacto Secret+seis Deployments, paridad `PERMS_KEY`, promoción one-way, auditoría redactada, rollback y reentrada tras cortes. La revisión final corrigió TTL de checkpoint, preflight Python, rematerialización parcial y falsos TOCTOU por `nlink` de directorios. | El agregador de 14 suites pasa con lock 27/27, barrera 56/56, DB 54/54 más PostgreSQL real, coordinador 153/153 y nueve suites Node 30/34/20/20/18/13/6/6/28; además pasan seguridad 47/47, recuperación 243/243, Bash/ShellCheck, Node/Python, Actionlint, diff-check y Gitleaks 6,75 MB sin filtraciones. Revisiones independientes: cero P1/P2 pendientes. Durante esta validación final no hubo nuevo acceso al clúster ni nueva lectura de values/manifiesto privado, checkpoint nuevo, rotación live o cambio de producción; PR/CI/merge siguen pendientes. |
| 2026-07-18 | Los primeros runs del PR raíz `#6`, `29662313458` y `29662323279`, pasaron PostgreSQL 12.19/14.23 pero fallaron antes de las suites en ShellCheck: una versión Linux interpretó una función real cargada por `source` como referencia adelantada por existir un mock posterior (`SC2218`) y marcó tres callbacks de fixture indirectos (`SC2317`). | Se añadieron supresiones locales y justificadas solo a esas cuatro líneas, sin cambiar comportamiento. El bucle exacto `shellcheck -x` del workflow sobre todos los shells, Bash syntax y `git diff --check` pasan localmente; se exige un nuevo CI verde sobre el commit corregido antes del merge. |
| 2026-07-18 | Los runs corregidos `29662453239` y `29662454505` confirmaron PostgreSQL 12.19/14.23 verde y que las cuatro advertencias anteriores estaban cerradas; ShellCheck 0.9 avanzó al siguiente fichero y detectó un helper de fixture con dos argumentos opcionales nunca usados (`SC2120`/`SC2119`). | Se sustituyeron esos parámetros muertos por las constantes de fixture equivalentes. La barrera pasa 56/56 y se ejecutó ShellCheck 0.9.0, igual que Ubuntu, con `-x` sobre todos los shells: cero diagnósticos; también pasan la versión local actual, Bash syntax y diff-check. Sigue prohibido fusionar hasta un nuevo CI completamente verde. |
| 2026-07-18 | Los runs `29662625549` y `29662626428` pasaron PostgreSQL 12.19/14.23, ShellCheck 0.9, seguridad 47/47 y recuperación 243/243; al alcanzar el agregador AUD-065, el runner Ubuntu reveló que el fixture del coordinador dependía de `rg`, herramienta no instalada, y terminó con código 127. | Se reemplazaron todas las aserciones `rg` de ese fixture por `grep`, disponible de serie tanto en macOS como en Ubuntu, sin añadir una instalación oculta al workflow. El foco del coordinador pasa 153/153 y quedan verdes Bash syntax, ShellCheck 0.9.0 y 0.11.0 y diff-check. La corrección cambia de forma material el input del siguiente CI; sigue prohibido fusionar hasta que ese CI complete todos los gates. |
| 2026-07-19 | Los runs `29663204419` y `29663205680` avanzaron por PostgreSQL 12.19/14.23, Gitleaks, Actionlint, ShellCheck, seguridad 47/47 y recuperación 243/243, y ejecutaron el coordinador completo; su único fallo fue la aserción 86 del fixture. El fallback inline `stat -f ... || stat -c ...` acumulaba salida parcial de GNU `stat` antes de la variante correcta y comparaba un resultado multilínea. | La aserción reutiliza ahora `aud065_link_count`, el helper portable del propio driver que captura cada variante por separado, en vez de duplicar detección de plataforma. No cambia código runtime ni la reconciliación privada; se exige foco local y un CI nuevo completamente verde antes del merge. |
| 2026-07-19 | Los runs `29663668400` y `29663669349` terminaron completamente verdes sobre `2a9ab1c`, incluidos PostgreSQL 12.19/14.23 y el agregado. Antes del merge, una auditoría de la revocación GHCR detectó un P1: `Secret/ghcr-pull` y `ServiceAccount/default` no estaban ligados, por lo que el auditor podía aceptar el PAT antiguo y un reinicio futuro fallaría tras revocarlo. | PR `#6` permanece abierto. La Fase 2A se amplía sin tocar producción: conservar 42 recursos históricos y 13 digests, capturar los dos bindings legacy, reemplazar `configs` y `ghcr-pull` más seis Deployments por CAS y verificar `default` sin mutarlo. La ruta histórica `kubectl apply`/`patch` queda retirada; se repetirán fixtures, gates, revisión y CI sobre el input materialmente nuevo. |
| 2026-07-19 | Cerramos localmente el P1 GHCR previo al merge: la operación captura 44 recursos, aplica por UID/resourceVersion CAS dos Secrets más seis Deployments y conserva `ServiceAccount/default` bind-only. El gate online autentica OLD -> NEW contra todos los digests GHCR aplicables más runner antes del operation lock/CAS; NEW se revalida antes del primer pool y en auditoría. Las fixtures separan denegación OLD/NEW, dispatcher forward, rama `bundle-applied`, resume, rollback y audit exacto. | El agregador final de 15 suites termina con `exit 0`: coordinador 163/163, perfil 31/31, operación 34/34, source 20/20, prepare 21/21, materialización 18/18, publicación 13/13, proyección 6/6, GHCR 44/44, captura 9/9 y auditoría 31/31, además de PostgreSQL real. Dos revisiones independientes no encuentran falsos verdes P1/P2. No hubo lectura de valores privados, mutación live, checkpoint ni merge; siguen pendientes gates raíz y nuevo CI del PR `#6`. |
| 2026-07-19 | Ejecutamos los gates raíz finales sobre la corrección GHCR completa. | `verify-project.sh` y `verify-project.sh --full` terminan con código 0: seguridad 47/47, recuperación 243/243, agregado AUD-065, Actionlint, ShellCheck, Gitleaks root/Hubs/Cloud, Hubs 97/97 y build, Admin, navegador 11/11, capacidad 115/115 fail-closed, servicios CE, Spoke 68/68 y build, y Reticulum 430 tests + 5 properties. Los submódulos permanecen limpios y no hubo acceso a valores privados, checkpoint, clúster ni producción. Falta publicar el SHA, obtener CI verde y fusionar PR `#6`. |
| 2026-07-19 | Publicamos `4ac252b875e8`; los seis checks de los runs push/PR `29667729457` y `29667730561` terminaron verdes y el PR raíz `#6` se fusionó como `main=c87f5b3982f7`. El `main` sincronizado quedó limpio con Hubs `674ece411691` y Cloud `5392495b0772`. | Fase 2A queda integrada sin crear credenciales, checkpoint ni mutación live. La auditoría post-merge corrigió el handoff: `plan`, después del checkpoint, crea los únicos snapshots OLD/NEW y valida GHCR OLD -> NEW; no existe snapshot de credenciales para `rollback`, que es recuperación one-way al estado nuevo; la revocación externa solo empieza tras `aud065_rotation_verified`. |
| 2026-07-19 | El handoff documental pasó los seis checks de los runs `29668519915`/`29668525862` y el PR raíz `#7` se fusionó como `main=83732fe6a437`. Sobre ese `main` limpio, con Hubs `674ece411691` y Cloud `5392495b0772`, repetimos una sola vez `verify-project.sh` y `verify-project.sh --full`; ambos terminaron con código 0. | Fase 2B puede preparar credenciales, pero aún no se creó ninguna, no existe checkpoint nuevo y producción permanece intacta. La preparación reveló que faltaba una ruta trackeada para construir NEW sin editar secretos a mano. |
| 2026-07-19 | Implementamos el preparador fail-closed de NEW y su puente macOS Keychain: frame binario limitado solo por FD 3, etiquetas NEW separadas, secretos internos/RSA generados localmente, URI/JWT/GHCR derivados, preservación estructural, relectura de OLD y publicación `0600` sin clobber. El agregador de 17 suites termina con código 0: lock 27, barrera 56, DB 54 más PostgreSQL real, coordinador 163, perfil 33, operación 34, source 21, NEW 19, Keychain 9, prepare 21, materialización 18, publicación 13, proyección 8, GHCR 44, captura 9 y auditoría 31. | Tres revisiones no dejaron P1/P2. No se leyó Keychain ni una fuente real de values, ni hubo credencial/checkpoint/clúster. Una búsqueda diagnóstica con un wildcard alcanzó por error el `hcce.yaml` ignorado de Cloud y mostró el JWT público derivado; no se reutilizó y `PERMS_KEY`/JWT siguen obligatoriamente dentro de AUD-065. El siguiente límite es pasar los gates raíz/estáticos, PR/CI/merge y solo entonces capturar credenciales externas por Keychain. |
| 2026-07-19 | Una revisión amplia posterior detectó un P2 TOCTOU: prepare releía OLD antes de publicar NEW y verify capturaba el par una sola vez, por lo que una escritura concurrente posterior podía producir un token verde obsoleto. Ambos caminos capturan ahora `OLD -> NEW -> OLD`; los tests mutan OLD después de publicar y después de leer NEW, y la exclusión de escritores comienza antes de preparar NEW. Otra revisión rechazó una tubería de shell que podía escribir un secreto Actions vacío y creó en su lugar un supervisor que completa Keychain, prelista ambos repositorios, pasa el Buffer solo por stdin y exige un `updatedAt` nuevo. Una revisión concurrente añadió lock global por usuario y el caso parcial hubs-éxito/hubs-cloud-fallo seguido de reintento con el mismo token. | NEW pasa 21/21, Keychain/supervisor 27/27, source 21/21 y proyección 8/8; sintaxis Node y `git diff --check` pasan. El gate raíz en curso se interrumpió deliberadamente con código 130 al quedar obsoleto su input; agregado y gates raíz sobre el árbol corregido siguen pendientes. El plan sitúa ambos `REGISTRY_PASSWORD` tras `aud065_rotation_verified` y antes de revocar GHCR OLD. No hubo credenciales reales, checkpoint, clúster ni mutación de producción. |
| 2026-07-19 | Cerramos la validación local del preparador NEW/AUD-065 sobre el árbol final. El gate normal terminó con código 0. En el primer full, Reticulum agotó una sola vez el timeout de prueba de 100 ms al esperar el ACK de spawn; el foco pasó 100/100, la suite completa 430/430 y una repetición canónica de `verify-project.sh --full` terminó con código 0. Un falso positivo de Gitleaks en el nombre de una propiedad de fixture se corrigió mediante un renombrado, sin cambio funcional. | Quedan verdes `verify-project.sh` y `verify-project.sh --full`, incluido Reticulum 430/430, junto con el agregado AUD-065 y Gitleaks root/Hubs/Cloud. No se crearon credenciales reales ni un checkpoint nuevo, y no hubo acceso al clúster, despliegue ni mutación productiva. La entrega aún debe pasar PR/CI/merge antes de capturar credenciales externas. |
| 2026-07-19 | Publicamos `a6ed7b3fe3f9`; los seis checks de los runs push/PR `29675286715` y `29675308171` terminaron verdes, incluidos PostgreSQL 12.19/14.23 y ambos `static-security`. El PR raíz `#8` se fusionó como `main=623d70c607f2`, y el `main` local sincronizado quedó limpio con los gitlinks Hubs `674ece411691` y Cloud `5392495b0772`. | El preparador NEW ya pertenece a la base oficial. Fase 2B avanza a crear y custodiar por canales privados las credenciales nuevas; todavía no se creó ninguna, no existe checkpoint nuevo y no hubo acceso al clúster ni mutación productiva. |
| 2026-07-19 | Antes de crear credenciales, la consulta auditada `parse-local-values.mjs --keys` falló cerrada en la línea 1 de OLD. Dos comprobaciones booleanas sin contenido descartaron BOM y confirmaron un marcador YAML `---`, válido para la fuente canónica pero no admitido por el subset escalar. Se detuvo la fase y se añadió una compatibilidad mínima: solo un marcador exacto en la primera línea, preservado byte a byte y ligado con su LF/CRLF entre OLD y NEW; duplicados, marcadores internos, `...` y variantes siguen rechazados. | No se mostró ninguna línea o valor privado, no se leyó Keychain, no se creó credencial ni NEW/checkpoint y no hubo clúster o producción. Deben pasar focos, gates raíz, revisión, PR/CI/merge antes de volver a la creación privada de credenciales. |
| 2026-07-19 | Tras admitir `---`, la prueba redactada de OLD detectó como única sintaxis no escalar `PERMS_KEY: |`. Un inventario sin contenido confirmó 28 líneas, dos espacios, LF uniforme y RSA válido de al menos 2048 bits. Se añadió una única gramática compartida que solo acepta ese legado, lo canonicaliza a `\n` y hace que el preparador sustituya cabecera y cuerpo completos por una línea quoted, conservando OLD. La transición compara la SPKI pública para que la misma clave reserializada no pueda fingir rotación. | Pasan seguridad 50/50, proyección 8/8, preparador 23/23, Keychain 27/27 y focos de transición. El parser real termina con código 0 y muestra solo `SKETCHFAB_API_KEY=missing` y `TENOR_API_KEY=missing`; no se mostró ningún valor, no se leyó Keychain, no se creó credencial/NEW/checkpoint y no hubo clúster ni producción. Faltan agregado, gates raíz, revisión y PR/CI/merge. |
| 2026-07-19 | La compatibilidad OLD pasa transición 24/24 y el agregado AUD-065 completo con código 0; tres revisiones independientes dejan 0 P1/P2. La primera ejecución del gate raíz normal completó seguridad, recuperación y todo AUD-065, pero el Gitleaks final marcó dos cabeceras PEM sintéticas repetidas del nuevo fixture. Se sustituyeron exclusivamente por sentinels no-PEM y la misma matriz del parser 50/50, Gitleaks raíz y `git diff --check` pasan. | No eran secretos reales ni se añadió allowlist. El árbol queda congelado para ejecutar de nuevo los gates raíz normal y full canónicos antes de PR/CI/merge; credenciales, NEW, checkpoint, Keychain real, clúster y producción siguen intactos. |
| 2026-07-19 | Tras sustituir únicamente las cabeceras PEM sintéticas del fixture por sentinels no-PEM, sin allowlist ni cambio de cobertura, ejecutamos de nuevo los gates canónicos sobre el árbol congelado. `./scripts/verify-project.sh` y `./scripts/verify-project.sh --full` terminaron con código 0; seguridad se mantuvo 50/50 y el agregado AUD-065 completo volvió a pasar. | La compatibilidad OLD queda validada localmente. La primera casilla obligatoria es ahora commit, PR/CI y merge de esta compatibilidad antes de crear credenciales o NEW. No se leyó Keychain real, no se creó credencial/checkpoint y no hubo acceso al clúster ni mutación de producción. |
| 2026-07-19 | Una revisión tardía posterior a los primeros gates verdes detectó un P2 en la migración one-way de `PERMS_KEY`: el parser debía admitir el bloque legado para leer OLD, pero la transición podía sellar o promover un NEW que aún conservara `PERMS_KEY: |`. Los verdes anteriores quedaron obsoletos al cambiar sus inputs. La corrección inspecciona las líneas físicas de `newBytes`, falla con `new_source_perms_key_not_canonical` ante NEW literal y conserva OLD literal -> NEW scalar. | La re-revisión no encontró P1/P2. Post-fix, transición 24/24, preparador 23/23, seguridad 50/50, el agregado AUD-065, `verify-project.sh` y `verify-project.sh --full` terminaron con código 0; el full cerró Reticulum con 430 pruebas, 5 propiedades y 0 fallos. PR/CI/merge siguen pendientes antes de credenciales; no se leyó Keychain real, no se creó credencial/NEW/checkpoint y no hubo clúster ni mutación de producción. |
| 2026-07-19 | Los primeros runs push/PR del PR raíz `#9` pasaron los cuatro jobs AUD-065 PostgreSQL 12.19/14.23, pero ambos `static-security` fallaron al escanear el rango Git: Gitleaks clasificó como `private-key` dos cabeceras PEM literales usadas dentro de aserciones, aunque el fixture genera claves efímeras y el worktree estaba limpio. Se reemplazaron esas literales por una cabecera construida desde dos fragmentos y las mismas comprobaciones `startsWith`/`includes`, sin allowlist ni reducción de cobertura. | El preparador vuelve a pasar 23/23, `git diff --check` y Gitleaks raíz terminan con código 0. El commit correctivo y su nuevo CI siguen pendientes; no se leyó Keychain real, no se creó credencial/NEW/checkpoint y no hubo acceso al clúster ni mutación de producción. |
| 2026-07-19 | Para retirar también las literales del historial escaneado, la rama corta del PR `#9` se condensó en un único commit limpio `83db789` y se publicó con `--force-with-lease`; el rango local exacto `origin/main..HEAD` escaneó un commit sin hallazgos. El evento push conservó como `before` el commit reemplazado no ancestro, redujo el rango a un SHA suelto y Gitleaks recorrió 135 commits históricos, encontrando nueve hallazgos previos ajenos al árbol candidato. | No es un fallo del candidato ni autoriza una allowlist. Este commit documental fast-forward restablece una relación ancestral normal para que el siguiente push escanee únicamente el delta nuevo; el CI PR usa en paralelo el rango base correcto. Código, Keychain, credenciales, NEW, checkpoint, clúster y producción no cambian. |
| 2026-07-19 | El head final `5ee444fd2357` del PR raíz `#9` pasó los seis checks de los runs push/PR `29681808358`/`29681809883`: cuatro jobs AUD-065 PostgreSQL 12.19/14.23 y ambos `static-security`, incluidos Gitleaks de worktree/rango, Actionlint, ShellCheck, seguridad y recuperación. El PR se fusionó como `main=50b504a15a4ada8658cf4ce1a3b827d4fab8fc31`; el `main` local quedó limpio con Hubs `674ece411691` y Cloud `5392495b0772`. | La compatibilidad OLD ya pertenece a la base oficial y la primera casilla de Fase 2B pasa a crear/custodiar por canales privados las credenciales externas nuevas. No se leyó Keychain real, no se creó credencial/NEW/checkpoint y no hubo acceso al clúster ni mutación de producción. |
| 2026-07-19 | En `main=f14f1f40869d`, sin leer contenido, verificamos que el OLD canónico es un fichero regular owner-owned, single-link y `0600`, con padre owner-owned no escribible por grupo/otros; creamos vacío y `0700` el directorio privado `/Users/yengalvez/.yenhubs-private/aud065-20260719-01`. Una comprobación componente a componente descartó symlinks en OLD y en los componentes existentes de NEW, cuyo target permanece ausente. Las tres etiquetas NEW seguían ausentes según una consulta de existencia sin `-w`; OpenAI/Mailtrap quedaron preparados sin enviar y GitHub ante passkey. | Se cierra únicamente la disponibilidad privada de OLD/directorio NEW. Credenciales externas e internas, NEW, checkpoint, clúster y producción siguen pendientes; OLD no se modificó y no se ejecutó ninguna acción destinada a invalidarlo ni a crear, leer, revocar o enviar credenciales. |
| 2026-07-19 | Custodiamos por canales privados las tres credenciales externas NEW sin mostrar valores: service account OpenAI de proyecto con Responses Write y Moderations Request, token Mailtrap limitado al dominio `meta-hubs.org` y PAT classic GitHub con caducidad, solo `read:packages`/`write:packages` y sin `repo`, `workflow` ni borrado de paquetes. La clave personal OpenAI creada por error durante el formulario fue revocada. | Las tres etiquetas revisionadas existen en macOS Keychain y todas las credenciales OLD continúan válidas. No existe todavía el fichero NEW, no se creó checkpoint, no se consultó el clúster y producción permanece intacta. |
| 2026-07-19 | Implementamos en `codex/aud065-sequencing-unblock` el desbloqueo de secuencia sin ejecutarlo contra datos reales: verificación conjunta de un recibo y cuatro bundles ligados al gitlink Cloud, snapshot privado de cinco artefactos antes de `kubectl`, `DOCKER_CONFIG` efímero, completador OLD bajo Lease/CAS, gestor bootstrap-only de candidata y rollback/reconciliación exactos incluso entre procesos. Revisiones independientes corrigieron sustituciones de paths, cortes post-rename, cleanup parcial, pérdida/ACK ambiguo del Lease y contradicciones de orden documental. Revisiones tardías detectaron que el completador ligaba por error el parent live al parent candidato del build sin deploy, llamaba live al digest OLD sin capturar aún el Deployment y no demostraba que ese parent heredase el pull config verificado. Se detuvieron los gates, se eliminó la igualdad imposible y las capturas ligan ahora UID, resourceVersion, contenedor, imagen e herencia exacta de `ServiceAccount/default` del parent realmente live, mientras OLD deriva solo el runner candidato. | Los verdes anteriores a los hallazgos quedaron obsoletos. Las suites focales deben repetirse sobre la corrección, seguidas de ambos gates raíz, PR/CI y merge. El tooling aún no pertenece a `main`; no se abrió ningún values/manifiesto privado ni hubo checkpoint, acceso Kubernetes o mutación de producción. |
| 2026-07-19 | Tras corregir los hallazgos tardíos de la fila anterior, repetimos las suites focales y los dos gates raíz canónicos sobre el árbol congelado de `codex/aud065-sequencing-unblock`. | `./scripts/verify-project.sh` y `./scripts/verify-project.sh --full` terminaron con código 0. Pasaron transición 38/38, completador OLD 33/33, gestor de candidata 14/14, procedencia 63/63, Docker privado 8/8, seguridad 50/50 y recuperación 243/243; el full cerró Hubs 97/97, capacidad 115/115, bot-orchestrator 128/128, Spoke 68/68 y Reticulum con 430 pruebas, 5 propiedades y 0 fallos. La entrega queda validada localmente y autorizada únicamente para commit, PR y CI; la casilla continúa pendiente hasta el merge. No se abrió ningún values/manifiesto privado ni hubo checkpoint, acceso Kubernetes o mutación de producción. |
| 2026-07-19 | El head `e144dafe6e77` del PR raíz `#12` pasó los seis checks de los runs push/PR `29699523880`/`29699535163`: cuatro jobs AUD-065 PostgreSQL 12.19/14.23 y ambos `static-security`, incluidos Gitleaks de worktree/rango, Actionlint, ShellCheck, seguridad y recuperación. El PR se fusionó como `main=4651596b452aa4227446f5046ab916a3c1810264`; el `main` local quedó limpio con Hubs `674ece411691` y Cloud `5392495b0772`. | El tooling de secuencia ya pertenece a la base oficial y Fase 2B queda cerrada sin ejecutar el completador ni materializar NEW. La primera casilla activa pasa a Fase 3 `AUD-078`. No se abrió ningún values/manifiesto privado ni hubo checkpoint, acceso Kubernetes o mutación de producción. |
| 2026-07-20 | Cerramos la integración Cloud de `AUD-078`: el candidato `a3b7396` pasó 79/79 pruebas de aplicación y revisión independiente; el PR Cloud `#13` se fusionó en `development=0a21634688445eeb2ad2935627ad1c2f7a233f72` y la promoción separada PR `#14` terminó con CI verde en `master=1cf95ca8719b40aa94adc8ffa987cce835316066`. | Cloud contiene ya la outbox transaccional, `runtime_revision`, claims recuperables y parada terminal ligada a ausencia nombre+UID y cero Pods. Hubs permanece en `674ece411691`; root `main=ed8c9d13fbb` todavía fija Cloud `5392495`, por lo que el gitlink nuevo no está integrado. No hubo build de imágenes, checkpoint, apply, despliegue ni mutación live. |
| 2026-07-20 | Implementamos en `codex/aud078-root-integration` la fuente Fase 3B de checkpoint/restore: generaciones `legacy-absent`/`durable-v2`, metadata schema 3, inventario schema 4, evidencia runner schema 2 ligada por checksum/HMAC, snapshot privado de inputs, fences permanentes, CAS del Deployment completo, restore por fases y supervisión de streams DB/PVC/backup con progreso atómico estrictamente nuevo, frescura máxima 10 s, poll productivo 1 s, Lease timeout 5 s y cancelación por PID+identidad. | La implementación fuente no cierra la fase. Siguen pendientes las regresiones sobre los bytes definitivos, los gates raíz normal/full, la revisión final sin P0/P1/P2, commit, PR/CI y merge del gitlink candidato `1cf95ca`. Producción, builds, credenciales live y checkpoints no se tocaron. |
| 2026-07-20 | Cerramos la regresión definitiva de recovery de Fase 3B tras corregir cuatro fixtures obsoletos sin cambiar fuente productiva: reaparición de runner después del binding legacy inicial, timeout posterior, Pod concurrente con token ajeno y frontera temporal del payload Role en el finalizer. | `env -u NODE_PATH /usr/bin/time -p bash tests/recovery/test-recovery-safety.sh`: `613/613`, exit `0`, `real 2588.76` s; dos revisiones independientes de esos cuatro ajustes: `0 P0 / 0 P1 / 0 P2`. Siguen pendientes gates raíz normal/full, revisión final, commit y PR/CI/merge. Producción no se tocó. |

## 2026-07-21 (Cierre causal de AUD-078)

Time reference: Europe/Madrid (CEST).

| Time | Action | Result |
|------|--------|--------|
| 2026-07-21 | Repetimos una sola vez la suite Cloud completa después del último cambio que convierte cualquier fallo de recogida del grupo de procesos en error fail-closed. | `npm run test:apply` pasa 106/106; `git diff --check`, la sintaxis Node de los cinco ficheros afectados y Gitleaks sobre 57,24 MB terminan con código 0. La revisión independiente, commit, PR/CI y promoción siguen pendientes; no hubo build, checkpoint ni mutación de producción. |
| 2026-07-21 | Una revisión independiente leyó el diff Cloud final y reprodujo las rutas normales y de error del transporte y los relevos. | No encontró P0, P1 ni P2 nuevos: EPERM/ESRCH bloquean cleanup, adopción y acción; el sucesor se recoge ante fallo y el descendiente que abandona deliberadamente la sesión queda fuera del límite confiable documentado. Commit, PR/CI y promoción siguen pendientes; no hubo mutación live. |
| 2026-07-21 | Publicamos el commit Cloud `7baee36e6e06` y abrimos el PR listo `#15` de `codex/aud078-watch-boundary` hacia `development`. | El guard de rama y ambos checks de seguridad están verdes; servicios/Spoke continúan en curso. La casilla sigue abierta hasta fusionar y promover por un PR separado a `master`; no hubo build de rollout ni mutación live. |
| 2026-07-21 | El PR Cloud `#15` terminó con todos los checks verdes y se fusionó en `development=1a370dd6e48d`; la promoción separada `#16` repitió guard, seguridad, servicios Node y Spoke y se fusionó en `master=4c0f7be4a479`. Los runs post-merge `29790381506`/`29790381527` de seguridad/servicios terminaron también verdes sobre ese SHA. | La corrección causal AUD-078 ya pertenece a ambas ramas base Cloud. La siguiente casilla es actualizar el gitlink y cerrar los callsites raíz; no hubo imagen, checkpoint, apply ni mutación live. |
| 2026-07-21 | Cambiamos el submódulo Cloud del worktree raíz a su rama `master` y avanzamos por fast-forward exacto a `4c0f7be4a479`. | El gitlink candidato local apunta ya al commit promovido, pero la casilla permanece abierta hasta que los callsites raíz, gates, PR/CI y merge root estén cerrados. Hubs sigue en `674ece411691`; producción permanece intacta. |
| 2026-07-21 | Probamos read-only 12 rondas `WatchList` en `hcce` y `hcce-bot-runners`: todas completaron, con máximos de 1,083 s y 0,939 s. El análisis causal confirmó que su bookmark inicial solo delimita el snapshot propio y no el historial entre dos RV opacos; los bookmarks ordinarios observados tardan 62–121 s y no tienen deadline garantizado. | No se aceptará una rotación rápida de watches como prueba causal. Fase 3B añade un fence de admission en el servidor: con las excursiones prohibidas, los checks menores de 10 s sirven para frescura/health. El smoke no imprimió payloads/RV, no dejó procesos y no mutó producción. |
| 2026-07-21 | La revisión de la primera propuesta de admission confirmó que un `paramRef` al ConfigMap del lock exigiría permiso de lectura por petición; el mismo objeto contiene la anotación privada de recuperación. | Se descartó antes de commit y sin ampliar RBAC ni tocar live. El diseño vigente usa una policy sin parámetros y un binding permanente que cambia por CAS entre selector imposible y los namespaces protegidos bajo el lock/Lease; la activación y desactivación se prueban con dry-runs server-side negativos y positivos. |
| 2026-07-21 | La revisión independiente del fence sin parámetros detectó que las rutas de bootstrap/refence activo podían volver dormido un binding activo antes de probar cinco consumers a cero. Se eliminó esa dormancia de la preparación y toda transición `active -> dormant` exige ahora consumers, parent, runtime runner y autoridad ya quiescentes. | `node --check apply/index.js` y el foco `runner-activation.test.js` pasan 24/24 sobre el cambio. La casilla Cloud sigue abierta hasta el E2E Kubernetes, CI, PR y promoción; no hubo build, checkpoint, apply ni mutación live. |
| 2026-07-21 | Cerramos el fence de operación Cloud. La revisión previa corrigió pin UID/RV antes y después de las sondas y exigió éxito también al probe runner. El primer Kind real descubrió que Kubernetes omite `request.subResource` en CREATE; el commit `111d59d9b744` añadió el acceso CEL opcional y repitió 30/30 generator, 110/110 apply, manifiesto de 68 recursos, Gitleaks y dos E2E Kubernetes 1.34.8 verdes. El PR `#17` fusionó `development=2813cc83fb27`; la promoción `#18` fusionó `master=24d09706c2d9`. | Los runs post-merge `29795271416` (seguridad) y `29795271411` (Kind, Node y Spoke) terminaron verdes, y el gitlink raíz candidato fija `24d09706c2d9`. Una revisión separada mantiene las casillas raíz abiertas por huecos causales en los dos monitores, ya en corrección. No hubo imagen de rollout, checkpoint, apply, despliegue ni mutación live. |
| 2026-07-21 | Avanzamos la fuente raíz de Fase 3B tras el fence Cloud: evidencia runner schema 3 con estado `dormant|active` y cinco pares policy/binding; monitores causales separados de writers y runner con autoridad checksummed, handshakes tokenizados y `operation_owner` exacto; capabilities padre completas en hijos DB/PVC; y callsites de checkpoint/restore con `FINAL` durable antes de writer. El checkpoint durable hace CAS `dormant -> active -> dormant`; restore deja PREPARE dormido, adopta el fence activo creado por Cloud `restore-fence` durante EXECUTE y exige que Cloud `active` vuelva a dormant antes de FINALIZE. | Es progreso de fuente, no cierre de Fase 3B. Los callsites/fixtures y revisiones siguen estabilizándose; después deben repetirse la suite recovery completa, ambos gates raíz, revisión final, commit, PR/CI y merge. Una interrupción de PVC permanece fail-closed para inspección manual, sin auto-delete ni afirmación de reentrada completa. Hubs sigue en `674ece411691`, root `main=ed8c9d13fbb` aún fija Cloud `5392495`, y no hubo build, checkpoint, apply, despliegue ni mutación live. |
| 2026-07-21 | Cerramos los callsites y fixtures de checkpoint sobre bytes congelados: el monitor storage durable conserva solo su guard local PVC/policy/pod/consumidores, mientras writer y runner durable siguen como capabilities padre independientes; legacy mantiene vigilancia continua de cero runners. Se corrigieron además el timeout Lease adaptativo de 1 s, el falso residuo durable del fixture legacy y la recolección completa de grupos ante HUP/INT/QUIT/TERM o muerte del padre. | Pasan `checkpoint-children` 147/147, `stream-guards` 74/74, monitor durable Node 57/57 y `checkpoint-durable` 60/60; Bash/Node syntax, ShellCheck y diff-check pasan, con cero procesos fixture o huérfanos al cierre. Las casillas amplias siguen abiertas hasta completar restore, congelar todos los bytes y repetir una sola vez la suite recovery completa y los gates raíz. No hubo cluster, build, checkpoint real ni mutación de producción. |
| 2026-07-21 | Congelamos los callsites finales de streams y directorios privados: `pg_dump` y restores DB/PVC conservan guards continuos local+padre; los backups standalone fallan antes de Kubernetes; staging/final, materialización, validadores y `WORK_DIR` usan una capacidad común `dev:ino`/`dirfd` con allowlist exacta, latches terminales y señales diferidas. Una revisión adicional movió el mensaje de éxito storage después del commit local. | Bash/Node syntax, ShellCheck, compilación del Python embebido, `git diff --check`, ausencia de `rm -rf` en los callsites y smokes happy/extra/marker/root-swap terminan con código 0. El límite POSIX same-UID queda documentado y siempre preserva reemplazo/orphan. Los fixtures finales siguen en curso; aún no se repitió la suite recovery completa, no hubo clúster, checkpoint real, build ni mutación de producción. |
| 2026-07-21 | Una auditoría causal del supervisor multi-guard demostró que esperar cada guard secuencialmente podía fechar un incremento sin observación causal y aceptar o rechazar erróneamente la ventana conjunta. La fuente usa ahora dos vueltas round-robin en foreground, progreso monotónico con cota temporal conservadora, identidad/autoridad exactas y ventana simultánea estricta antes de abrir el stream; las regresiones positivas y negativas esperan marcadores privados deterministas. | Sobre `recovery-safety.sh=93afad8ce84a` y `test-recovery-safety.sh=37e814b149c2`, el foco multi-guard final pasa 48/48 y la matriz durable DB/PVC pasa 72/72, exit `0`, `real 824.87` s, incluidas sustitución y revocación en vuelo de PID, progreso y autoridad con lock/fence/frontera cero preservados; no quedan procesos fixture. La revisión independiente de fuente no encontró P0–P3. La suite recovery completa y los gates raíz siguen pendientes; no hubo clúster, checkpoint real, build ni mutación de producción. |
| 2026-07-21 | La primera suite recovery completa posterior alcanzó 104 verdes y encontró dos fixtures legacy obsoletos: el segundo inventario de `runner-reappears*`, antes reservado al waiter del child, lo consumía ahora la nueva validación exacta del parent-writer y el test fallaba antes por capability. Se detuvo la ejecución ya inválida, sin seguir gastando el gate. El fixture conserva vacías la lectura de generación y la frontera padre y hace reaparecer el runner en el tercer inventario; los dos casos se centralizaron además en un foco reutilizable. | La fuente productiva conserva `recovery-safety.sh=93afad8ce84a`. Sobre el test corregido `a36447bc9e1e`, el foco legacy pasa 49/49, exit `0`, `real 32.33` s: alcanza los diagnósticos exactos residual/timeout y demuestra dos veces ausencia de `dropdb`. Bash syntax y `git diff --check` pasan; ShellCheck no añade diagnósticos en las líneas cambiadas, y una revisión independiente no encuentra P1–P3 ni modos hermanos afectados. La suite completa debe reiniciarse una sola vez sobre este hash; no hubo clúster, checkpoint real, build ni mutación de producción. |
| 2026-07-21 | La segunda suite recovery completa alcanzó 155 verdes y creó/validó/publicó el checkpoint durable usado por restore, pero falló al reanudar writers después de retirar el marker; la salida genérica no permitía localizar el subgate. El sistema mantuvo cinco writers a cero y el lock. El foco durable aislado confirmó además que los casos `fence-disappears`/`fence-replaced` ahora abortaban correctamente antes, durante el stream DB, por el monitor continuo; se sustituyó la expectativa tardía por un marker causal exacto y el foco de los tres drifts pasó 51/51. | El run completo no se acepta ni se continúa como verde. La evidencia descarta backup, validación, checksums, publicación y limpieza de staging; el fallo quedó acotado a `resume_writers`, sin mutación real. Antes de repetir se auditan diagnóstico y continuidad exacta del fence dormant. |
| 2026-07-21 | Cerramos los dos P2 de finalización del checkpoint: la identidad `dormant` resultante del CAS queda fijada por UID/RV durante cada resume y hasta el DELETE exacto del lock, y el error trap conserva status con un único `stage/code` allowlisted sin datos dinámicos. Dos fixtures modelan ABA `rv3 -> rv4 -> rv5` antes del primer resume y antes de liberar lock; otro fuerza status 73 al commit de `SHA256SUMS`. | El foco finalization inicial pasa 51/51, exit `0`, `real 579.11`; después de corregir un P3 exclusivamente diagnóstico, el único caso afectado vuelve a pasar 47/47, exit `0`, `real 184.49`. Dos revisiones independientes no encuentran P0–P2; Bash syntax, ShellCheck source y `git diff --check` pasan. Hashes actuales: `create-checkpoint=6ae5a7c4567a`, `recovery-safety=93afad8ce84a`, test `3f0868c452a7`. Siguen focos durable/writer, suite completa, gates y PR; producción permanece intacta. |
| 2026-07-21 | Cerramos el foco writer sobre los bytes finales tras corregir solo fixtures: seed `dormant` en cada checkpoint durable, diagnóstico final `writer-monitor`, autoridad re-firmada al sustituir el PID de la prueba 143 y marker causal post-READY. | El foco corto pasa 46/46 y `YENHUBS_RECOVERY_TEST_FOCUS=checkpoint-writers` pasa 170/170, exit `0`, `real 2001.66` s, sin procesos residuales. Tres revisiones independientes confirman la causa sin P0/P1; Bash syntax, ShellCheck y diff-check están verdes. Hashes: checkpoint `2cec747e1dd2`, recovery safety `93afad8ce84a`, durable watcher `54a25fbf17d4`, writer watcher `6acc75c82110`, fixture `b3ec10604aea`. El foco durable y la suite completa deben repetirse sobre este fixture final; producción permanece intacta. |
| 2026-07-21 | Repetimos el foco durable después del último cambio del fichero de fixtures, sin modificar fuente productiva. | `YENHUBS_RECOVERY_TEST_FOCUS=checkpoint-durable` pasa 60/60, exit `0`, `real 838.38` s: conserva el fence permanente, elimina runner/intent solo por UID/RV exactos, no borra Pods desconocidos y retiene lock y cinco writers a cero ante fence ausente, reemplazado o terminando. No quedan procesos. Se cierra la casilla de quiescencia; streams y cobertura esperan la suite completa. Producción permanece intacta. |
| 2026-07-21 | Cerramos la suite recovery integral sobre los bytes definitivos. Los intentos previos se detuvieron al primer fallo y descubrieron exclusivamente fixtures desfasadas: estado fail-closed de Role/fence, disparo causal del consumidor transitorio, restauración de `STUB_SQL_PLAIN`, baseline dormant de casos directos, handshake del monitor PostgreSQL y binding dormant del preflight. Cada ajuste se validó focalmente y mediante revisión independiente antes del run final. | `env -u NODE_PATH /usr/bin/time -p bash tests/recovery/test-recovery-safety.sh` pasa 861/861, exit `0`, `real 13325.23` s. Hashes finales: checkpoint `2cec747e1dd2`, recovery safety `93afad8ce84a`, durable watcher `54a25fbf17d4`, writer watcher `6acc75c82110`, restore coordinator `d1ab89e77635` y fixture `4b2eb4401cd9`. Bash syntax, ShellCheck y diff-check pasan, sin procesos residuales. Se cierran streams y cobertura de Fase 3B; quedan gates raíz y PR/CI/merge. No hubo clúster, build, checkpoint real ni mutación de producción. |
| 2026-07-21 | Ejecutamos el gate raíz normal y comenzamos el full sobre recovery 861/861. El normal terminó verde; el full repitió seguridad 51/51 y recovery 861/861, pero se detuvo en el primer audit Hubs por dos advisories nuevos de Immutable.js `<4.3.9`. Se rechazó silenciarlos y se aisló la única ruta de aplicación, el editor Tweet basado en Draft.js 0.11.7. | Hubs PR `#5` fija exactamente Immutable.js 4.3.9, aplica durante `npm ci` dos ajustes de compatibilidad con fuente/version estrictas y añade tres regresiones de bloques, randomización y texto+emoji. Audit de producción da 0 vulnerabilidades; pasan 100/100 pruebas, lint, TypeScript, build y los tres checks GitHub. El PR se fusionó como `master=ce8390a8905f`; ambos gates raíz deben repetirse por el nuevo gitlink. Producción permanece intacta. |
| 2026-07-21 | Repetimos el gate raíz normal sobre los gitlinks finales Hubs `ce8390a8905f` y Cloud `24d09706c2d9`. | `./scripts/verify-project.sh` termina con código 0, `real 13647.23` s: seguridad 51/51, recovery 861/861, todas las suites `AUD-065`, Gitleaks en root/Hubs/Cloud y auditoría upstream con 0 releases estables pendientes. Solo queda una ejecución `--full`, revisión y PR/CI raíz antes de la fase de build/checkpoints/rotación/rollout; producción sigue intacta. |
| 2026-07-21 | La primera repetición full alcanzó el happy path durable y falló en el caso 284 al revalidar los monitores padre después de storage; los casos 285, 287 y 288 fueron cascadas. Se detuvo la ejecución inválida. Tres diagnósticos independientes demostraron que el kubectl stub cerraba WATCH de dos segundos instantáneamente y, sin su pacing documentado, acumulaba cientos de rondas y podía hambrear el sweep; producción mantiene WATCH reales y no comparte ese comportamiento. | No hubo defecto determinista ni mutación insegura: DB/PVC validaron y lock/fence quedaron retenidos fail-closed. Se añadió `STUB_MONITOR_WATCH_PACE=1` únicamente a los dos fixtures EXECUTE durable, sin cambiar producto ni timeouts. Sobre esos bytes pasan `restore-finalize-positive` 54/54 (`real 639.24` s) y `restore-execute-cas` 55/55 (`real 956.62` s), sin procesos residuales. Reiniciar una sola vez el full; producción permanece intacta. |
| 2026-07-21 | La segunda repetición full confirmó el pacing al pasar los casos 284–290, pero el siguiente restore positivo consecutivo volvió a fallar durante la revalidación final de monitores; se detuvo con exit `1`, `real 4153.50` s. El diagnóstico descartó el budget productivo de frescura: el fixture durable exigía en 1 s una frontera inicial que abre dos procesos reales por namespace, frente a los 7 s ya usados por producción. | El límite de 7 s se aplica solo bajo la atestación `local-fixture`; producción queda byte a byte con el mismo deadline. Se conserva diagnóstico seguro solo ante fallo y se añadió un foco que ejecuta tres restores positivos en un único proceso. El test Node pasa 11/11 y el foco final pasa 61/61, exit `0`, `real 1334.59` s: cada vuelta prueba DB, storage, WATCH writer/durable con `watch=true`, replace CAS exacto del lock tras validar UID/RV/estado, lock RV2, fence activo RV2, cinco consumers a cero y helpers limpios. Revisión independiente queda limpia P0–P2; queda una única repetición full. No hubo clúster ni mutación de producción. |
| 2026-08-01 | Auditoría general de proceso y red-team Extra High, estrictamente read-only, confirmaron que no existe loop ni deriva remota: root `origin/main=ed8c9d13fbb`, Hubs `origin/master=ce8390a8905f`, Cloud `origin/master=24d09706c2d9`, subrepos limpios y ningún PR raíz abierto. La revisión encontró cuatro huecos P1 del plan: faltaban build/procedencia/despliegue del cliente Hubs, staging Reticulum-first/Hubs-second y preparación Spoke de sitting v2, aceptación explícita de personalizaciones del cliente, y repetir ambos gates después del futuro código de procedencia/consumidor de Fase 4. Detectó además ambigüedad de worktree, hashes intermedios rotulados como actuales, aprobación AUD-077 débil y una contradicción en `development-workflow.md`. | Se corrigieron únicamente plan y documentación: panel operativo único en `aud078`, camino crítico acotado, hashes vigentes, un solo full al reanudar, una sola auditoría final por diff, builds trazables de cuatro imágenes, staging y cuatro generaciones `bootstrap-server -> bootstrap-client -> admission -> active`, aprobación individual AUD-077, magic link/cámaras/español/avatares/Admin/Spoke en aceptación y prompt de meta estable. Las prohibiciones pasan a guardarraíles y no a tareas. No se reanudó el objetivo bloqueado, no se ejecutaron gates largos ni se accedió a values, clúster o producción. |
| 2026-08-01 | La reanudación `--full` pasó los casos 284–294 y falló después con `durable_runner_monitor_error:parent_pod_contract`, exit `1`, `real 4261.81` s. El diagnóstico y dos revisiones independientes localizaron una carrera exclusiva del stub: el Pod helper se representaba mediante varios ficheros y un LIST concurrente con DELETE podía reconstruir un objeto parcial. | El fixture publica ahora un snapshot JSON atómico, LIST mantiene abierto el snapshot completo y DELETE lo retira por UID exacto; el contexto kubectl queda además aislado preventivamente entre escenarios. Los focos `storage-helper-pod-snapshot-race` y `restore-context-isolation` pasan 46/46 cada uno; `restore-repeat-positive` pasa tres restores completos, 61/61, exit `0`, `real 1081.42` s. Bash syntax, ShellCheck y `git diff --check` pasan. No cambió producto ni se tocó el clúster; al cambiar el fixture corresponde una única revalidación `--full` post-corrección. |
| 2026-08-01 | El siguiente `--full` pasó los antiguos casos críticos 284–288, pero reveló diagnósticos repetidos del shell cuando DELETE retiraba el snapshot entre la comprobación de existencia y `open(2)`. Se interrumpió voluntariamente antes de gastar el resto del gate sobre bytes ya incompletos. | La apertura fallida esperada de LIST se trata ahora como lista vacía sin stderr, mientras GET conserva el fallo obligatorio. El foco determinista cubre DELETE antes y después de open y pasa 46/46, exit `0`, `real 8.49` s; Bash syntax, ShellCheck y diff-check pasan. La semántica y el retorno no cambiaron, por lo que no se repite 61/61; sí queda una revalidación `--full` sobre el fixture corregido. No hubo clúster ni mutación de producción. |
| 2026-08-01 | Ejecutamos una sola revalidación raíz `--full` post-snapshot. Terminó verdes todos los bloques anteriores —incluidos seguridad, recovery 861/861, AUD-065, auditoría upstream, Gitleaks, Hubs/Admin, navegador, capacidad fail-closed, orquestador, servicios CE, Spoke y compilación Reticulum— y falló exclusivamente en el `mix hex.audit` final por dos advisories publicados después del baseline. | El run terminó exit `1`, `real 12474.87` s. `EEF-CVE-2026-59248` afecta Cowlib 2.18.0 y se corrige en 2.19.0; `EEF-CVE-2026-65624` afecta Cowboy 2.15.0 y se corrige en 2.18.0. Se rechazó ampliar la allowlist o actualizar dependencias en masa; producción permaneció intacta. |
| 2026-08-01 | Aislamos la corrección Reticulum en Cloud `codex/reticulum-http-advisories`: commit `fc0b45f` cambia solo la restricción Cowboy y las entradas Cowboy/Cowlib del lock; el resolvedor intentó mover Ranch, pero se restauró exactamente a 2.2.0. | Formato, compilación estricta, `mix hex.audit`, dos verificadores de migración, PostgreSQL 14.23 con 461 tests + 5 properties y 0 fallos, release `turkey`, diff-check y Gitleaks de fuente trackeada pasan. El PR Cloud `#19` hacia `development` queda abierto para exigir CI PostgreSQL 12.19/14.23 antes de merge y promoción; no hubo build de imagen ni mutación live. |
| 2026-08-01 | El PR Cloud `#19` repitió verdes guard, seguridad, PostgreSQL 12.19, PostgreSQL 14.23 y release Turkey y se fusionó en `development=b2abe936`. La promoción separada `#20` comparó únicamente `mix.exs`/`mix.lock`, repitió los mismos gates y se fusionó en `master=c540c292`. | El submódulo Cloud local está limpio en ese `master`; quedan los runs post-merge `30709212953`/`30709212992`, actualizar el gitlink raíz y una única revalidación `--full` porque cambiaron los bytes Reticulum. No se construyó imagen ni se tocó producción. |
| 2026-08-01 | Comprobamos los runs post-merge exactos de Cloud `master=c540c292`: `security-ci=30709212953` y `reticulum-ci=30709212992`. | Ambos terminaron verdes; Reticulum volvió a pasar Hex audit, formato, compilación, migraciones, tests PostgreSQL 12.19/14.23 y release Turkey. El siguiente y único paso amplio es `verify-project.sh --full` con el gitlink nuevo; producción sigue intacta. |
| 2026-08-01 | Ejecutamos una única revalidación raíz `--full` sobre Hubs `ce8390a8905f` y Cloud `c540c292`. Todos los bloques previos avanzaron y recovery terminó con 860/861: el caso 850 informó `call-elapsed=4646`, `stream-elapsed=-1`, Lease 1/1/1, stream iniciado y terminado sin completar y descendiente ausente; exit `1`, `real 12190.65` s. | El gate no se presentó como verde y el mensaje temporal quedó pendiente de diagnóstico, sin asumir todavía defecto productivo ni inocuidad. Se acotó la siguiente acción a ese foco. Producción permaneció intacta. Se creó `docs/estado-sencillo.md` como panel humano obligatorio y se enlazó desde la meta activa para actualizarlo tras cada hito real. |
| 2026-08-01 | El diagnóstico focal corrigió la interpretación anterior del caso 850: `call-elapsed=4646` estaba dentro de 5 s; el único predicado fallido era `stream-elapsed=-1`. Timeouts Lease 1/1/1, stream iniciado y terminado sin completar y descendiente ausente demostraban revocación correcta. Una repetición focal reprodujo la inversión exacta de −1 ms. | Se modificó solo el fixture: los marcadores se publican por temporal privado y `mv`; el camino normal exige `0 <= stream-elapsed < 5000` y solo se admite `-1` si el cronómetro completo también es `<5000`; el log exige al menos dos Lease checks para demostrar uno lento post-launch. No cambió `recovery-safety.sh` ni sus reservas/timeouts. Cuatro focos consecutivos pasaron 81/81 antes del último endurecimiento y el foco final volvió a pasar 81/81; Bash, ShellCheck y diff-check verdes, revisión independiente sin P0/P1. Corresponde una única repetición `--full`; producción intacta. |
| 2026-08-02 | La única revalidación raíz `--full` post-fixture confirmó recovery `861/861`, incluido el caso 850, y dejó verdes los bloques anteriores, suites AUD-065, Gitleaks, Hubs/Admin, navegador, capacidad fail-closed, servicios CE y Spoke. | El gate terminó exit `1`, `real 12834.18` s, exclusivamente en el `mix hex.audit` final: Guardian `2.4.0` recibió cuatro advisories nuevos (`EEF-CVE-2026-54894`, `55733`, `55734` y `55735`; tres medios y uno alto). No se silencian ni se repiten las suites verdes. La siguiente acción se limita a verificar/aplicar la primera corrección oficial compatible y validar Reticulum; producción permaneció intacta. |
| 2026-08-02 | Hex publicó Guardian `2.4.1`, primera release que corrige los cuatro advisories. La rama Cloud `codex/guardian-2-4-1-advisories` cambió solo una entrada de `mix.lock`; ninguna otra dependencia se movió. | Pasan formato, compilación estricta, Hex audit, las dos migraciones, `461` tests + `5` properties, release `turkey`, diff-check y Gitleaks del árbol trackeado. PR `#21` se fusionó en `development=67e89a15` y promoción `#22` en `master=c0a3419b`. Un run PostgreSQL 12 agotó DNS al resolver `hubs.local`; el mismo job paralelo y su único rerun pasaron sin cambio de código. Los runs post-merge `security-ci=30725805066` y `reticulum-ci=30725805072` están verdes en PostgreSQL 12.19/14.23 y release. Producción permaneció intacta. |
| 2026-08-02 | Actualizamos el gitlink raíz a Cloud `c0a3419b` y cerramos la validación proporcional posterior al full: pin exacto de ambos submódulos, diff-check, auditoría upstream sin release estable pendiente y Gitleaks raíz. Recovery, Hubs, Spoke y capacidad no se repitieron porque sus bytes no cambiaron; Guardian ya quedó cubierto por la suite Reticulum y CI PostgreSQL 12/14. | La única revisión final del entregable staged no encontró P0/P1/P2 ni archivos accidentales. Corrigió dos estados documentales ya superados y actualizó la huella real del fixture `test-recovery-safety.sh=0ea88ed52e80`; los otros cinco hashes coincidían. El árbol queda listo para commit/PR/CI/merge raíz; producción permaneció intacta. |
| 2026-08-02 | Publicamos el commit raíz `1d45626` y abrimos el PR `meta-hubs#14`. Sus cuatro jobs AUD-065 PostgreSQL 12.19/14.23 pasaron; los dos jobs `static-security` fallaron únicamente por `SC2015` en dos señalizaciones de descarte de `recovery-safety.sh`. | Se sustituyó cada cadena `cmd && asignación || :` por su `if` explícito equivalente, sin cambiar comandos ni estados. Pasan Bash syntax, ShellCheck, diff-check, el foco de librería 46/46 y `checkpoint-writers` 170/170, exit `0`; hash candidato `recovery-safety.sh=c5a1f2df9a2c`. La corrección se publicará como segundo commit sin reescribir historia y el PR no se fusionará hasta CI completamente verde. Producción permaneció intacta. |
| 2026-08-02 | El segundo commit raíz `6601cb1` publicó la corrección `SC2015`. Los cuatro jobs AUD-065 volvieron a pasar y ambos `static-security` avanzaron hasta reproducir los mismos diagnósticos del ShellCheck Linux antiguo: `SC2317` en dos callbacks invocados por `trap` y `SC2119/SC2120` en una función que recibe overrides indirectamente a través de `expect_success`. | Se añadieron únicamente supresiones locales justificadas, siguiendo el patrón ya presente para callbacks; no cambió ninguna instrucción ejecutable. Bash syntax, ShellCheck 0.11 sobre el fichero completo y diff-check pasan; hash del fixture con comentarios `01c41bb2d461`. Debe confirmarlo un nuevo CI antes del merge; producción permaneció intacta. |
| 2026-08-02 | El tercer commit raíz `15f713d` permitió a ambos jobs `static-security` superar gitlinks, Gitleaks, workflows y ShellCheck. Los dos fallaron después en el mismo caso recovery 156 con `runner_pod_list_file_invalid`: en Linux sin `TMPDIR`, la raíz canónica era `/tmp` `01777`, mientras el lector estricto exige que el fichero `0600` tenga también un padre privado propio `0700`. | No se exportó un `TMPDIR` artificial ni se relajó el lector. `recovery_canonical_private_tmp_root` crea o reutiliza de forma fail-closed `.yenhubs-recovery-private-<uid>`, sin chmod ni sustitución de rutas existentes, y rechaza modo, dueño, tipo o symlink inseguros. Pasan Bash syntax, ShellCheck, diff-check, lector Node `32/32`, helper privado `78/78` y la reproducción exacta `env -u TMPDIR` durable `50/50`, exit `0`, `real 390.86`; hashes candidatos: fuente `cbcc5f67d1d1`, fixture `75a64c92d2f6`. El siguiente CI sustituye al full local y producción permanece intacta. |
| 2026-08-04 | Publicamos el ajuste de pacing del fixture como `9315efb` en el mismo PR raíz `#15` y actualizamos su descripción con la causa y la evidencia enfocada. | La concurrencia canceló el push duplicado `30890445598` y dejó el run PR `30890450972` como único gate integral autoritativo sobre el mismo SHA. No se relanzó CI, no se repitieron bloques verdes, no se inició Fase 4 y producción permanece intacta. |
| 2026-08-08 | Se comprobó el estado terminal de `30890450972`: finalizó el 4 de agosto con PostgreSQL 12/14 y estáticos verdes, pero recovery `859/864`; fallaron `402`, `478`, `764`, `766` y `768`. Los mismos cinco resultados persistieron en cuatro runs y los focos macOS no demostraron la secuencia Linux completa. | Se detiene el loop de publicar hipótesis. El plan adopta clean-room Ubuntu 24.04 local, reproducción causal, un full Linux local por candidato y un único CI remoto final por SHA, seguido directamente sin heartbeat de 17 horas. PR `#15` permanece draft/open, no se relanzó Actions y producción sigue intacta. |
| 2026-08-08 | Durante el diseño read-only de la clean-room, una búsqueda alcanzó `deployment/input-values.local.yaml` y mostró sus valores en una terminal interna. No se publicaron bytes ni hubo mutación live. | Por la regla fail-closed, todos los dominios de credenciales aplicables representados allí se consideran potencialmente expuestos. No se vuelve a abrir/imprimir el fichero; se usarán únicamente inventarios de presencia/huellas y la rotación coordinada obligatoria después del checkpoint, antes de cualquier rollout. |
| 2026-08-08 | Se creó la VM aislada `yenhubs-ci-arm64` con Ubuntu 24.04 ARM, sin mounts de Home/repositorios, agente SSH, proxy ni secretos; se clonaron por HTTPS solo los commits trackeados root `9315efb`, Hubs `ce8390a` y Cloud `c0a3419`. El foco Linux `checkpoint-process-local` terminó `88/89`, exit `1`, `real 157.44` s: el caso equivalente al 402 falla también aislado, por lo que el orden previo del full no es causa suficiente. | No se editó producto ni se ejecutó un full. A petición del propietario la VM quedó apagada y preservada, se congeló toda implementación y se preparó `docs/prompt-auditoria-general-2026-08-08.md` para una única auditoría adversarial que repare plan, panel humano y objetivo antes de reanudar. Producción, GitHub y recursos externos permanecieron intactos. |
| 2026-08-08 | Cerramos una auditoría general con cinco carriles read-only y red-team. Confirmó que el baseline live sigue operativo y la dirección funcional es válida, pero PR raíz `#15` repitió los mismos cinco fallos en cuatro runs y mezcló fixes demostrados con hipótesis de fixture refutadas. También detectó documentación GLB sobreafirmada, regresiones ausentes de cámara/idioma, inventario update-friendly obsoleto, duplicación normal/`--full`, staging DO inexistente y coste base actual aproximado de USD 65. | `docs/audit-general-2026-08-08.md` pasa a ser la auditoría vigente y se reconstruye, en la misma ruta, el plan único `docs/active-goal-plan-2026-07-18.md`, sincronizado con `docs/estado-sencillo.md`; el objetivo nuevo queda activado con P1.1 como primera acción, sin ejecutarla en esta auditoría. La decisión es congelar `#15`, cerrarlo después sin merge y preparar desde `origin/main` un sucesor reducido tras causa Linux demostrada; un full local y un CI por SHA. Se retiran receipts/monitors/fences nuevos y se aplazan escala, HA, VR, SaaS de avatares y upgrades masivos. No hubo gate largo, Actions, build, producción ni coste nuevo. |
| 2026-08-08 | P1.1 trasladó la auditoría a `codex/recovery-closure`, creada directamente desde `origin/main=9c1b85be99a7`. | El diff inicial contiene solo 18 documentos/`AGENTS.md`, conserva los gitlinks Hubs `ce8390a` y Cloud `c0a3419` y no incorpora workflows, scripts, tests ni código del PR raíz `#15`. `codex/project-security-timeout` y el PR permanecen congelados en `9315efb`; no hubo push, Actions, producción ni coste. P1.2 pasa a reproducir causalmente el 402 en Ubuntu x86 local. |
| 2026-08-08 | P1.2 reprodujo el contrato 402 sobre Ubuntu 24.04 x86 emulado: `2/2` variantes writer y `3/3` ordinarias terminan con parent `0`, lock retenido y postcondición fallida; también reproduce en ARM y en el foco x86 completo. | Quedan refutados arquitectura, orden del full, wrapper writer y pacing de 20 ms. La traza confirma dos reintentos de `resume_writers()` fallidos, pero `automatic=present|absent` varía y no aparece un subpaso estable; producto frente a fixture sigue indeterminado. Se prohíbe repetir 402 y se limita la siguiente acción a un único 478 aislado como discriminador, sin producto, full, CI, producción ni coste. |
| 2026-08-08 | El único intento aislado del 478 no alcanzó su inyección de checksum: en x86 QEMU falló antes `database-backup:stream`; el rollback hizo dos intentos y terminó en `terminal-boundary:durable-stop`, con `45` checks verdes y `2` rojos en `934.80` s. | La ejecución queda invalidada como evidencia del 478 y ese subcódigo no explica el 402 `process-local`. No se repiten 402/478 ni se inicia 764/766/768. La siguiente y única acción es el foco existente `durable-monitor-library`, sin DB/storage/checkpoint integral; no hubo producto canónico, full, CI, producción ni coste. |
| 2026-08-08 | El foco unitario `durable-monitor-library` falló aislado `45/46`, `real 111.85` s, confirmando interferencia en su wrapper/fixture; no explica el 402 `process-local`. La auditoría del CFG exacto comprobó que `step=none` excluye las 13 salidas de preparación instrumentadas, pero deja ocho familias posteriores, mientras `automatic=absent` contradice el flujo y prueba observabilidad incompleta. | Se cierran sin repetición los carriles 402/478/durable y no se inicia 764/766/768. Como el transcript no puede recuperar la arista causal, P1.2 queda abierta únicamente para un microselector test-only de la secuencia `process-local monitor-prepare`, con enums sin valores y una sola ejecución. No hubo producto canónico, full, CI, producción ni coste. |
| 2026-08-08 | El primer microselector test-only pasó sus `45` comprobaciones comunes, pero terminó `monitor_prepare_step:mode`, exit `1`, `real 76.30` s. La autopsia estática demostró que el propio selector omitió tras `source` `RECOVERY_CHECKPOINT_STAMP`, `RECOVERY_DUMP_SHA256` y `RECOVERY_STORAGE_SHA256`, por lo que el lock no podía validar antes de alcanzar `monitor-prepare`. | El ensayo se declara inválido y no se atribuye a producto. Red-team autoriza una única ejecución corregida porque restaura exactamente tres precondiciones que producción sí establece; revisión previa obligatoria y hard stop después de cualquier resultado. No se repite ningún escenario integral ni hubo producto canónico, full, CI, producción o coste. |
| 2026-08-08 | El microselector corregido pasó `45/45`, exit `0`, `real 84.58` s, atravesando modo, contrato/identidad/cero del parent, consumer-zero, delete/wait/ausencia y start/health/ausencia del watcher hasta `monitor_prepare_result:PASS`. | Quedan descartadas las primitives aisladas de preparación process-local, pero no se clasifica aún la diferencia como wrapper productivo o fixture/estado global. Se activa hard stop de ejecución; P1.2 solo admite una matriz estática de productores/consumidores y queda abierta si no demuestra causa exclusiva. No hubo producto canónico, full, CI, producción ni coste. |
| 2026-08-08 | La matriz estática final comparó lock/mode, cinco contratos y resourceVersions, receipts, flags y ciclo del watcher. El microselector termina después de preparar el watcher y no consume los contratos/receipts ni su stop/boundary; el marcador `waited` solo fuerza el fallo inicial. Permanecen al menos dos familias independientes y no puede atribuirse 402 exclusivamente a producto o fixture. | P1.2 se cierra como diagnóstico NO-GO, no como defecto corregido; se mantienen el hard stop y las VM ARM/x86 apagadas. P1.3 sustituye nuevas pruebas por el diseño estático de un recovery reducido: conserva DB+medios, Lease/lock, inventario, CAS y fence, auto-reanuda solo estados inequívocos y usa fail-closed más recuperación manual ante ambigüedad. No hubo código canónico, GitHub, producción ni coste. |
| 2026-08-08 | P1.3 demostró una incoherencia del plan: el contrato original de los cinco Deployments y del fence vive solo en memoria y el staging se elimina, de modo que un runbook posterior no puede recuperar con exactitud tras perder proceso u host bajo la prohibición absoluta de persistencia. | Se diseñó la excepción mínima dentro del mismo lock inmutable: autoridad JSON canónica/redactada, HMAC privado de las specs, identidades Namespace/PVC/fence, reglas generation/RV, takeover de Lease sin reloj de pared y `plan/execute` confirmado. Las revisiones detectaron y corrigieron autoridad incompleta, plan pre/post-takeover, el oracle que habría creado un SHA público sobre checksums derivados de credenciales y un registry de claves sobretrabajado; la custodia queda reducida a records inmutables append-only, UUID explícito y copia cifrada restaurada. Solo cambió documentación; no hubo tests, código, GitHub, secretos, producción ni coste. |
| 2026-08-08 | La revisión final única de P1.3 cerró el bootstrap de la clave con `create-candidate` CSPRNG sin salida sensible, copia cifrada externa, restore e import append-only no-clobber; también alineó auditoría, prompt y panel para no reabrir el diagnóstico agotado. | Seguridad, ejecutabilidad, coherencia y documentación terminaron GO con 0 P0/P1/P2. P1.3 queda cerrada, el progreso pasa a 8/23 y P1.4 es la única acción activa. No hubo tests, código funcional, GitHub, secretos, producción ni coste. |
| 2026-08-08 | La primera unidad aislada de P1.4 implementó el helper compartido de autoridad de checkpoint y su CLI/test: JSON canónico duplicate-safe, HMAC raíz sobre los cinco specs sin réplicas, payload público redactado, key records privados append-only por UUID explícito y recuperación crash-safe de una importación pendiente. | `node --check`, `git diff --check` y la suite focal pasan `9/9`; tres defectos encontrados por la primera revisión —pending no recuperable, réplicas originales cero y vectores autorreferenciales— quedaron corregidos, y las revisiones finales de aceptación y seguridad terminaron GO sin P0/P1/P2. La unidad aún no está conectada a `create-checkpoint.sh`, los monitores ni el recovery manual, por lo que P1.4 sigue abierta. No hubo GitHub, credenciales reales, clúster, producción ni coste. |
| 2026-08-09 | La segunda unidad de P1.4 integró en el lock inmutable de `checkpoint-backup` la autoridad pública redactada y su HMAC privado, recapturó exactamente Namespace/PVC/fence/cinco Deployments antes de mutar, propagó el contrato owner-aware a hijos y monitores y conservó restore/AUD-065 con `data == {}`. También retiró de `argv` los specs, fingerprints, listas de Deployments y mapas de annotations alcanzados por el componente compartido. | Helper `9/9`, autoridad `51/51`, CAS `59/59`, writer fence `104/104`, durable monitor `47/47`, AUD-065 `32/32`, restore lock y detectores privados backup/restore `46/46` quedaron verdes; Bash/Node syntax, ShellCheck y diff-check pasaron. Dos revisiones finales terminaron GO, 0 P0/P1/P2. El código quedó fijado localmente como `75ab970`; P1.4 sigue abierta para retirar la reentrada/receipts solo de backup, añadir `plan/execute`, temporal privado y su matriz. No hubo push, Actions, secretos reales, clúster, producción ni coste. |
| 2026-08-09 | La tercera unidad de P1.4 eliminó del backup la reentrada automática ambigua y sus receipts sin tocar el protocolo restore. Un latch monotónico se arma antes del primer CAS; tras cualquier fallo postmutación se conservan lock, Lease y fence, se detienen solo monitores/heartbeat locales y se emite una vez `manual-recovery-required`. El camino inequívoco reanuda `pgbouncer -> pgbouncer-t -> reticulum -> coturn -> bot-orchestrator`, valida `G -> G+1 -> G+2`, rehash, Lease/lock adyacentes a publicación y borra lock antes de liberar Lease. | Quedaron verdes backup CAS `74/74`, positivo durable `47/47`, terminal `48/48`, restore shell `47/47`, transporte privado `46/46`, durable parser `47/47`, restore lock y process-local positivo/drift `46/46` y `47/47`; ShellCheck, sintaxis, diff-check y Gitleaks pasan. Cuatro revisiones finales cerraron seguridad, restore, matriz y terminal con 0 P0/P1/P2. El código está fijado localmente como `3a6d6ad`; P1.4 sigue abierta solo para `plan/execute`, temporal privado y matriz manual. No hubo push, Actions, secretos reales, clúster, producción ni coste. |
| 2026-08-09 | La cuarta unidad de P1.4 añadió `recover-checkpoint-backup.sh plan`, estrictamente read-only. Recaptura y liga lock, Lease expirada, keyring, Namespace/PVC, cinco Deployments, runner/fence/helpers; genera plan canónico y confirmación exacta/caducable, con temporal privado `0600` y cleanup cooperativo. `execute` permanece deshabilitado. | Pasan positivo `47/47`, durable-dormant `46/46`, contrato `46/46`, hard-stops `66/66`, redacción `46/46`, señal `46/46`, helper `9/9`, Bash syntax, ShellCheck, diff-check y Gitleaks. Dos revisiones independientes cerraron con 0 P0/P1/P2. P1.4 sigue abierta únicamente para implementar y probar `execute`; no hubo push, Actions, secretos reales, clúster, producción ni coste. |
| 2026-08-09 | Una auditoria general de producto, operaciones, update-friendly, valor de recovery, proceso y red-team corrigio el requisito comercial: hibernar una instancia de cliente, retirar sus recursos facturables y reactivarla despues sobre infraestructura nueva. Confirmo que la plataforma base y sus forks deben preservarse, pero el restore actual rechaza `cold-rebind` y exige los UID originales del Namespace/PVC, mientras la linea P1.4 se habia desviado a resolver respuestas ambiguas con HMAC/keyring, monitores y matrices extensas. | Se congela P1.4 sin ejecutar sus grupos pendientes, full, GitHub o produccion. El plan y panel anteriores se conservan en `OLD/docs/` y las rutas canonicas contienen ahora seis hitos finitos: baseline/bundle, freeze+cold-rebind minimo, ensayo con UID nuevos sin coste DO adicional, integracion unica, hibernacion comercial real y cierre para volver a features. No se borro trabajo, no se toco produccion ni se genero coste. |
| 2026-08-09 | H1 preservo `codex/recovery-closure` sin fusionar y creo el worktree limpio `codex/client-hibernation` desde `origin/main=9c1b85b`, con Hubs `ce8390a` y Cloud `c0a3419`. La aceptacion publica no destructiva refresco portada espanola, sala/vestibulo, login, redireccion Admin y carga protegida de Spoke; no se concedio microfono, envio magic link ni modifico contenido. | `docs/client-hibernation-design-v1.md` fija nueve artefactos de `freeze-bundle-v1`, identidades source/target separadas, custodia externa, bootstrap exacto, dos preflights y el mapa keep/reimplementar/congelar. Los commits avanzados `6d2b0f9`, `75ab970`, `3a6d6ad`, `0e33acb` y los tres ficheros locales permanecen fuera del candidato. La revision adversarial termino GO sin P0/P1/P2; H1 queda pendiente solo de una sesion autorizada de login/audio/camara/avatar/Admin/Spoke. No hubo tests largos, GitHub, produccion ni coste. |
| 2026-08-09 | La sesion privada H1 envio y entrego el magic link real, autentico `info@virtualmente.com` y abrio Admin y el proyecto Spoke sin guardar ni publicar. La sala `VJopCY3` cargo con dos presencias; el avatar GLB fue visible y se verifico visualmente el ciclo tercera/primera persona. El control de asiento rechazo correctamente la accion sin waypoint a menos de 2 m. | H1 permanece abierto solo porque Chrome espera el permiso fisico de microfono: faltan mute/unmute y audio bidireccional. Se actualizan plan, panel humano y contrato con esa frontera exacta; no se inicia H2, no se repite recovery, no se toca DigitalOcean, GitHub ni contenido persistido. |
| 2026-08-09 | Cerramos la aceptacion privada H1 con dos clientes simultaneos en `VJopCY3`. Ambos enumeraron y activaron su microfono; silenciar/reactivar el cliente Chrome se reflejo como `Silenciado`/`Sin hablar` en el cliente interno y la misma transicion se verifico en sentido inverso. Los dos terminaron presentes y no silenciados. | H1 queda completo y H2 pasa a ser la unica fase activa. La evidencia se limita al baseline comercial v1; no promete bots publicos, sitting v2 ni calidad acustica/SLA no medidos. No se guardo contenido, no se mutaron Admin/Spoke/sala, no se toco DigitalOcean, GitHub ni produccion. |
| 2026-08-09 | H2 implemento `freeze-bundle-v1`, el preflight offline de una reactivacion greenfield y el restore coordinado cold-rebind sobre cluster/Namespace/PVC nuevos. Conserva el modo historico, publica los nueve artefactos antes de reanudar y liga por separado origen, contenido, recibo y destino. | Pasan bundle `5/5`, materializacion `46/46`, greenfield `4/4`, target preflight `49/49`, creacion `49/49`, cold restore/fail-close `49/49`, Bash syntax, ShellCheck y 51 gates de seguridad. Un fallo live post-escritura vuelve a cercar los cinco consumidores y retiene el lock. H2 cierra localmente; H3 debe ensayarlo en Namespace/PVC realmente aislados sin crear recursos DO nuevos. No hubo full, GitHub, produccion, secretos ni coste. |
| 2026-08-09 | H3 ensayo `freeze-bundle-v1` y cold-rebind en una VM Ubuntu 24.04 ARM64 con K3s `v1.35.5+k3s1`, sin mounts del host ni recursos externos. Los intentos H3a-H3c aislaron de forma causal la envolvente hardened de PostgreSQL 14, las listas Kubernetes tipadas y el preflight coordinado source/target; H3d fue el unico candidato aceptado. | H3d paso `14/14`: identidades source/target distintas de cluster, Namespace y PVC; DB con `356` relaciones, `94` migraciones y `17` hubs; avatar, escena, proyecto Spoke y medio diferido byte-exactos; tres UUID activos y cinco writers `1/1`, sin lock residual. Reactivacion local observada: `11 s`, no RTO comercial. Focos afectados, sintaxis, ShellCheck y diff-check quedaron verdes; no se repitieron bytes aceptados ni se ejecuto full, GitHub, DigitalOcean, produccion o secretos. H4 pasa a ser la unica fase activa. |
| 2026-08-11 | H4 completo el gate local sin repetir su bloque comun de cuatro horas: Hubs/Admin, navegador, capacidad `115/115`, HCCE, bots `154/154`, Dialog, Photomnemonic, Coturn, Spoke y Reticulum `461` tests + `5` propiedades quedaron verdes. Se corrigieron solo los pins transitivos vulnerables `ip-address 10.3.1` y `postgrex 0.22.4`. El cost gate mostro GitHub Free, Actions facturado USD 0 y USD 12.91 brutos totalmente descontados. | Se publicaron los PR draft Cloud `#23` y root `#16` con `[skip ci]`, sin workflows duplicados. El run manual `31518137826` dejo PostgreSQL 12/14 verde, pero ShellCheck fue cancelado sin diagnostico en sus dos intentos; el loop equivalente termino verde localmente. Anti-loop: no parchear el workflow por hipotesis ni relanzar el mismo intento; queda una sola ejecucion nueva observada mediante lecturas antes de fusionar Cloud y root. Produccion y DigitalOcean permanecen intactos. |
| 2026-08-11 | El run causal `31520065425` supero PostgreSQL 12/14 y permanecio activo hasta que GitHub anoto `The hosted runner lost communication with the server` durante ShellCheck. La medicion local demostro un pico de `16.214.080 KiB` RSS al usar `-x` sobre el arnes recovery de 17.000 lineas, que contiene multiples sources de la misma libreria. | La correccion no toca producto: ese unico arnes se analiza sin seguir fuentes externas y excluye solo `SC1091/SC2030/SC2034/SC2317/SC2329`, diagnosticos resultantes; `deployment/lib/recovery-safety.sh` y todos los demas scripts conservan ShellCheck `-x`. El modo acotado tarda unos 10 s y usa unos 4,1 GB. Falta un unico CI terminal sobre estos bytes; no se relanza ningun run anterior. |
| 2026-08-11 | Antes de dejar avanzar el siguiente CI se comparo su contrato con la evidencia local: el job `static-security` tenia `timeout-minutes: 75`, pero la regresion recovery secuencial aceptada tarda alrededor de cuatro horas. Incluso con ShellCheck corregido el workflow estaba programado para un falso timeout. | Se conserva la suite completa y su orden; solo se restaura el limite `360` minutos. El run intermedio se usa exclusivamente para confirmar ShellCheck y se cancela al entrar en regresion, antes del timeout conocido. No se cuentan sus bloques verdes como otro gate integral ni se recortan pruebas. |
| 2026-08-12 | H5-B0 completo el preflight live y la receta greenfield legacy. El primer checkpoint que entro en downtime escalo los cinco writers a cero pero fallo antes del handshake del monitor; no llego a copiar DB ni storage. Se recuperaron los cinco writers, se verifico produccion `12/12`, se retiro el lock exacto y DigitalOcean quedo intacto. | La evidencia raw live demostro que Kubernetes conserva el GVK del LIST pero omite `apiVersion/kind` en sus `12` Deployment, `67` ReplicaSet y `12` Pod items. El monitor ahora acepta solo ausencia/null bajo LIST tipado, mantiene WATCH/GET estrictos y rechaza GVK explicito falso. Positivo `46/46`, negativos `48/48`, sintaxis, ShellCheck y diff-check verdes; revision independiente GO. Anti-loop: no se repitio el checkpoint sobre los mismos bytes; queda autorizada una sola repeticion causal y la misma firma produce STOP. |
| 2026-08-12 | La unica repeticion causal H5-B completo la captura redactada con los ocho metadatos `FREEZE_*`, pero el monitor volvio a fallar antes del handshake con `checkpoint_writer_monitor_stage:baseline`; no copio DB/medios ni publico bundle. El rollback restauro exactamente los cinco writers. | Reconciliacion terminal: produccion `12/12`, recovery lock ausente, Lease libre, cero bundles publicados y DigitalOcean intacto. Se activa el STOP anti-loop: no hay tercer checkpoint, cifrado de artefactos incompletos ni borrado DO. H5-B1 queda limitada a una unica autopsia read-only que decida reparacion estrecha o simplificacion de ruta. |
| 2026-08-12 | H5-B1 hizo una unica autopsia read-only: los siete Pods no writers materializan `enableServiceLinks=true` y `imagePullSecrets` del ServiceAccount aunque el PodTemplate los omita; `7/7` proyecciones coinciden con su ServiceAccount y no hay drift de ownership/selector/ReplicaSet. | El monitor conserva WATCH continuo, normaliza solo el default, valida y liga la proyeccion admission a baseline/LIST/WATCH. El foco pasa `49/49`; sintaxis, ShellCheck y diff-check verdes; revision adversarial GO sin P0/P1/P2. El plan permite un unico candidato final; cualquier fallo termina la ruta automatica H5. No hubo checkpoint, downtime, bundle, GitHub ni mutacion DO durante la autopsia. |
| 2026-08-12 | El candidato final H5-B completo la captura redactada, pero volvio a fallar antes de `ready` con stage `baseline`; no copio DB/medios ni publico bundle. El rollback restauro los cinco writers y termino con produccion `12/12`, lock ausente, Lease libre y cero bundles. | Se cierra definitivamente la ruta automatica del monitor: no habra otra autopsia/parche/retry dentro de H5 y DigitalOcean permanece intacto. H5-B3 debe elegir una ruta finita: barrera temporal de writers, ventana pragmatica con riesgo explicitamente aceptado o conservar costes DO. |
| 2026-08-13 | H5-B19 recreo la topologia exacta autorizada, cerro DNS/certificados `4/4` y llevo cold-rebind hasta la fase DB. Un primer intento dejo la DB target vacia antes del stream; el siguiente se detuvo antes del reset con `database_restore_stage:quiescence`, conservando lock y cinco writers a cero. | La autopsia read-only confirma contratos `5/5`, cero Pods writer/runner, LIST/WATCH runner valido y PostgreSQL Ready; la Lease existe pero esta libre/sin holder. Se anadio solo un diagnostico allowlisted por subetapa y su foco pasa `51/51`, junto con Bash, ShellCheck y diff-check. No hubo otro restore, borrado DO, reanudacion ni prueba de navegador; el siguiente limite es un unico candidato tras limpieza exacta del lock. |
| 2026-08-20 | El propietario actualizo el Goal para que H5-B19 consolide primero una unica version final de root y Hubs Cloud y ejecute un solo `verify-project.sh --full` antes de cualquier nueva mutacion productiva. | El plan pasa a v27 y corrige el orden: diff completo y candidato reproducible, revision unica, full unico, limpieza exacta del lock, un solo restore, verificador live y navegador frio. El Goal no se cierra con pruebas parciales ni se repite un restore sin causa nueva. No se modifico produccion en esta actualizacion. |
| 2026-08-20 | La revision independiente unica del candidato H5-B19 encontro tres incompatibilidades de integracion antes del full: el verificador live solo admitia control plane durable, el full no convocaba toda la cobertura H5 y el submodulo Cloud debia fijarse antes del gate. Tambien detecto que el ejemplo de limpieza stale omitía el target cold-rebind. | El plan pasa a v28 con tres correcciones finitas y ownership separado; el runbook exige `RESTORE_TARGET_MODE=cold-rebind`. No se ejecuta full ni se toca produccion hasta consolidar Cloud y root en commits limpios. |
| 2026-08-20 | Se cerraron las tres incompatibilidades pre-full: contrato Cloud durable/cold disjunto, verificador live exacto para legacy process-local y selector final H5 agregado sin repetir prechecks; el runbook de limpieza incluye target, recibo y values. | Cloud apply pasa 120/120, security gates 57/57 y cold-rebind execute 49/49; Bash/Node/ShellCheck/diff-check dirigidos estan verdes. El plan pasa a v29. No se simula Internet ni se toca produccion; el siguiente paso es commit Cloud, gitlink/root limpio y un unico full. |
| 2026-08-20 | Cloud `7e56f90` y root `b88cee7` quedaron limpios y reproducibles. El primer full se detuvo a los 40 s en el bloque estatico por `SC2329`: ShellCheck no reconocia una funcion invocada indirectamente desde un trap EXIT. | Se añadió solo la supresion local documentada; ShellCheck exacto y diff-check pasan. El plan pasa a v30 y aplica STOP anti-loop: no se relanza el full en esta misma parada, no se tocaron suites largas ni produccion. |
| 2026-08-20 | La siguiente ejecucion full se detuvo a los 52 s, aun en ShellCheck, porque una supresion `SC2016` existente estaba separada de su cadena SQL por una asignacion. | Se movio solo el comentario a la linea adyacente; ShellCheck exacto y diff-check pasan. STOP sin relanzar, sin suites largas ni produccion. La ruta y la version del plan no cambian. |
| 2026-08-20 | La siguiente ejecucion full se detuvo a los 160 s, aun antes de las suites, por ocho avisos ShellCheck `SC2030/SC2031/SC2016` en fixtures recovery con subshells, PATH, PID y cadenas literales. | Se añadieron solo supresiones locales justificadas; `shellcheck -x tests/recovery/test-recovery-safety.sh` y diff-check pasan. El heartbeat del proceso terminal se elimino. STOP sin relanzar ni tocar produccion; la ruta y version del plan no cambian. |
| 2026-08-20 | El siguiente full supero el bloque estatico y dejo verdes las suites recovery observadas `32/32` y monitor `57/57`, pero se detuvo a los 320 s en `profile-specific bot health contract`. | La causa exacta era un regex `jq` con escapes duplicados que rechazaba el room id valido `VJopCY3`; se corrigio una linea y `security-gates` pasa `57/57`, junto con ShellCheck y diff-check dirigidos. El plan pasa a v31. STOP sin relanzar full ni tocar produccion. |
