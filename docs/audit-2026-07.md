# Auditoria integral de YenHubs - Julio 2026

Fecha de corte: 16 de julio de 2026.

Estado: auditoria integral completada sobre codigo, dependencias, upstream,
operacion, recuperacion, seguridad y UX. DigitalOcean, DNS, TLS, Mailtrap, los
12 deployments, Spoke, la sala y las releases estables estan operativos. La
credencial GHCR del cluster y de Actions esta renovada y validada. La clave
OpenAI historica se sustituyo en produccion y despues se revoco, con rechazo
HTTP 401 confirmado. No quedan acciones externas de rotacion pendientes. Los
cambios de CI y
hardening realizados durante este cierre estan validados localmente y por
GitHub Actions, pero no se presentan como desplegados.

## 1. Conclusion ejecutiva

YenHubs fue recuperado, auditado y actualizado de forma incremental. Hubs `prod-2026-03-11` y Hubs CE `2.1.0` estan
integrados y aceptados con las personalizaciones principales. El proceso valido la estrategia correcta: restaurar el
baseline, crear checkpoints coherentes, modernizar por capas y desplegar por digest mediante el pipeline estandar.

Reticulum ya no usa el stack Elixir/Phoenix vulnerable del baseline. La modernizacion, el proxy CORS, los limites de
upload, el aislamiento por sala, la higiene de logs, los NetworkPolicies y los security contexts estan desplegados.
La historia Git con secretos rotados no se reescribio porque esa limpieza seria destructiva para clones y referencias.

La extension final de la auditoria creo checkpoints completos DB+storage,
automatizo la comparacion upstream y el gate integral, limpio documentacion
obsoleta y anadio controles de supply chain. Tambien detecto dos hechos que el
estado live por si solo no mostraba: el pull token de GHCR estaba caducado y la
clave OpenAI expuesta en Git era tambien la clave live. GHCR se renovo en
Actions, llavero y Kubernetes; la clave OpenAI se sustituyo y probo end-to-end.
La credencial OpenAI anterior se revoco despues de la aceptacion live y una
comprobacion posterior confirmo HTTP 401.

La rama actual de bot-orchestrator tiene un contrato de privacidad razonable para un MVP:

- la clave OpenAI solo existe en backend;
- las peticiones usan `store: false`;
- no se guarda historial en Reticulum ni en el orquestador;
- el historial del navegador vive en memoria y se pierde al abandonar la sala;
- entrada y salida pasan por moderacion;
- hay limites de longitud, frecuencia y acciones;
- la respuesta de `gpt-5-nano` usa Structured Outputs con JSON Schema estricto;
- OpenAI recibe un `safety_identifier` seudonimo, no el identificador de cuenta en claro.

Esto reduce datos propios almacenados, pero no equivale a Zero Data Retention en OpenAI. Para un evento con publico se
necesita aviso de privacidad y, si se exige retencion cero del proveedor, solicitar ZDR a OpenAI.

Los guardarrailes anteriores se reforzaron y redesplegaron el 15 de julio. El cliente muestra un aviso explicito de
privacidad, Reticulum filtra `message` y `prompt` en sus logs y el rate limit se aplica por cuenta y sala, no por bot.
Pruebas live confirmaron moderacion, resistencia basica a prompt injection, ausencia de prompts/respuestas en logs y
bloqueo concurrente aunque el usuario rote entre bots. Esto no equivale a Zero Data Retention del proveedor.

## 2. Versiones y ruta de actualizacion

### Cliente Hubs

- Base congelada original: `hubs/master` en `7aa9a35f4`.
- Fuente del runtime desplegado: `a7214eb88`.
- Fuente final auditada tras el cierre: `hubs/master` en `492625c57`.
- Base oficial comun: `prod-2025-12-17` (`55ae9ec20`).
- Ultima release de produccion encontrada: `prod-2026-03-11` (`e3b9cc749`).
- La release `prod-2026-03-11` esta integrada como ancestro de la rama final.
- Contra `upstream/master` de julio hay 13 commits oficiales no publicados y
  conflictos anticipados solo en workflows.

### Hubs Community Edition

- Fork congelado original: `hubs-cloud/master` en `832d8e3`.
- Fuente del runtime desplegado: `5a82de5`.
- Fuente final auditada tras el cierre: `hubs-cloud/master` en `4a1e3b9`.
- Version del generador: 2.1.0.
- Release oficial disponible: 2.1.0 (`410bc52`).
- La release `2.1.0` es ancestro de `hubs-cloud/master`; el fork final queda 79 commits por delante y 0 por detras de
  esa release. El conflicto deliberado de `community-edition/input-values.yaml` se resolvio conservando el fichero
  local e ignorado para no publicar secretos.

### Orden ejecutado

1. Baseline restaurado y smoke tests completados.
2. Checkpoint DB+storage coherente preparado y restaurado en aislamiento.
3. Reticulum, Dialog, Photomnemonic, Coturn y bot-orchestrator modernizados por separado.
4. `hubs/prod-2026-03-11` integrado en una rama dedicada.
5. Hubs CE 2.1.0 integrado en otra rama.
6. Imagenes construidas solo con GitHub Actions y fijadas por digest.
7. Rollout con `gen-hcce` + `kubectl apply`, rollback real y aceptacion de navegador.
8. `upstream/master` queda fuera: no debe tratarse como una release de produccion.

### Artefactos finales publicados

- Hubs: commit `a7214eb88`, Actions `29506573351`, digest
  `sha256:cff099ef4759c8ec8e8d6010ae9268c6b6e99f29ff5ecb50f6e50ce884d20a8c`.
- Reticulum: cloud commit `5a82de5`, CI `29506498175`, build `29506780044`, digest
  `sha256:9ae6712fa5cd4380048ec559cbf75596507ae91cdbd653cac1978b685254faef`.
- Bot orchestrator: cloud commit `5a82de5`, Actions `29506574473`, digest
  `sha256:325c5c10e4ee039518693771c0974a0e5c876dcf54c443295e84490f4fa8ec53`.
- Spoke: digest `sha256:f5120264938e189e702f835182ed4a28a5ce20b140d7262bc2a3074e6d0b6657`.
- Dialog: digest `sha256:95687f4765e7a68ef05a714b807bf5c80e0f9187e2715f3a5a96e2d664377a23`.
- Photomnemonic: digest `sha256:aef369b82212429d01c0f1f554b16c34a99cf4bbb75e0693e190c796b33012f2`.
- Coturn: digest `sha256:c2ad335349d477d342d5b17c82b513bfebc8c17b8e6b4e27a3049f3478207780`.
- Rollback Hubs probado: `sha256:aa703dc35b80e05a3ece28a9827375fd9d2d312dfa5a173991ebf54a3e978481`.

Los tags no se usan como unica evidencia. El checkpoint final conserva digests, DB, storage, commits y checksums.

## 3. Hallazgos prioritarios

| ID | Severidad | Estado | Hallazgo y accion |
| --- | --- | --- | --- |
| AUD-001 | P0 | Parcial | Hubo secretos reales en la historia publica de Git. Se dejo de versionar `community-edition/input-values.yaml`, se eliminaron credenciales hardcoded y se rotaron localmente claves de DB, Phoenix, Guardian, Erlang, permisos, bots y SMTP. Reescribir historia requiere una decision separada porque rompe clones y referencias. |
| AUD-002 | P0 | Desplegado y verificado | Reticulum se modernizo a Elixir 1.18.4/OTP 27.3.4.14, Phoenix 1.6.17, Plug 1.16.6, Cowboy 2.15, Ecto 3.14, Postgrex 0.22.3 y el resto del lock compatible. Los 26 avisos iniciales se redujeron a dos CVE de cowlib 2.18.0 sin version corregida publicada; se reconocen por ID y cualquier aviso nuevo falla CI. GitHub Actions `29403210817` paso formato, compilacion estricta, 278 pruebas en PostgreSQL 12.19/14.23 y release `turkey`; build `29403433612`. El digest se acepto primero con PostgreSQL 12 y el storage coherente en un canary aislado, y despues en produccion con rollout `Recreate`, readiness 200, DB/storage invariantes y manifiesto sin drift. |
| AUD-003 | P1 | Desplegado y verificado | Los 13 contenedores live estan fijados por `repository@sha256`; Postgres, PgBouncer, PostgREST, Coturn, Nearspark, Photomnemonic, Spoke y las imagenes custom ya no dependen de tags mutables. El verificador rechaza cualquier Deployment sin digest y `kubectl diff` queda vacio. |
| AUD-004 | P1 | Corregido en codigo | Los workflows reutilizables apuntaban a `hubs-ops@master`. Se fijaron al commit oficial `afcc26a61a6353affc116e8fa5fb409078bbcb82`. |
| AUD-005 | P1 | Desplegado, smoke basico | El navegador podia originar directamente una accion de movimiento de bot. Reticulum es ahora la autoridad: valida la respuesta del orquestador y emite el comando; el canal rechaza comandos enviados por clientes. |
| AUD-006 | P1 | Desplegado y aceptado en vivo | El chat IA incorpora moderacion de entrada/salida, rate limit por cuenta y sala, limites de payload/prompt/tokens, `store:false`, `safety_identifier` HMAC, JSON Schema estricto, rechazo seguro, acciones allowlist y logs sin prompts ni respuestas. El cliente avisa que la conversacion es temporal y que el proveedor procesa cada mensaje. Pruebas reales con `gpt-5-nano` verificaron navegacion valida, rechazo de contenido danino, resistencia basica a prompt injection y ausencia del texto de prueba en logs. No implica ZDR del proveedor. |
| AUD-007 | P1 | Desplegado y verificado tras reinicio | La reconciliacion elimina estado obsoleto y detiene runners de salas desactivadas. Tras reiniciar Reticulum, los dos runners ghost atravesaron la ventana esperada de reconexion, recargaron 11 waypoints y rehidrataron `VJopCY3`/`XesSAqd`; una entrada tardia recibio las cinco entidades configuradas. Falta una prueba prolongada de caida deliberada del orquestador y cinco salas simultaneas. |
| AUD-008 | P1 | Desplegado y aceptado en vivo | El entorno usa `RUNNER_BACKEND=ghost`; el fallback seguro tambien es ghost y Phoenix JS esta fijado. Chromium solo se activa explicitamente. Un navegador real entro en `VJopCY3`, recibio cinco bots y capturo posiciones distintas cuatro segundos despues; el runner no aparecio en `Personas`. |
| AUD-009 | P1 | Desplegado y verificado | Reticulum ya no es privilegiado, no propaga montajes, impide escalada, elimina capabilities y usa seccomp `RuntimeDefault`; tiene request 250m/2Gi y limite 4Gi sin CPU limit. Un smoke previo y el verificador live demuestran escritura, bind, logs, readiness y seguridad tras los rollouts. |
| AUD-010 | P1 | Corregido y verificado | El cliente y Reticulum validan el formato y la pareja de archivos antes de crear/actualizar un avatar. El servidor acepta solo extensiones MIME allowlist, exige tupla GLTF+BIN o GLB standalone valida, limita modelo a 64 MiB, texturas a 16 MiB y thumbnail a 5 MiB, y devuelve errores controlados. Las pruebas backend y CI `29459634118` pasan. |
| AUD-011 | P2 | Desplegado; VR pendiente | La tercera persona puede alternarse en vivo. Primera persona en VR tiene prioridad en codigo, pero falta aceptacion con dispositivo VR. |
| AUD-012 | P2 | Desplegado; multiusuario pendiente | Sitting funciona con `Disable motion`, replica `isSitting`, bloquea posicion IK y reproduce `sit`. Ambos loaders cancelan viaje y pose al perder ownership; el movimiento movil tambien levanta al avatar. Si no hay asiento libre a menos de 2 m, la UI informa en lugar de navegar silenciosamente. Sit/stand basico paso en produccion; falta solo la carrera multiusuario real y recolocar los dos asientos provisionales. |
| AUD-013 | P2 | Parcialmente aceptado | Nueve avatares normales/full-body fueron reimportados y sus endpoints pasan. La UI privada Avaturn y su guia estan live, autenticadas y en espanol; guardar permanece bloqueado sin preview compatible. Falta una prueba de guardado real, que requiere checkpoint por mutar DB/storage. |
| AUD-014 | P2 | Corregido y aceptado en vivo | Escena, toolbar, chat, selector de avatares y guia Avaturn se revisaron a 1440x900, 1024x1366 y 390x844. El cierre Obsidian Aurora centraliza capas, evita que toolbar/sidebar se solapen, mejora contraste y controles y conserva el scroll movil. El digest final se acepto sin excepciones ni overflow con cinco bots. Evidencia en `output/final-acceptance-20260716/` y `output/ui-award-audit-20260716/production/`. |
| AUD-015 | P2 | Medido; optimizacion futura | Carga fria medida: DCL ~2.31 s, load ~2.96 s y ~46.0 MiB transferidos; carga caliente ~1.91/2.06 s. En el Mac de prueba se observaron ~144 FPS sobre pantalla 144 Hz y ~307 MiB de heap. El entrypoint de sala sigue en 8.42 MiB (`hub-vendors` 5.5 MiB); dividir dependencias queda como mejora, no como cambio a ciegas de este cierre. |
| AUD-016 | P2 | Corregido | `npm audit --omit=dev` devuelve 0 advisories para Hubs y Admin. La deuda restante del arbol completo es de tooling/dev y no se aplico `npm audit fix --force`. |
| AUD-017 | P2 | Corregido y documentado | `verify-page-assets.mjs` valida assets, hashes inline y CSP y forma parte del verificador live. La incidencia `js-cookie 3` demostro que HTTP/CSP no detecta incompatibilidades runtime: tras cada Hubs rollout es obligatorio reiniciar Reticulum y hacer carga fria real comprobando `APP`, `AFRAME` y excepciones antes de aceptar. |
| AUD-018 | P1 | Corregido | GHCR rechazaba con `403` la publicacion de paquetes existentes cuando el workflow usaba solo `GITHUB_TOKEN`. Las credenciales se guardan ahora como secretos en GitHub Actions (`REGISTRY_USERNAME`/`REGISTRY_PASSWORD`), nunca como inputs del workflow; Reticulum y bot-orchestrator volvieron a publicarse por el pipeline normal. |
| AUD-019 | P1 | Desplegado y verificado | Canal, Reticulum y orquestador exigen una clave interna fuerte; el orquestador usa comparacion constante, el contrato live de chat pasa y la clave fue rotada de forma coordinada el 14 de julio. |
| AUD-020 | P1 | Desplegado y aceptado en vivo | El ghost runner limita `scene.model_url` al origen de Hubs o hosts explicitamente permitidos, revalida redirecciones e impone limites. Lee parcialmente el GLB, extrae `nav-mesh`, proyecta waypoints y calcula rutas A*. La escena live produce 350 triangulos, 11 grupos, 11 waypoints, 8 `spawbot-*` y 56/56 rutas dirigidas. Logs de ambos runners confirman `navmesh_preferred` sin fallback; navegador real recibio cinco bots en movimiento. |
| AUD-021 | P2 | Documentado | El chat privado con bots usa una ruta `auth_required`: un invitado puede ver bots pero debe iniciar sesion para hablar. Es el contrato de seguridad actual y debe figurar en la ayuda de usuario, no tratarse como una averia del LLM. |
| AUD-022 | P2 | Corregido | Las notificaciones imponian `min-width: 420px` y desbordaban moviles de 390 px. El contenedor usa ahora ancho fluido hasta 420 px y conserva margen lateral. |
| AUD-023 | P1 | Corregido | El despliegue dependia de cuatro ediciones manuales y un parche RBAC que se perdian al regenerar. El template contiene ahora cert-manager, SSL por ingress, `ingressClassName`, RBAC CRD/Gateway y HAProxy compatible; elimina el certificado autofirmado sin uso. `gen-hcce` falla si quedan placeholders, aparece mas de un LoadBalancer, cambian los dos PVC de 10 GiB o se rompe una de esas invariantes. |
| AUD-024 | P0 | Corregido y respaldado | Los 93 `owned_files` cuyos bytes ya no existen se marcaron inactivos en una transaccion revisada; no se borro ningun registro ni archivo. Produccion queda coherente con 34 filas activas, 34 blobs y 34 metadatos, y `VJopCY3` apunta a la escena recuperada `f6VKtim`. El estado se restauro dos veces en PostgreSQL 12 aislado y dispone de dump+storage posteriores con checksums. |
| AUD-025 | P1 | Corregido y consolidado | Las ramas finales se fusionaron por fast-forward y se publicaron: `hubs/master` apunta a `492625c57`, `hubs-cloud/master` a `4a1e3b9` y el superproyecto registra ambos punteros en `meta-hubs/main`. Los digests live siguen procediendo de `a7214eb88`/`5a82de5` hasta un rollout posterior. |
| AUD-026 | P1 | Parcial | Los 13 contenedores ya tienen budgets, cinco reglas ingress protegen bots/DB/pools/thumbnails, Photomnemonic tiene egress web publico fail-closed y solo HAProxy monta token Kubernetes con una cuenta dedicada. Varios contenedores siguen como root, no hay aislamiento egress global y los limites de memoria estan sobrecomprometidos para conservar capacidad de burst; requiere hardening por imagen y carga, no cambios masivos a ciegas. |
| AUD-027 | P2 | Corregido y verificado | `Inicio`/`VJopCY3` fue reasignada de la escena historica perdida `vrsQuSc` a la reconstruida `f6VKtim`. La portada, el lobby y la sala renderizan la oficina recuperada. |
| AUD-028 | P1 | Desplegado y verificado | PostgreSQL y Reticulum hacian `RollingUpdate` sobre PVC single-writer; Coturn/Dialog podian bloquearse al solapar `hostPort` en un unico nodo. Los cuatro usan ahora `Recreate`, el verificador lo exige y los rollouts terminaron 1/1. |
| AUD-029 | P2 | Desplegado y verificado | HAProxy no tenia startup/readiness y su liveness lo reinicio durante un rollout sano. Ahora usa startup, readiness y liveness sobre `/healthz:1042`; la nueva revision arranco sin reinicios y HTTPS siguio en 200. |
| AUD-030 | P1 | Desplegado y verificado | Reticulum tenia privilegios de host sin una necesidad demostrable. El template y el verificador eliminan `privileged`/`mountPropagation`, bloquean escalada, eliminan capabilities y exigen seccomp; el proceso live conserva escritura en `/storage`, responde y arranca con `CapEff=0`/`NoNewPrivs=1`. |
| AUD-031 | P0 | Corregido, rotado y verificado | Phoenix registraba parametros completos de union y podia incluir `bot_access_key`. El filtro global cubre credenciales anidadas, una prueba de regresion conserva el contrato y la huella automatica reinicia Reticulum+orquestador al rotar. Tras el rollout, el valor real no aparece en logs, se observa `[FILTERED]`, ambos procesos comparten la nueva clave por hash y `kubectl diff` es cero. |
| AUD-032 | P1 | Desplegado y verificado | Cilium no tenia NetworkPolicies. Cinco reglas ingress fail-closed permiten solo Reticulum->bot/ambos pools/Photomnemonic y ambos pools->PostgreSQL en los puertos exactos. Una sexta regla egress restringe Photomnemonic a kube-dns y web publica TCP/80,443, excluyendo rangos privados/reservados. Las pruebas positivas y negativas pasaron; HTTPS, DB y ghost runner siguieron sanos. |
| AUD-033 | P1 | Desplegado; carga pendiente | Ninguno de los 13 contenedores declaraba recursos. Todos tienen ahora requests y memory limits sin CPU limits, desplegados con los cuatro `Recreate` uno a uno. Tras medir Chromium 149, requests de aplicacion: 665m/3.200Mi; reserva total de nodo: 33% CPU/62% RAM. Los memory limits suman 150% y no sustituyen una prueba escalonada de CCU/WebRTC. |
| AUD-034 | P0 | Corregido, rotado y verificado | La imagen Coturn live imprimia `PSQL` completo al arrancar y lo pasaba en `argv`. Una capa minima sobre el digest probado mueve la URI a configuracion `0600`, elimina logs/argumentos sensibles y tiene prueba con credencial senuelo. Tras checkpoint y restauracion aislada, la clave DB se roto; la anterior fue rechazada por autenticacion remota, los cuatro consumidores comparten checksum, TURN TCP es accesible y el manifest rechaza el digest vulnerable. |
| AUD-035 | P1 | Desplegado y verificado | Kubernetes montaba su token por defecto en todos los pods aunque solo HAProxy usa la API. Los 11 Deployments restantes declaran `automountServiceAccountToken: false`; se desplegaron por lotes y cada servicio conservo readiness, DB, HTTPS, TURN y ghost runner. |
| AUD-036 | P2 | Corregido y verificado | Todos los Actions directos usan releases Node 24 fijados por SHA (`checkout/setup-node` v7, Docker setup/login v4 y build-push v7). `actionlint` descubrio y se corrigieron dos filtros `branches` vacios, un input smoke inexistente y una inyeccion de shell por `github.head_ref`. Builds reales Hubs `29373238787` y cloud `29373240303` pasaron sin los avisos Node 20/buildx. |
| AUD-037 | P1 | Corregido para produccion | El baseline inicial tenia Hubs 20, Admin 7, Dialog 15 y Photomnemonic 4 advisories de produccion. Todos los `npm audit --omit=dev` terminan en 0 para Hubs, Admin, Dialog, Photomnemonic, generador y bot-orchestrator. La evidencia inicial se conserva en `output/dependency-audit-20260715-0035/`. |
| AUD-038 | P1 | Desplegado y verificado | Bot-orchestrator corria como root pese a no necesitar privilegios. La imagen y el manifiesto usan UID/GID 1000, `runAsNonRoot`, cero capabilities, `NoNewPrivs` y seccomp RuntimeDefault. La imagen se probo primero en un pod aislado; el rollout rehidrato dos salas ghost, mantuvo 10 bots maximos, cero errores/restarts y `kubectl diff` cero. |
| AUD-039 | P1 | Desplegado y aceptado en vivo | Dialog usaba Node 18 EOL y acumulaba 15 advisories. Se retiro `sctp` directo no usado, se actualizaron Express/body-parser y Mediasoup a 3.19.22, se sustituyeron APIs privadas de worker/producers, se anadieron pruebas reales de worker/router/WebRtcTransport y sondas TCP. Produccion corre como UID/GID 1000 con cero capabilities, `NoNewPrivs`, seccomp, cero reinicios y cero advisories. Dos navegadores aislados publicaron y consumieron audio simultaneamente: signaling, send/recv transports, ICE y PeerConnection quedaron `connected`, cada cliente mantuvo un producer y consumer live y envio/recibio mas de 125 paquetes RTP en 2,5 segundos. La configuracion ESLint antigua tambien estaba rota; repararla expuso 63 errores de estilo/deuda preexistente que no bloquean sintaxis/runtime y deben corregirse por separado. Evidencia: `output/audit-dialog-node22-20260715-0105/`. |
| AUD-040 | P1 | Desplegado y verificado | Photomnemonic usaba Node 14/Buster, Chromium 112 y Puppeteer 19, tenia 4 advisories y validaba DNS una sola vez antes de entregar la URL a Chrome, dejando riesgo SSRF por redireccion o rebinding. Ahora usa Node 22, Chromium 149 y Puppeteer Core 25.1, revalida URL/redirect/request, rechaza rangos privados/reservados, limita concurrencia, evita path/query en logs y cierra paginas. Pruebas unitarias, pod aislado y produccion verificaron PNG real, 403 para loopback/metadata/puertos no web, 429 concurrente, egress sin API/metadata, UID/GID 1000, cero capabilities, seccomp, probes, cero advisories/restarts y manifiesto sin drift. RSS medido ~400 MiB; budget 384/768 MiB. Evidencia: `output/audit-photomnemonic-20260715/` y `output/audit-photomnemonic-predeploy-20260715-0138/`. |
| AUD-041 | P0 | Desplegado y verificado | El proxy CORS validaba una IP pero entregaba el hostname original al cliente HTTP, permitiendo una segunda resolucion DNS y posible rebinding; tambien conservaba cabeceras sensibles y no bloqueaba redirecciones con suficiente profundidad. El nuevo adaptador fija la conexion a una IP publica validada, conserva Host/SNI, rechaza respuestas DNS mixtas y rangos no globales, limita metodos/puertos, elimina credenciales/cookies/cabeceras hop-by-hop y convierte redirecciones en 502. Pruebas TCP reales demuestran pinning, saneamiento y que un destino de redireccion no recibe conexion; forma parte del digest Reticulum aceptado. |
| AUD-042 | P1 | Desplegado y verificado por tests | `EntityManager` buscaba entidades y subentidades por `nid` sin acotar `hub_id`; dos salas con identificadores iguales podian resolver o modificar el registro equivocado. Las consultas quedan acotadas por sala, entidad raiz y transaccion, con regresion que usa los mismos `nid` en dos salas. |
| AUD-043 | P1 | Desplegado y verificado por tests | El borrado de una cuenta reasignaba avatares pero no sus `avatar_listings`, dejando riesgo de fallo por clave foranea o datos huerfanos. La operacion multi reasigna tambien listings y la prueba comprueba expresamente el nuevo propietario. |
| AUD-044 | P2 | Desplegado y verificado | Statix 1.x podia bloquear indefinidamente Reticulum en OTP 27 esperando una respuesta interna del puerto UDP. Se sustituyo por un cliente StatsD asincrono supervisado que conserva la API, no bloquea procesos de request/canal y tiene pruebas de paquetes, tags y sampling. |
| AUD-045 | P2 | Corregido y aceptado en vivo | La escena recuperada carecia de `nav-mesh`; tras checkpoint se anadio un Floor Plan y se republico `f6VKtim`. El jugador y el ghost runner reutilizan ese navmesh; los bots calculan A* sin depender de `box-collider`. Permanecen avisos A-Frame/MeshBVH no fatales. |
| AUD-046 | P1 | Desplegado y verificado | El rate limit del chat se calculaba por sala, bot y cuenta, por lo que alternar bots multiplicaba el caudal permitido. Ahora la clave es cuenta+sala para todos los bots; 18 pruebas locales y una prueba concurrente live demostraron que una de dos peticiones simultaneas a bots distintos queda limitada. Reticulum filtra ademas las claves `message` y `prompt` antes de escribir parametros. |
| AUD-047 | P1 | Desplegado y verificado | El contenedor sustituia `{{rawhubs-base-assets-path}}` solo en HTML/CSS, aunque Webpack tambien lo incrusta en bundles JavaScript; 24 bundles quedaban afectados y el hero solicitaba una URL literal rota. El arranque procesa ahora JS y falla si queda un placeholder. La imagen live carga el hero 1920x1080 desde `assets.meta-hubs.org`, conserva la sala 3D y no tiene drift. |
| AUD-048 | P1 | Desplegado y verificado | Si el POST final de publicar en Spoke devolvia `401`, el cliente llamaba a un metodo inexistente y podia repetir exportacion y uploads. Ahora reautentica y repite solo el POST final con el token nuevo; cancelar aborta tambien el upload GLB principal. Pasaron 65 pruebas, build Node 16.13.2, Actions `29408127281`, rollout 1/1, HTTPS y verificador live sin drift. |
| AUD-049 | P1 | Desplegado; carrera multiusuario pendiente | El controlador cancela el viaje, limpia `isSitting` y restaura el flujo coherente cuando hay movimiento movil o se pierde ownership en loader legacy/bitECS. Check, lint, pruebas y build pasaron; sit/stand basico fue aceptado live. La carrera simultanea de dos navegadores sigue pendiente. |
| AUD-050 | P1 | Desplegado; guardado mutable pendiente | El flujo Avaturn privado parsea el GLB completo, exige upper-body compatible, espera preview y thumbnail validos y descarta cargas asincronas obsoletas antes de habilitar Guardar. Tres pruebas de skeleton, check, lint y build pasaron. La UI live mantiene Guardar deshabilitado sin archivo; no se hizo un save real sin checkpoint. |
| AUD-051 | P2 | Desplegado; publish verificado, delete aislado pendiente | Dos guardados y una publicacion real de Spoke confirmaron sustitucion segura. Antes del publish se creo `output/magiclink-scene-prepublish-20260716-093347/`; el proyecto `qa3U3Ke` republico la escena existente `f6VKtim`, mantuvo 34 archivos activos y dejo nueve pares completos diferidos sin perder bytes. Reticulum valida la pareja fisica antes de reactivar un archivo, marca activos sin referencias tras 24 h y mueve pares fisicos huerfanos a `expiring` tras otra ventana, con lock por UUID y aislamiento por archivo. La prueba Delete debe seguir usando un duplicado no referenciado. |
| AUD-052 | P2 | Desplegado y verificado | `isAuthenticated()` valida `exp`, elimina estado malformed/expired y `getToken()` ya no devuelve credenciales obsoletas. Pasaron las cinco pruebas directas y las 68 pruebas completas con el glob AVA entre comillas; Actions y rollout live quedaron sanos. |
| AUD-053 | P1 | Desplegado y aceptado en vivo | Los cambios de configuracion de una sala se aplican a un ghost runner ya activo. La prueba controlada cambio `VJopCY3` de 5 a 10 y de nuevo a 5 bots; el mismo proceso siguio vivo y los navegadores recibieron el recuento correcto sin reinicio ni entidades obsoletas. |
| AUD-054 | P1 | Corregido, rollback probado | La actualizacion `js-cookie` 2 -> 3 retiro `Cookies.getJSON` y la primera candidata produjo pagina sin `APP`/`AFRAME`. Se detecto en carga fria, se revirtio inmediatamente al digest estable, se sustituyo por parseo JSON seguro con pruebas y se volvio a desplegar por Actions. Este incidente origina el gate de navegador obligatorio. |
| AUD-055 | P0 | Corregido, rotado y revocado | La clave OpenAI encontrada en la historia Git era la clave live anterior. Se creo una clave nueva, se valido con Models, Moderation y Responses, se aplico mediante el manifiesto estandar y un chat live respondio con `gpt-5-nano`; el orquestador cargo la huella esperada y el verificador quedo 0/0. Despues se revoco la clave anterior en el panel: la API devuelve HTTP 401 para la antigua y 200 para la nueva. Su copia temporal se elimino del llavero. No se registra ningun valor y Gitleaks del entregable actual esta limpio. |
| AUD-056 | P1 | Corregido y revocado | El PAT GitHub historico del mismo commit fue extraido en un entorno temporal `0600`, comprobado y revocado por API. Tras la revocacion devuelve 401. La historia no se reescribio. |
| AUD-057 | P1 | Corregido y verificado | Se renovo la credencial de paquetes en el llavero, ambos repos de Actions y `Secret/ghcr-pull`; el `ServiceAccount/default` conserva el pull secret. Se validaron pull de Hubs, Reticulum y bot-orchestrator por digest y permiso GHCR de inicio de upload sin publicar una imagen. El preflight termina con 0 fallos y 0 avisos. |
| AUD-058 | P1 | Corregido y verificado en CI; no desplegado | Los workflows custom ya no aceptan passwords de registry como inputs. Se anadieron Gitleaks, Actionlint y ShellCheck en CI, audits/builds de Hubs/Admin y suites de servicios. Los Actions usados por los nuevos workflows estan fijados por SHA y Gitleaks se instala con checksum. Pasaron Hubs Security `29518981250`, Storybook `29518980804`, cloud Security `29520235224`, Services `29520235446`, Reticulum `29519815859` y root Security `29519331721`. |
| AUD-059 | P2 | Corregido en rama; no desplegado | Dialog declaraba una variable global accidental, iteraba un `Map.values()` con `for...in` y no cerraba correctamente rutas 404. Las correcciones pasan lint y dos tests. Bot orchestrator, Dialog y Photomnemonic reciben healthchecks Docker adicionales; requieren nueva imagen para llegar a produccion. |
| AUD-060 | P1 | Medido | Hubs y root actuales tienen 0 vulnerabilidades Trivy. El snapshot completo de Hubs CE reporta 6 critical, 60 high, 55 medium y 8 low, concentradas en copias legacy embebidas, Spoke y Speelycaptor no desplegado. Las imagenes actuales separadas de Hubs/servicios pasan sus audits; Spoke necesita una modernizacion dedicada con Node 16 como baseline de compatibilidad. |
| AUD-061 | P2 | Automatizado | `scripts/audit-upstream.sh` confirma 0 commits de release pendientes, 79/79 commits propios y anticipa conflictos de `upstream/master` solo en workflows. `docs/development-workflow.md` y `docs/customization-inventory.md` convierten el upgrade en un proceso repetible. |
| AUD-062 | P1 | Verificado | `scripts/verify-project.sh --full` cubre Hubs, Admin, generator, bot orchestrator, Dialog, Photomnemonic, Coturn, Spoke y Reticulum. Pasaron 12 tests Hubs, 22 bots, 2 Dialog, 7 Photomnemonic, 1 Spoke y 305 Reticulum, ademas de builds y audits de produccion. |
| AUD-063 | P1 | Verificado | Se creo `output/backups/20260716-183112`: DB 49 KiB y storage 183 MiB, schema 356, 94 migraciones, 33 activos, 47 pares completos y 14 diferidos. Ambos restore dry-runs y todos los checksums pasan; incluye commits, digests e inventario no secreto. |
| AUD-064 | P2 | Corregido | La documentacion activa se redujo a fuentes vigentes. Runbooks sustituidos, freeze incompleto, investigacion Avaturn previa y parches antiguos estan indexados en `OLD/`. Se anadieron ciclo de vida por cliente, indice documental y reglas unicas en `AGENTS.md`. |

## 4. Cambios incorporados durante la auditoria

### Cliente Hubs

- Prompt de bot configurable por sala, limitado a 1500 caracteres.
- Mensajes y errores de chat bot en espanol sin exponer errores internos.
- Aviso visible de conversacion temporal y tratamiento por el proveedor de IA.
- El cliente envia solo nombres `spawbot-*` saneados como contexto.
- La accion de navegacion ya no se emite desde el navegador.
- Validacion GLB 2.0, limite 64 MiB, limpieza de object URLs y recuperacion de errores.
- Correccion de tercera persona al entrar en VR.
- Reserva de asientos bitECS: respeta `Can be occupied` y recorre candidatos hasta encontrar uno libre.
- Correccion del desbordamiento de notificaciones en pantallas estrechas.
- Resolucion runtime de assets importados desde JavaScript y cabecera Home movil sin solapamiento.
- Workflows reutilizables fijados por commit.

Validacion local:

- `npm run check`: correcto.
- `npm run lint:js`: correcto.
- `npm run build`: correcto, con warnings de tamano.
- El locale `es.json` contiene todas las claves del locale base `en.json`; los identificadores dinamicos requieren
  comprobacion visual porque no todos son claves directas.

### Bot orchestrator

- Autenticacion interna fail-closed con clave minima de 32 caracteres y comparacion constante.
- Express sin cabecera identificativa y body JSON limitado.
- Moderacion de entrada y salida con `omni-moderation-latest`.
- Responses API con `store:false`, salida corta, reasoning bajo y Structured Outputs con JSON Schema estricto.
- Rechazos de modelo convertidos a respuesta local segura sin exponer texto interno del proveedor.
- Rate limit agregado por sala y usuario, aunque cambie de bot.
- Fallback sin repetir el texto privado del usuario.
- Deteccion de navegacion por verbos completos; ya no confunde substrings como `ve` dentro de `brevemente`.
- Reconciliacion de salas y parada de runners obsoletos.
- Backend por defecto `ghost`; Chromium limitado a una sala si se activa.
- `phoenix-js` fijado a commit.
- Descarga de escenas same-origin por defecto, con redirecciones, bytes, JSON, nodos y aristas limitados.
- Parser glTF iterativo con deteccion de ciclos y transforms no finitos.
- Parser GLB parcial con HTTP Range, extraccion del `nav-mesh`, proyeccion de puntos y A* mediante
  `three-pathfinding`.
- Movilidad `static` que conserva chat pero bloquea rutas automaticas y acciones `go_to_waypoint`.

Validacion local:

- 22 pruebas automatizadas: correctas.
- `npm audit --omit=dev`: 0 vulnerabilidades.
- `node --check app.js` y `node --check run-ghost-runner.js`: correctos.
- Prueba real de `gpt-5-nano`: saludo con `action=null` y orden explicita con accion allowlist correcta.
- Escena live recuperada: GLB 2.0, 350 triangulos, 11 grupos de navmesh, 11 waypoints, 8 `spawbot-*` y 56/56
  rutas dirigidas.
- `npm run gen-hcce`: manifiesto verificado de 44 recursos con limites del ghost runner explicitos.

### Reticulum

- Configuracion de bots normalizada en 0..10 y prompt limitado.
- Endpoints internos fail-closed y comparacion constante.
- El identificador de cuenta solo viaja por la red interna y se seudonimiza antes de OpenAI.
- Acciones de bot validadas y emitidas por backend.
- Clave de bot minima de 32 caracteres tambien en el canal Phoenix.
- Se elimino un PAT hardcoded del build de Spoke.
- Runtime y Docker fijados en Elixir 1.18.4, OTP 27.3.4.14 y Alpine 3.21.7.
- Stack actualizado a Phoenix 1.6.17, Plug 1.16.6, Cowboy 2.15, Ecto 3.14 y Postgrex 0.22.3.
- Proxy CORS fail-closed con DNS/IP fijada, sin redirecciones, cookies, autorizacion ni forwarding del navegador.
- Entidades/subentidades aisladas por `hub_id`; borrado de cuentas reasigna tambien `avatar_listings`.
- Statix sustituido por un cliente StatsD UDP asincrono y supervisado compatible con OTP 27.
- Dockerfile reproducible sin imagen `alpine/openssl` flotante, sin `apk update` y con release copiada desde una ruta de build explicita.

Validacion de la rama candidata:

- `mix format --check-formatted`: correcto sobre todo Reticulum.
- Base PostgreSQL 14.23 recreada y todas las migraciones aplicadas desde cero: correcto.
- `mix compile --force --warnings-as-errors`: correcto para el codigo de aplicacion.
- `mix test`: 5 properties, 305 pruebas, 0 fallos y 3 excluidas.
- `MIX_ENV=turkey mix release --overwrite`: correcto.
- GitHub Actions `29506498175`: PostgreSQL 12.19, PostgreSQL 14.23 y release, todos correctos.
- `mix hex.audit`: solo dos avisos reconocidos de cowlib 2.18.0, actualmente la ultima version; revisar al publicarse parche.

### Generador Hubs CE

- Las configuraciones de cert-manager, redireccion SSL, clase de ingress y permisos HAProxy estan en el template.
- Se elimino la generacion del secreto autofirmado `cert-hcce`, que no tenia consumidor.
- El generador devuelve codigo distinto de cero si falla; ya no puede aparentar exito tras una excepcion.
- `npm run gen-hcce` valida el manifiesto y exige exactamente un `Service` de tipo `LoadBalancer`.
- Se elimino `node-forge` y se fijo `tmp` a una version corregida; `npm audit` del generador devuelve 0 avisos.
- El manifiesto generado se aplica sin modificaciones manuales. Una incidencia se corrige en el template y se regenera.
- Photomnemonic queda limitado por politica egress a DNS y web publica, con seguridad non-root y sondas verificadas
  tanto en el manifiesto generado como en el cluster live.

## 5. Privacidad y guardarrailes de bots

### Datos que procesa el backend

- `hub_sid`, `bot_id`, mensaje limitado a 800 caracteres y lista saneada de hasta 64 waypoints.
- Prompt de sala limitado a 1500 caracteres.
- Identificador de cuenta solo para rate limit y derivacion HMAC.

### Datos que no se almacenan en YenHubs

- No hay tabla ni fichero de conversaciones.
- El orquestador no escribe prompts ni respuestas en logs.
- El cliente no usa `localStorage` para historial del bot.
- Al cambiar o salir de sala, el estado React de conversaciones se elimina.
- Reticulum filtra parametros llamados `message` o `prompt` antes de registrarlos.

### Limite externo

`store:false` impide conservar estado de Responses para recuperacion por API. Aun puede existir retencion de monitoreo
de abuso de OpenAI durante hasta 30 dias salvo que la organizacion tenga Zero Data Retention aprobada. Antes de publico:

1. mantener visible y revisar legalmente el aviso corto de privacidad ya desplegado;
2. no pedir ni incentivar datos sensibles;
3. fijar prompt y politica de moderacion del evento;
4. decidir si hace falta solicitar ZDR;
5. ampliar periodicamente las pruebas en espanol de abuso y prompt injection ya iniciadas.

## 6. Aceptacion funcional y limites residuales

Aceptado en produccion:

- Home, lobby, sala recuperada, carga fria y assets/CSP.
- Audio/WebRTC real entre dos navegadores y Coturn/TCP.
- Spoke: carga, guardados y reconciliacion de storage sin perder pares fisicos.
- Third person desktop y retorno de modo por toggle.
- Sit/Stand basico, movimiento movil y perdida de ownership protegida.
- Avatares normales/full-body, featured, previews, UI Avaturn privada y validacion server-side.
- Ghost runner: late join, reconexion, cinco bots moviendose y cambio dinamico 5 -> 10 -> 5.
- Chat IA: moderacion, rate limit cruzado entre bots, prompt injection, waypoint allowlist, logs y aviso de privacidad.
- Capturas comparables de sala, chat, avatar browser y guia Avaturn en desktop/tablet/movil.

Limites que requieren otra campana y no bloquean este cierre:

- VR fisico para validar la prioridad de primera persona.
- Carrera real de dos usuarios ocupando el mismo asiento.
- Save de un nuevo Avaturn y Publish/Delete sobre un duplicado Spoke no referenciado, ambos con checkpoint dedicado.
- Cinco salas de bots y carga concurrente representativa de alto CCU/WebRTC.
- ZDR de OpenAI y revision legal del aviso para un evento publico.

## 7. Criterio de salida

La auditoria se cierra porque:

1. los P0 runtime estan corregidos; la clave OpenAI live fue sustituida y la
   anterior esta revocada con rechazo HTTP 401 confirmado;
2. el baseline y el candidato coherente se restauraron desde backup y pasaron smoke tests;
3. las imagenes activas estan registradas por digest;
4. existe rollback probado de Hubs y restauracion verificada de DB/storage;
5. el flujo IA tiene aviso de privacidad y pruebas de guardarrailes;
6. la auditoria visual tiene capturas comparables y correcciones verificadas;
7. los limites no ensayados estan documentados sin afirmaciones falsas de cobertura;
8. el preflight demuestra pull de las imagenes privadas y termina 0/0.

## 8. Referencias operativas

- Indice: `/Users/Shared/Gits/YenHubs/docs/README.md`
- Despliegue: `/Users/Shared/Gits/YenHubs/deployment/README.md`
- Ciclo de vida: `/Users/Shared/Gits/YenHubs/deployment/client-instance-lifecycle.md`
- Handoff final: `/Users/Shared/Gits/YenHubs/docs/project-handoff-2026-07.md`
- Actualizaciones upstream: `/Users/Shared/Gits/YenHubs/docs/development-workflow.md`
- Inventario de personalizaciones: `/Users/Shared/Gits/YenHubs/docs/customization-inventory.md`
- Bots: `/Users/Shared/Gits/YenHubs/features/bots/README.md`
- Checkpoint vigente: `/Users/Shared/Gits/YenHubs/output/backups/20260716-183112/`
- OpenAI data controls: <https://developers.openai.com/api/docs/guides/your-data>
- OpenAI Structured Outputs: <https://developers.openai.com/api/docs/guides/structured-outputs>
- Hubs Foundation: <https://github.com/Hubs-Foundation/hubs>
- Hubs CE: <https://github.com/Hubs-Foundation/hubs-cloud>
